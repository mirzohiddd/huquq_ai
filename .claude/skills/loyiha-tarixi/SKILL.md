---
name: loyiha-tarixi
description: Huquq AI loyihasining to'liq texnik tarixi va qaror jurnali (2026-08-02 dan buyon, 66 ta yozuv) — AI/RAG tizimi, tarjima va til almashtirish, darslar va Konstitutsiya sahifalari, qonunlar kutubxonasi, to'lov/tarif tizimi, email provayderlari, mobil tuzatishlar, tezlik optimizatsiyalari. Har bir yozuvda: nima o'zgardi, NIMA UCHUN aynan shunday qilindi, nimaga ATAYLAB tegilmadi va qanday tuzoqlar aniqlangan. MAVJUD funksiyani o'zgartirishdan yoki nosozlikni tekshirishdan OLDIN shu skillni yuklang — aks holda allaqachon hal qilingan muammo qaytadan takrorlanadi.
---

# Huquq AI — loyiha tarixi va texnik qarorlar

Bu jurnal ilgari `CLAUDE.md` ning `## Loyiha holati` bo'limi edi va har bir sessiyaga
yuklanardi (~38 300 token). Endi u faqat kerak bo'lganda yuklanadi — mazmuni
o'zgarmagan, bir qatori ham o'chirilmagan.

Eng muhim TAQIQLAR `CLAUDE.md` da qoldirilgan (ular har doim yuklanishi shart).
Quyida to'liq tarix, eng eskisidan boshlab.

## Loyiha holati (kontekst uchun)

- `frontend/` — asosiy sayt (Vite + React). `admin/` — **haqiqiy ishlatiladigan** standalone admin dashboard ilovasi (alohida login/Dashboard/Users/Payments/... sahifalari, `https://huquq-ai-admin.vercel.app`ga deploy qilinadi). `backend/` — Express + MongoDB API. Diqqat: `frontend/src/pages/AdminPanel.jsx` (`/admin/*` route) — bu ESKI/ishlatilmaydigan ikkinchi admin panel, `admin/` app real ishlatiladigani. Ikkalasi ham (agar ishlatilsa) bitta backendga ulanadi.
- **Tarif/Credit tizimi (2026-08-02 qurildi):** markazlashgan konfiguratsiya `backend/config/plans.js` da — 4 ta tarif: `free` (5/kun), `basic` (30/kun, 19 000 so'm/oy), `pro` (200/kun, 49 000 so'm/oy, Claude), `premium` (500/kun, 99 000 so'm/oy, Claude). Narx/limit raqamlari — mening taxminim, haqiqiy biznes raqamlari bilan almashtirish kerak (`.env`: `BASIC_PLAN_PRICE_UZS`, `BASIC_PLAN_DAILY_LIMIT`, `PRO_PLAN_PRICE_UZS`, `PRO_DAILY_LIMIT`, `PREMIUM_PLAN_PRICE_UZS`, `PREMIUM_PLAN_DAILY_LIMIT`). `User.plan` shu 4 qiymatdan birini oladi (`backend/models/index.js`), limit har doim `getUserTier()` + `getPlanConfig()` orqali AVTOMATIK hisoblanadi (`backend/middleware/usageLimit.js`) — admin qo'lda limit belgilamaydi (bu funksiya ikkala admin ilovasidan ham olib tashlandi, o'rniga tarif tanlash dropdown qo'shildi).
- `backend/routes/payment.js`: `GET /plans` — barcha tariflar ro'yxati (Landing Page Pricing uchun tayyor), `POST /checkout` — `{provider, tier}` qabul qiladi. Click/Payme haqiqiy merchant kalitlari hali kiritilmagan (`.env`da placeholder) — https://merchant.click.uz va https://business.payme.uz'dan olinishi kerak.
- **Pricing (2026-08-02 qurildi):** `frontend/src/components/PricingSection.jsx` — Landing Page'da (`Home.jsx`, Contact bilan CTA orasida, `id="pricing"`, nav'da "Narxlar" havolasi bilan) barcha 4 tarifni ko'rsatadi, login qilinmagan foydalanuvchi "Sotib olish" bossa tanlovi `localStorage.pendingCheckout`ga saqlanib `/login`ga yo'naltiriladi, login qilingandan keyin `SiteLayout.jsx`dagi effekt avtomatik checkout'ni davom ettiradi. `ProfileModal.jsx`dagi "Pro" tab hali ham bor (eski, faqat "pro" tarifni sotib olish) — endi asosiy xarid yo'li Landing Page, Profil esa faqat joriy holatni ko'rsatish uchun qoladi.
- `admin/src/pages/Payments.jsx` — yangi qo'shildi, to'lovlar ro'yxati + statistika (`/admin/payments`, `/admin/payments/stats`).
- **Landing Page kengaytmasi (2026-08-02):** yangi bo'limlar qo'shildi — `AICapabilities.jsx` (AI imkoniyatlari, dark glass-effect kartalar), `WhyUs.jsx` (Nima uchun aynan biz), `Testimonials.jsx` (foydalanuvchilar fikri — HOZIRCHA PLACEHOLDER matnlar, haqiqiy sharhlar bilan almashtirilishi kerak), `FAQ.jsx` (akkordeon). Barchasi `frontend/src/components/` ichida, uch tilda (uz/ru/en) to'liq tarjima qilingan (`i18n/translations.js`). Joylashuv: Stats'dan keyin AICapabilities+WhyUs, Pricing'dan oldin Testimonials+FAQ — mavjud anchor havolalar (#about/#services/#articles/#contact) buzilmasligi uchun eski bo'limlar joyidan siljitilmadi.
- **Header tarif ko'rsatkichi (2026-08-02):** `SiteLayout.jsx` endi login qilgan foydalanuvchi uchun navbar'da tarif nomi + qolgan kun sonini ko'rsatadi (`GET /api/payment/plan` orqali, `profileOpen` yopilganda ham yangilanadi).
- **AI ishlatilishi kuzatuvi (2026-08-02):** `DailyStat.providers.{claude,groq,cloudflare,gemini}` — har bir AI javobi qaysi provayderdan kelgani endi qayd etiladi (`legalAI.js` `getLegalAdvice()` natijasida `provider` maydonini qaytaradi, `chat.js` → `stats.js` `recordStat()` orqali saqlanadi). Ikkala admin ilovasi Dashboard'ida "Claude (pullik) vs bepul provayderlar" statistikasi ko'rinadi — Pro/Premium AI xarajatini nazorat qilish uchun.

- **BARCHA KODEKSLAR + admin panelidan boshqarish (2026-08-06):** kutubxonada 6 ta hujjat bor edi, endi **20 ta** (O'zbekistonning amaldagi kodekslari + iste'molchi qonuni). Bundan muhimrog'i — ro'yxat endi KODDA emas, BAZADA va admin panelidan boshqariladi.
  - **Yangi arxitektura:** `LawSource` modeli (`models/index.js`) — hujjatlar reyestri; `services/lawRegistry.js` — yagona kirish nuqtasi (`getLaws`, `seedLawSources`, `syncFromConfig`, 60s kesh); `routes/adminLaws.js` — to'liq CRUD + `POST /verify` + `POST /:id/ingest` + `POST /sync`; `admin/src/pages/Laws.jsx` — admin UI (`/laws` marshruti, yon menyuda "Qonunlar").
  - **`config/laws.js` endi faqat SEED** ("zavod sozlamalari"): kolleksiya bo'sh bo'lsa server ishga tushganda bir marta ko'chiriladi. Mavjud yozuvlarga TEGILMAYDI — admin o'zgartirgan ma'lumot har restartda bosib ketilmasligi kerak. `TRACKED_LAWS` ni to'g'ridan-to'g'ri o'qiydigan joylar (`routes/library.js`, `scripts/ingestLaws.js`) `lawRegistry` ga o'tkazildi — aks holda admin qo'shgan kodeks ba'zi joyda ko'rinib, ba'zi joyda ko'rinmay qolardi.
  - **⚠️ HAR BIR lex.uz ID QO'LDA TEKSHIRILGAN (2026-08-06):** 20 ta hujjatning har biriga haqiqiy HTTP so'rov yuborilib, sahifa ochilgani, sarlavhasi va moddalar soni tasdiqlangan. Bu shart edi — mening dastlabki TAXMINLARIM (`-2304140` Budjet, `-111017` Havo) 404 qaytardi. Eskirgan tahrirlar ataylab CHETLAB o'tildi va `config/laws.js` da izohda qayd etildi: `-142859` (Mehnat 1995), `-152637`/`-1286558` (Soliq 1997/2007), `-186105` (FPK 1997).
  - **Yangi qo'shilganlar:** FK2 (Fuqarolik k. 2-qism), MJK, FPK, JPK, JIK, IPK, MSK, SK (Soliq), BJK (Bojxona), BK (Budjet), UJK (Uy-joy), SHK (Shaharsozlik), HK (Havo), SYK (Saylov).
  - **Admin nima qila oladi:** kodeks qo'shish (lex.uz havolasi yoki faqat ID — `-4674902` ham, to'liq havola ham qabul qilinadi), tahrirlash, o'chirish, vaqtincha o'chirib qo'yish (`enabled`), lex.uz'dan qayta yuklashni ishga tushirish. **Saqlashdan OLDIN manzil tekshiriladi** — sahifa ochiladimi, moddalarga bo'linganmi; xato bo'lsa saqlanmaydi (aks holda bazaga yaroqsiz yozuv tushib, yuklash jim yiqilardi). Sinov: 404 ID va begona sayt — ikkalasi ham to'g'ri rad etildi.
  - **Nozik jihatlar:** (a) **kod o'zgartirilganda** o'sha kodeksning BARCHA moddalari ham ko'chiriladi (`LegalChunk.lawCode`), aks holda ular "yetim" bo'lib kutubxonada ko'rinmay qolardi; (b) **o'chirishda moddalar standart holatda QOLADI** — `?withArticles=1` berilgandagina o'chiriladi (minglab modda va embeddingni tasodifan yo'q qilib qo'ymaslik uchun, admin UI ikki marta so'raydi); (c) **yuklash FON rejimida** — javob darhol qaytadi, natija `lastIngestAt`/`lastIngestStats`/`lastIngestError` ga yoziladi va ro'yxatda ko'rinadi.
  - **AI qidiruvi ham kengaytirildi:** `CATEGORY_TO_LAWCODE` (legalAI.js) endi MASSIV qabul qiladi — `civil: ["FK","FK2"]`, `real_estate: ["YK","UJK","SHK"]`, yangi `tax: "SK"`, `administrative: ["MJK","MSK"]`. Buning uchun `legalRetrieval.js` ga `lawCodeFilter()` qo'shildi (bitta kod → `$eq`, ro'yxat → `$in`) — avval faqat bitta kod berilardi va savolning yarmi qidiruvdan tashqarida qolardi. `lessonPractice.js` dagi `TOPIC_LAW_CODE` ham tuzatildi: **meros FK emas FK2 da** (ikkinchi qism), **iste'molchi FK emas IHQ da** — ikkalasi ham noto'g'ri kodeksdan qidirilardi.
  - **Kodeks nomlari:** 20 tasining rus/ingliz rasmiy nomi QO'LDA yozilgan (aniqroq va o'zgarmas). Admin tarjimasiz kodeks qo'shsa — nom AI tarjimasiga tushadi (`translateLawNames`), aks holda ruscha rejimda o'zbekcha nom ko'rinib qolardi.
  - **Sinovlar:** admin CRUD 6/6 (qo'shish, takroriy kod rad etilishi, yaroqsiz manzil rad etilishi, tahrirlash, o'chirilgan hujjat kutubxonada ko'rinmasligi, o'chirish), `verify` 4/4, kutubxona uchala tilda.
  - **⚠️ Yuklash UZOQ davom etadi:** har bir modda uchun embedding kerak (~0,3 s). 20 ta hujjat ≈ 8000+ modda ≈ 40-60 daqiqa. Server ishga tushganda fon rejimida ketadi (bloklamaydi) va har 24 soatda `contentHash` orqali faqat O'ZGARGANLARI qayta ishlanadi.

- **Kutubxonada BARCHA kodekslar va BARCHA moddalar ko'rinadi (2026-08-06):** foydalanuvchi so'roviga ko'ra ikkita cheklov olib tashlandi.
  - **(1) Barcha kodekslar.** Avval `articleCount > 0` filtri bor edi — moddalari hali yuklanmagan kodeks saytda UMUMAN ko'rinmasdi va foydalanuvchi uni yo'q deb o'ylardi. Endi 20 tasi ham ro'yxatda turadi; yuklanmagani "Tayyorlanmoqda…" holatida, xiraroq va bosilmaydi (`.lawCardPending`, `pointer-events: none`). Javobga `ready` maydoni qo'shildi.
  - **(2) Barcha moddalar.** Sahifalash (40 tadan, FK2 uchun 21 sahifa) BUTUNLAY olib tashlandi — kodeksning barcha moddalari bir yo'la keladi. Buni imkonli qilgan narsa: ro'yxatdan modda MATNI olib tashlandi (`select("articleNumber title -_id")`). Matn allaqachon modda OCHILGANDA alohida so'rov bilan kelardi, ya'ni ro'yxatda u behuda yuborilardi. `lawName`/`sourceUrl` ham olib tashlandi — ular har bir modda uchun bir xil va javobda 800 marta takrorlanardi (ular javobning `law` qismida bor). **O'lchov (FK2, 810 modda): 179 KB → 70 KB, 710 ms → 420 ms** — ya'ni to'liq ro'yxat eski BITTA sahifadan ham tez keladi.
  - **⚠️ TARJIMA ENDI BLOKLAMAYDI.** 800 ta sarlavhani AI bilan tarjima qilish ~2 daqiqa oladi — sahifa shuncha ochilmasdi. Yangi `translateManyCached()` (AI'ni UMUMAN chaqirmaydi, faqat keshdan o'qiydi) + `warmTranslate()` (fonda to'ldiradi, `MAX_BACKGROUND_JOBS = 2`) qo'shildi. Oqim: sahifa DARHOL ochiladi (keshdagi tarjimalar bilan, qolgani manba tilida) → javobda `pendingTranslation` soni qaytadi → frontend "Tarjima tayyorlanmoqda…" ko'rsatadi va 15 s dan keyin JIMGINA qayta so'raydi. Sinov: Uy-joy kodeksi (141 modda), birinchi so'rov **413 ms** (140 ta tarjimasiz), 20 soniyadan keyin **141/141 tarjima qilingan**.
  - **Moddalar tartibi tuzatildi:** avval `parseInt` bilan saralanardi va qo'shimcha moddalar ("346-1", "346-2") bir xil qiymat berib, tasodifiy tartibda chiqardi. Endi ikkala qism ham solishtiriladi (346 → 346-1 → 346-2 → 347). Uch kodeksda tekshirildi.
  - **⚠️ O'ZIM KIRITGAN XATO — topildi va tuzatildi:** ro'yxatdan `text` olib tashlangach, frontenddagi `(articleText[num] ?? a.text).split("\n")` ifodasi `undefined.split()` ga aylanib, modda matnini olish so'rovi muvaffaqiyatsiz bo'lgan HAR QANDAY holatda sahifani butunlay buzardi. Endi `a.text` ga umuman tayanilmaydi: matn yo'q bo'lsa yuklanish yoki "topilmadi" xabari ko'rsatiladi.
  - Yangi kalitlar: `library_preparing`, `library_translating` (uch tilda). Qidiruv o'zgarmadi va ishlayapti (Mehnat kodeksida "ta'til" → 84 natija).

- **TEZLIK — 3 ta haqiqiy sabab topildi va tuzatildi (2026-08-06):** foydalanuvchi "sayt juda sekin, ma'lumotlar sekin kelyapti" dedi. Taxmin qilinmadi — har bir so'rov `explain('executionStats')` bilan o'lchandi. **Ildiz sabab: har bir `LegalChunk` hujjati ~19,6 KB** (1024 o'lchamli embedding shuncha joy oladi), 6948 modda = ~136 MB kolleksiya. Shu sabab hujjatlarga tegadigan har qanday so'rov qimmatga tushardi.
  1. **Kutubxona ro'yxati butun kolleksiyani skanerlardi.** `/library/laws` har so'rovda `$group` bilan modda sonini hisoblardi va MongoDB **COLLSCAN** qilardi — ya'ni ~136 MB o'qish (o'lchangan: 1078 ms). Ikki chora: `.hint({ lawCode: 1 })` (endi PROJECTION_COVERED + IXSCAN, hujjatlar umuman o'qilmaydi) va natijani 60 s ga keshlash. **1078 ms → 223 ms (sovuq) / 4 ms (issiq).**
  2. **Moddalar ro'yxati 810 ta to'liq hujjatni o'qirdi.** Faqat raqam va sarlavha kerak edi, lekin indeks `title` ni qamramagani uchun MongoDB har bir 19,6 KB hujjatni diskdan o'qirdi (~16 MB, 524 ms). Yangi **qoplovchi indeks** `{ lawCode, articleNumber, title }` (`law_list_covered`) qo'shildi → `totalDocsExamined: 0`, server tomonida **1 ms**. Qidiruv ham tezlashdi: **1146 ms → 119 ms**.
  3. **⚠️ ENG KATTASI — server har ishga tushganda 16 000 ta baza so'rovi qilardi.** `ingestLaw()` har bir modda uchun ALOHIDA `findOne()` va (o'zgarmaganlar uchun) `updateOne()` bajarardi. 20 kodeks × ~7000 modda = ~16 000 KETMA-KET so'rov; Atlas'gacha borish-kelish ~30-50 ms bo'lgani uchun bu **8-13 daqiqa sof kutish**, va bu Render har qayta ishga tushganda takrorlanardi — server shu vaqt davomida so'rovlarga zo'rg'a javob berardi. Endi: bitta kodeksning barcha hash'lari BITTA so'rovda olinadi, `lastCheckedAt` esa bitta `updateMany` bilan yangilanadi (**16 000 → kodeks boshiga 2 ta**). Qo'shimcha: (a) oxirgi tekshiruvdan **20 soat** o'tmagan kodeks butunlay o'tkazib yuboriladi (`lastIngestAt`), (b) ishga tushishdagi yuklash **30 soniya kechikish** bilan boshlanadi — server avval so'rovlarga javob bera boshlaydi. Sinov: server qayta ishga tushganda yuklash **0,6 s** va **0 ta kodeks** ishlandi (avval 30+ daqiqa); admin panelidan qo'lda yuklash cheklovga tushmaydi va ishlaydi (`onlyCode` yo'li).
  - **Yakuniy o'lchov (lokal, issiq):** kodekslar ro'yxati 4 ms · FK2 (810 modda) 207 ms · JPK (763 modda) 213 ms · qidiruv 109 ms · darslar 100 ms · testlar 96 ms · sayt matni 95 ms.
  - **Barcha 20 ta kodeks yuklandi: 6948 modda** (32 daqiqada). BJK 419 · BK 179 · FK 386 · FK2 810 · FPK 508 · HK 136 · IHQ 33 · IPK 390 · JIK 199 · JK 404 · JPK 763 · MJK 662 · MK 581 · MSK 306 · OK 220 · SHK 85 · SK 500 · SYK 116 · UJK 141 · YK 110.
  - **Kelajakda:** eng katta qolgan zaiflik — `embedding` maydoni hujjat ichida turgani. Uni alohida kolleksiyaga ko'chirish har qanday hujjat o'qishni ~20 barobar yengillashtiradi, lekin bu RAG qidiruviga ta'sir qiladigan katta refaktor, shuning uchun hozir qilinmadi.

- **KONSTITUTSIYA — alohida sahifa (2026-08-06):** `/constitution` marshruti, `pages/Constitution.jsx` + `.module.css`. Barcha **155 modda TO'LIQ MATNI bilan**, **27 bob** bo'yicha guruhlangan holda BIR sahifada. lex.uz manbasi `-6445145` (2023-yil 30-aprel tahriri) — tekshirilgan.
  - **Kutubxonadan farqi ataylab:** u yerda moddalar RO'YXAT (akkordeon, matn ochilganda keladi), bu yerda esa O'QISH uchun HUJJAT — hamma narsa ochiq, akkordeon ham, sahifalash ham yo'q. Konstitutsiya kichik (155 modda, ~103 KB), shuning uchun to'liq matn bir yo'la kelishi mumkin (o'lchangan: 492 ms).
  - **Tuzilma:** to'q ko'k hero (ikona + nom + modda/bob soni + lex.uz havolasi) → qidiruv → ikki ustun: chapda **yopishqoq mundarija** (27 bob, har birida modda soni; scroll paytida joriy bob `IntersectionObserver` bilan ajratiladi), o'ngda boblar bo'yicha matn. "Yuqoriga" tugmasi 700px dan keyin chiqadi. Mundarijaga `data-scroll-lock` qo'yilgan — uning ustida g'ildirak burilganda sahifa qimirlamaydi.
  - **Bob xaritasi — yangi `parseChapterMap()`** (`scripts/ingestLaws.js`). ⚠️ Mavjud `parseArticles()` ATAYLAB o'zgartirilmadi: u 20 ta kodeks uchun to'g'ri ishlab turibdi va unga tegish hammasini buzish xavfini tug'dirardi. Bob sarlavhalari lex.uz'da alohida klassda (`search-text`) va `parseArticles` ishlatadigan `.CLAUSE_DEFAULT`/`.ACT_TEXT` to'plamiga umuman kirmaydi. `LegalChunk` ga `chapter` maydoni qo'shildi. Natija: **155/155 modda bobga biriktirildi**.
  - **Yangi endpoint** `GET /api/library/constitution` — boblar bo'yicha guruhlangan, to'liq matn bilan, qidiruv bilan (sinov: "mehnat" → 4 bobda 9 modda). Tarjima kutubxonadagi kabi BLOKLAMAYDI (kesh + fon).
  - **Kutubxonada KO'RINMAYDI (2026-08-06, foydalanuvchi so'rovi):** "library sahifasida faqatgina kodekslar bo'lishi kerak". Buning uchun `LawSource` ga **`showInLibrary`** maydoni qo'shildi va Konstitutsiya uchun `false` qilindi.
    - **`enabled` dan FARQI muhim:** `enabled: false` — hujjat umuman ishlatilmaydi (lex.uz'dan yangilanmaydi, AI qidiruvida qatnashmaydi); `showInLibrary: false` — hujjat TO'LIQ ishlaydi, faqat kutubxona ro'yxatida ko'rinmaydi. Konstitutsiyani reyestrdan butunlay olib tashlash NOTO'G'RI bo'lardi — u holda avtomatik yangilanish ham, AI qidiruvi ham yo'qolardi.
    - Eski yozuvlarda maydon yo'q → `undefined !== false` → `true`, ya'ni mavjud 20 ta kodeks avvalgidek ko'rinadi (orqaga moslik). Bazada ular aniq belgilandi.
    - Admin panelida alohida belgi qo'shildi ("Qonunlar kutubxonasida ko'rinsin") — kelajakda boshqa hujjatga ham alohida sahifa ajratilsa, kodga tegmasdan boshqarish mumkin.
    - **Sinov 4/4:** kutubxonada 20 ta hujjat va KONS yo'q · `/constitution` ishlaydi (155 modda, 27 bob) · reyestrda `enabled: true`, ya'ni yangilanishda qatnashadi · RAG qidiruvi Konstitutsiya moddalarini topadi (masalan mehnat huquqi bo'yicha 42-modda).
  - Menyuda "Konstitutsiya" bandi Testlar bilan Kutubxona orasida. 10 ta yangi tarjima kaliti (uch tilda).
  - **⚠️ UZILISH HIMOYASI qo'shildi (circuit breaker) — haqiqiy muammodan keyin:** sinov paytida BARCHA bepul AI provayderlarning kunlik kvotasi tugadi (Gemini 20 so'rov/kun, Cloudflare 10 000 neuron/kun, Groq 100 000 token/kun — uchalasi ham). Natijada tizim **1610 marta behuda provayder chaqiruvi** qildi: har tarjima urinishi zanjirdagi hamma provayderni sinardi va hammasi 429 qaytarardi. Endi `translator.js` ketma-ket xatolarni sanaydi — **6 tadan keyin tarjima 15 daqiqaga to'xtatiladi** (`translationStatus()` bilan holatni ko'rish mumkin). Bu davrda sahifalar avvalgidek ishlaydi, matn manba tilida ko'rsatiladi. Muvaffaqiyatli tarjima hisoblagichni nolga qaytaradi. 5 ta test bilan tekshirilgan (AI'siz, soxta modul bilan).
  - **Tekshirildi:** sinflar 27/27, tarjima kalitlari 13/13 uchala tilda, backend 6 fayl toza, frontend va admin build toza.

- **MOBIL XATOLAR — auditdan keyin 6 ta tuzatish (2026-08-06):** foydalanuvchi "mobil versiyada dizaynda juda ko'p buglar" deb xabar berdi (aniq misollar: matn pastki panel ortida yashirinadi, Konstitutsiya sahifasi yo'q). Taxmin qilinmadi — o'lchandi va butun loyiha skanerlandi.
  1. **Kontent pastki panel ostida qolardi.** `.main`/`.footer` ga 64px bo'shliq berilgan edi, lekin panel HAQIQATDA ko'proq joy egallaydi: panel 59,6px + markazdagi AI doirasi paneldan 6px yuqoriga chiqadi (`margin-top: -18px`) + atrofidagi 4px oq halqa = **69,6px**. Ya'ni 5,6px kontent panel ostida qolardi. Endi **84px** (+ ~14px zaxira) va qiymat **`--bottom-nav-space`** o'zgaruvchisiga chiqarildi — boshqa sahifalar ham (Konstitutsiyadagi "Yuqoriga" tugmasi) shundan foydalanadi.
  2. **⚠️ Konstitutsiya sahifasi telefonda UMUMAN ochilmasdi.** Sabab tizimli edi: burger menyu `NAV_USER` ning HAMMASINI qattiq chetlab o'tardi ("ular baribir pastki panelda"), lekin yangi qo'shilgan Konstitutsiya pastki panelda YO'Q edi — natijada u na panelda, na burgerda ko'rindi. Endi `BottomNav.jsx` **`BOTTOM_NAV_PATHS`** ni eksport qiladi va burger menyu aynan shu ro'yxat bo'yicha chetlab o'tadi — ya'ni menyuga qo'shilgan har qanday YANGI band avtomatik burgerga tushadi va bu xato qaytarilmaydi.
  3. **`BOTTOM_NAV_PATHS` importi tushib qolgan edi** — skript yarmida to'xtaganda. Build O'TARDI (Vite named importni tekshirmaydi), lekin sahifa ishga tushganda `undefined.includes()` bilan qulardi. Import/eksport muvofiqligini tekshiradigan skript yozildi va butun loyihada ishga tushirildi (0 xato).
  4. **Konstitutsiya mundarijasi telefonda noqulay edi:** 27 ta bob ro'yxati kontentdan OLDIN, 220px blok bo'lib turardi va har safar uni aylanib o'tishga to'g'ri kelardi. Endi u **yig'ilgan holatda boshlanadi** (ochish tugmasi + bob soni), bob tanlangach avtomatik yopiladi. Desktopda hech narsa o'zgarmadi (tugma `display: none`).
  5. **Uzun so'z himoyasi yo'q edi.** Bitta uzilmaydigan uzun matn (havola, murakkab yuridik atama) telefonda butun sahifani kengaytirib, gorizontal scroll hosil qilishi mumkin edi. `global.css` ga `body { overflow-wrap: break-word }` qo'shildi — u faqat SIG'MAGAN so'zga ta'sir qiladi.
  6. **Konstitutsiya qidiruv paneli 360px ekranda siqilib qolardi** (kiritish maydoni ~125px). Ixchamlashtirildi → **176px**.
  - **Yo'l-yo'lakay:** `ChatDrawer.jsx` da mavjud BO'LMAGAN CSS sinfiga havola bor edi (`undefined` qiymat) — ko'rinish inline uslublar bilan berilgani uchun vizual muammo yo'q edi, o'lik havola olib tashlandi.
  - **Tekshirilgan, lekin muammo TOPILMAGAN** (soxta signal bermaslik uchun): barcha ko'p ustunli grid'lar mobilda yig'iladi (Library 600px, Tests 700px, PlatformFeatures 560px, Legal 880px — breakpointlari boshqacha bo'lgani uchun dastlab "yo'q" deb ko'ringan edi); `Lessons.ctaCard::before` dagi 420px bezak `overflow: hidden` bilan qirqiladi; `FloatingConsultButton` telefonda allaqachon yashirilgan; `nowrap` ishlatilgan joylar — qisqa yorliqlar (nishon, tugma), xavf yo'q.
  - **Yakuniy holat:** 381 ta CSS sinfi — barchasi mavjud (0 xato), import/eksport 0 xato, build toza. Telefonda yetib boradigan sahifalar: Bosh sahifa · Darslar · AI · Testlar · Kutubxona (pastki panel) + Konstitutsiya · Profil · Biz haqimizda guruhi (burger).

- **Qidiruv — modda raqami bo'yicha ANIQ moslik (2026-08-06):** foydalanuvchi "modda raqami bilan qidirsam boshqa moddalar ham chiqyapti" dedi. Sabab: qidiruv har doim `$or` bilan ishlardi — `{articleNumber: "23"}` YOKI `{text: /23/}`, ya'ni matnida "23" uchraydigan HAMMA modda chiqardi (sana, yosh, summa, boshqa moddaga havola). O'lchandi: `23` so'roviga Konstitutsiyada **2 ta**, Mehnat kodeksida **5 ta** natija qaytardi — kerakligi esa bittasi. (Raqam kichik ko'rinsa ham, foydalanuvchi uchun natija noto'g'ri: aynan kerakli modda o'rniga aralash ro'yxat chiqardi.)
  - Yangi `articleNumberQuery()` + `buildSearchFilter()` (`routes/library.js`): so'rov modda raqamiga o'xshasa — FAQAT `articleNumber` bo'yicha aniq moslik; aks holda avvalgidek sarlavha va matn ichidan qidiriladi.
  - Tanib olinadigan shakllar: `23` · `23-modda` · `23 modda` · `23.` · `61-1` · `276-13-modda` · `статья 23` · `23 статья` · `article 23` (uch til, qo'shimcha moddalar bilan). "23 yosh" yoki "mehnat 23" kabi so'rovlar esa MATN qidiruvi bo'lib qoladi.
  - **Ikkala joyda ham tuzatildi** — Konstitutsiya sahifasi va qonunlar kutubxonasi bir xil xatoga ega edi, endi yagona funksiyadan foydalanadi.
  - **Sinov 16/16** (11 ta raqam shakli + 5 ta matn holati), keyin haqiqiy server bilan: `23` → 1 ta, `23-modda` → 1 ta, `61` → 1 ta; matn qidiruvi buzilmagan (`mehnat` → Konstitutsiyada 9, Mehnat kodeksida 480 ta).

- **DARSLAR — faktlar bilan asoslandi va 6 tasi ham to'ldirildi (2026-08-06):** foydalanuvchi "matnini ko'proq qil, faktlar bilan tushuntir" dedi.
  - **⚠️ FAKT MANBASI — bazadagi HAQIQIY qonun matni.** Faktlarni o'zim yozish (modda raqami, muddat, summa) noto'g'ri va xavfli bo'lardi. O'rniga har bir bo'limga bazadagi haqiqiy moddaga HAVOLA qo'yildi va matn so'rov vaqtida `LegalChunk` dan JONLI olinadi (`routes/lessons.js` → `attachLawQuotes`). Ya'ni lex.uz'dan qonun yangilanganda (har 24 soatda) darsdagi iqtibos ham o'z-o'zidan yangilanadi. Matn darsda NUSXALANMAYDI — aks holda eskirib, foydalanuvchini chalg'itardi.
  - **Moddalar qo'lda tanlandi va tekshirildi** (kalit so'z qidiruvi noaniq natija berdi — sarlavha bo'yicha aniq qidiruv ishlatildi): MK 106/104 (shartnoma shakli va mazmuni), MK 243/245 (ish haqi), MK 182/183 (ish vaqti), MK 217 (ta'til), MK 155 (bekor qilish asoslari), OK 13/14 (nikoh tuzish), OK 23 (er-xotin umumiy mulki), OK 44 (ajralishda sud hal etadigan masalalar), IHQ 13/14/16 (nuqsonli tovar), FK2 1116/1118/1119/1124/1144 (meros), YK 22, UJK 11, JK 14/42, KONS 27. **Jami 29 ta havola.**
  - **Qolgan 3 ta dars ham yozildi:** Meros huquqi 696 → **4800** (6,9×), Yer va ko'chmas mulk 738 → **3524** (4,8×), Jinoiy javobgarlik 680 → **3767** (5,5×). Endi oltala dars ham to'liq: **jami 29 443 belgi** (avval ~4400 edi).
  - **Interfeys:** har bo'limda "QONUN NIMA DEYDI" bloki — modda raqami, sarlavhasi, haqiqiy matni (700 belgidan uzunlari qisqartiriladi) va kutubxonaga havola. Iqtibos oltin chiziq bilan ajratilgan, kursiv — qonun matni ekani darrov ko'rinadi.
  - **⚠️ Yo'l-yo'lakay tuzatilgan xato:** kontentni yozishda Python heredoc ichidagi `\n` haqiqiy qator uzilishiga aylanib, JS satrlarini buzdi va fayl sintaktik yaroqsiz bo'ldi. `node --check` buni darrov tutdi; buzuq qism olib tashlanib, kontent xom (raw) fayl orqali qayta qo'shildi.
  - Yangi kalitlar: `lesson_law_says`, `lesson_law_full` (uch tilda). Tekshirildi: CSS sinflari, tarjima kalitlari 3 tilda, backend 5 fayl va frontend build toza.

- **DARSLAR haqiqiy darsga aylantirildi (2026-08-06):** foydalanuvchi "har bir darslikda juda sodda va ko'p tushuntirish kerak, user natijaga chiqa olishi uchun darslik haqiqiy dars bo'lishi kerak" dedi. O'lchov: darslar o'rtacha **1618 belgi** edi (3 ta qisqa bo'lim) — ~2 daqiqalik o'qish, ya'ni dars emas, eslatma.
  - **⚠️ ASOSIY UX MUAMMOSI — SOXTA BLOKLAR.** Dars sahifasida "maslahat" va "asosiy fikrlar" bloklari bor edi, lekin ular matnni JUMLALARGA BO'LIB yasalardi (`splitInsights`). Matn qisqa bo'lgani uchun "asosiy fikrlar" matnning O'ZINI takrorlardi — o'quvchiga hech narsa qo'shmasdi, faqat sahifani to'ldirardi.
  - **Yangi pedagogik tuzilma.** `LessonSectionSchema` ga `example` (kundalik hayotdan misol), `keyPoints[]` (bo'lim xulosalari), `warning` (ko'p uchraydigan xato) qo'shildi; `LessonSchema` ga `objectives[]` ("Ushbu darsdan keyin siz:") va `practicalSteps[]` ("Amaliy qadamlar" — bilimni natijaga aylantiradigan qism). Barchasi IXTIYORIY: bo'sh bo'lsa sahifa ularni ko'rsatmaydi, eski darslar va PDF importi buzilmaydi.
  - **Kontent qayta yozildi** (`scripts/lessonContentData.js`, `scripts/upgradeLessons.js`): Mehnat huquqi **909 → 7126 belgi (7,8×)**, Oila huquqi **712 → 5360 (7,5×)**, Iste'molchi huquqlari **714 → 4866 (6,8×)**. Har bo'limda: sodda tushuntirish + hayotdan misol + 4 ta asosiy fikr + ogohlantirish.
  - **⚠️ HUQUQIY ANIQLIK QOIDASI saqlandi:** matnda ANIQ MODDA RAQAMI, MUDDAT yoki SUMMA ataylab yozilmagan — qonun tahriri o'zgarsa dars eskirib, foydalanuvchini chalg'itardi. O'rniga barqaror TAMOYILLAR va JARAYON tushuntiriladi; aniq raqam kerak bo'lganda amaliy qadamlarda Qonunlar kutubxonasi va AI yordamchiga yo'naltiriladi.
  - **Interfeys:** haqiqiy misol bo'lsa blok "Hayotdan misol" deb nomlanadi va ko'k rangda ajratiladi (oddiy "Maslahat"dan farqlash uchun); har bo'lim oxirida o'ziga xos ogohlantirish; dars oxirida raqamlangan "Amaliy qadamlar" kartasi. Yangi kalitlar: `lesson_example`, `lesson_goals`, `lesson_steps`, `lesson_steps_sub` (uch tilda).
  - **Tarjimaga ulandi:** `contentI18n.js` yangi maydonlarni (`objectives`, `practicalSteps`, `example`, `keyPoints`, `warning`) ham tarjima qiladi.
  - **Qolgan darslar** (Meros, Yer va ko'chmas mulk, Jinoiy javobgarlik) hali eski holatda — ular uchun kontent `lessonContentData.js` ga xuddi shu tuzilmada qo'shilishi kerak.
  - Tekshirildi: 113 ta CSS sinfi, 60 ta tarjima kaliti 3 tilda, backend va frontend build toza.

- **TIL ALMASHTIRISH TEZLIGI — 3 ta xato tuzatildi (2026-08-06):** foydalanuvchi "til o'zgarsa hamma joy uzog'i bilan 5 soniyada o'zgarsin" dedi. O'lchov ildiz sababni ko'rsatdi: **tarjima keshi atigi 8,7% to'lgan edi** (14 206 ta matndan 1230 tasi) — ya'ni til almashtirilganda kontentning aksariyati AI'da YANGIDAN tarjima qilinardi va bu daqiqalar olardi.
  1. **Qayta so'rash 15 soniyada BIR MARTA edi.** Fon tarjimasi shu vaqtda ulgurmasa, matn tilni qayta almashtirmaguncha manba tilida qolib ketardi. Endi **har 2 soniyada** tekshiriladi va tayyor bo'lishi bilan yangilanadi (`MAX_POLLS = 20`, ya'ni 40 soniya, keyin to'xtaydi — cheksiz so'rovdan himoya). Konstitutsiya va kutubxonada.
  2. **Kesh oldindan to'ldirilmasdi.** Yangi `services/translationWarmup.js` — fon rejimida, foydalanuvchi so'ramasdan OLDIN kontentni tarjima qiladi. Navbat tartibi foydalanuvchi ko'radigan tartibda: Konstitutsiya → darslar/testlar → modda SARLAVHALARI → modda MATNI. **Ataylab sekin-asta** (to'lqinda 60 ta matn, 3 daqiqa tanaffus): bepul AI kvotasi cheklangan va uni bir zarbada tugatib qo'ymaslik kerak. AI ishlamayotgani aniqlansa (uzilish himoyasi) ish TO'XTAYDI.
  3. **"Tarjima tayyorlanmoqda…" ABADIY turardi.** AI kvotasi tugagan bo'lsa tarjima hech qachon kelmasdi, lekin indikator va'da qilib turardi va frontend behuda so'rov yuborardi. Endi javobga **`translationAvailable`** maydoni qo'shildi — `false` bo'lsa indikator ko'rsatilmaydi va qayta so'rov yuborilmaydi.
  - **O'lchangan natija (barcha tillar):** Konstitutsiya 297-502 ms · kutubxona 2-102 ms · darslar 100-2076 ms · testlar 99-301 ms · sayt matni 97-188 ms. **Hammasi 5 soniyadan ancha tez.**
  - **Qamrov tekshirildi:** interfeys (bir zumda), sayt matni, darslar, testlar, kodekslar, modda matni, Konstitutsiya, AI topshiriqlari, AI chat — 10 ta `useEffect` til o'zgarishiga bog'langan, tilga bog'lanmagan kontent so'rovi qolmagan.
  - **⚠️ TASHQI CHEKLOV:** sinov paytida barcha bepul AI provayderlarining KUNLIK kvotasi tugagan edi (Cloudflare 10 000 neuron, Groq 100 000 token, Gemini 20 so'rov). Shu sabab keshda yo'q matn manba tilida qoladi — bu kod xatosi emas. Kvota yangilangach `translationWarmup` keshni o'zi to'ldiradi va til almashtirish butunlay bir zumda bo'ladi.

- **Konstitutsiya — UX qayta ishlandi: MUNDARIJA + MATN (2026-08-06):** foydalanuvchi "UX kuchli bo'lsin, user bir ko'rishda tushunishi kerak" dedi. Bosqichma-bosqich o'tish (bo'lim → bob → modda) O'RNIGA hujjat + mundarija sxemasi qo'yildi.
  - **Nima uchun almashtirildi:** bosqichma-bosqich variantda moddani ko'rish uchun IKKI marta bosish kerak edi va har bosqichda oldingi ro'yxat YO'QOLARDI — foydalanuvchi hujjatning umumiy tuzilishini ko'ra olmasdi. Endi chapda butun tuzilma DOIM ko'rinadi (6 bo'lim, ichida boblar), o'ngda tanlangan bobning moddalari. Bu qomus va hujjatlar uchun tanish, o'rganishni talab qilmaydigan naqsh.
  - **⚠️ ASOSIY UX MUAMMOSI — YORLIQLAR EDI.** lex.uz dan kelgan nomlar butunlay BOSH HARFDA, uzun prefiksli va rim raqamli: `"IKKINCHI BOʻLIM. INSON VA FUQARONING ASOSIY HUQUQLARI, ERKINLIKLARI VA BURCHLARI"` (80 belgi), `"XVIII bob. Oʻzbekiston Respublikasi Oliy Majlisi"`. Yangi `pages/constitutionLabels.js` ularni raqam + o'qishli sarlavhaga ajratadi: **[2] Inson va fuqaroning asosiy huquqlari…** va **18 · Oʻzbekiston Respublikasi Oliy Majlisi**. Bo'lim raqamlari SO'Z bilan yozilgani (`BIRINCHI`→1) va boblar RIM raqamida (`XVIII`→18) — ikkalasi ham raqamga aylantiriladi. Manba matn o'zgarmaydi — bu faqat ko'rsatish uchun, qidiruv/tarjima/baza to'liq nom bilan ishlaydi.
  - **Boshqa UX qarorlari:** sahifa ochilishi bilan BIRINCHI bob tanlangan (bo'sh ekran yo'q, foydalanuvchi darrov matnni ko'radi) · joriy bob mundarijada oltin fon bilan ajratilgan · matn tepasida "2-bo'lim · Inson huquqlari" izi va modda oralig'i · pastda **oldingi/keyingi bob** tugmalari (hujjatni boshdan-oxir o'qish uchun) · qidiruv natijalari tuzilmadan tashqarida, har biri qaysi bobda ekani bilan.
  - **HERO AVVALGI KO'RINISHDA QOLDI (foydalanuvchi so'rovi, 2026-08-06):** dastlab hero gorizontal (ikona chapda, qidiruv o'ngda) qilingan edi, lekin foydalanuvchi "tepadagi header'ning pastidagi joy avvalgi dizaynda tursin" dedi. Qaytarildi: markazlashtirilgan, 62px ikona, katta sarlavha (`clamp(1.5rem, 3.4vw, 2.2rem)`), tavsif matni (`const_sub`) va markazdagi meta qatori. Qidiruv ham hero ICHIDAN chiqarilib, avvalgi joyiga — hero ostiga, oq dumaloq panel sifatida qaytarildi. **Pastdagi qism (mundarija, bo'lim/bob ro'yxati, moddalar) TEGILMADI.**
  - **Telefonda** mundarija yuqoridagi ochiladigan panelga aylanadi va YOPIQ holatda ham joriy joylashuvni yozib turadi ("2-bo'lim · 7-bob") — foydalanuvchi qayerdaligini bilib turadi.
  - **Tuzatilgan nuqson:** OLTINCHI BO'LIM ning o'z bobi yo'q (moddalar to'g'ridan-to'g'ri bo'limga tegishli) — mundarijada bo'sh qator ko'rinardi. Endi bunday holatda bo'lim nomi ishlatiladi va raqam o'rniga "•" qo'yiladi.
  - Yangi kalitlar: `const_section`, `const_chapter`, `const_prev`, `const_next`, `const_back_to_doc` (uch tilda). Tekshirildi: CSS sinflari 50/50 mos, tarjima kalitlari 15/15 uch tilda, build toza.

- **Konstitutsiya — UCH BOSQICHLI tuzilma (2026-08-06):** foydalanuvchi "bo'limlar bilan ajratib chiq, uzun scroll bo'lmasin, alohida-alohida bo'lsin" dedi. Endi: **BO'LIM (6) → BOB (27) → MODDA (155)**. Bir ekranda ko'rsatiladigan eng ko'p modda **155 dan 14 taga** tushdi.
  - **Bo'limlar ajratib olindi.** Ular lex.uz'da SON bilan emas, SO'Z bilan va bosh harfda yozilgan ("BIRINCHI BOʻLIM. ASOSIY PRINSIPLAR") — shuning uchun `SECTION_RE` alohida naqsh. **Katta harf sharti muhim:** modda matni ichida "V boʻlimi", "toʻrtinchi boʻlimi" kabi HAVOLALAR uchraydi va ular sarlavha deb qabul qilinmasligi kerak edi. `LegalChunk` ga `section` maydoni qo'shildi. Natija: **155/155 modda bo'limga, 153/155 bobga biriktirildi** (qolgan 2 tasi — OLTINCHI BO'LIM, uning o'z bobi yo'q, bu to'g'ri).
  - **⚠️ Tuzatilgan xato:** yangi bo'lim boshlanganda BOB tozalanmasa, oldingi bo'limning oxirgi bobi keyingi bo'lim moddalariga "yopishib" qolardi — haqiqiy holat: 154-155-moddalar "XXVII bob. Mudofaa va xavfsizlik" ga tegishli deb ko'rsatilardi. Endi `chapter = ""` qilinadi.
  - **API ierarxik:** `/constitution` endi `sections[] → chapters[] → articles[]` qaytaradi, har darajada modda soni bilan. Ma'lumot BIR MARTA olinadi (~104 KB) — bosqichlar orasida o'tish qo'shimcha so'rovsiz, bir zumda.
  - **Interfeys:** 1-bosqich 6 ta bo'lim kartasi (raqam + bob/modda soni) → 2-bosqich tanlangan bo'limning boblari (modda oralig'i bilan) → 3-bosqich faqat o'sha bobning moddalari. Orqaga tugmasi va breadcrumb. **Qidiruv bosqichlardan TASHQARIDA** — natijalar yassi ro'yxatda, har biri qaysi bobda ekani ko'rsatilgan holda (foydalanuvchini modda raqamini bo'lim/bob orasidan izlashga majburlash noto'g'ri bo'lardi).
  - **Olib tashlandi:** yopishqoq mundarija (`.toc*`), "Yuqoriga" tugmasi va ular bilan bog'liq barcha CSS — endi ular keraksiz, chunki uzun scroll yo'q. Yangi kalitlar: `const_sections`, `const_sections_count` (uch tilda).
  - **Sinov:** uchala bosqich (6 → 7 → 11 modda), qidiruv buzilmagan (`95` → 1 ta, `95-modda` → 1 ta, `mehnat` → 9 ta), ruscha ishlaydi, CSS sinflari 30/30 mos, build toza.

- **INGLIZ TILI BUTUNLAY OLIB TASHLANDI (2026-08-07):** foydalanuvchi so'rovi — "butun saytdan barcha joyidan ingliz tilini olib tashla, rus va uz qolaversin". Saytda endi FAQAT ikki til: o'zbek va rus.
  - **Frontend:** `LangSwitcher.jsx` dan 🇬🇧 bandi; `i18n/translations.js` dan butun `en:` bloki (**690 qator**) — qolgan uz/ru kalitlari teng (439 tadan); `index.html` schema.org `inLanguage`; `ChatDrawer.jsx` (sana lokali), `VoiceChat.jsx` (2 ta matn), `AuthPages.jsx` (`safeLang`) dagi o'lik shoxlar.
  - **⚠️ ENG MUHIM NUQTA — `localStorage`da qolgan "en".** Ingliz tilini tanlagan foydalanuvchining brauzerida `lang: "en"` saqlanib qolgan bo'ladi. Tekshirilmasa ikki muammo: (1) `translations["en"]` endi yo'q — interfeys standart tilga tushardi, lekin `lang` "en" bo'lib qolardi; (2) shu "en" HAR BIR API so'roviga `?lang=en` bo'lib ketardi va server uni tanimay kontentni tarjimasiz qaytarardi. Yangi `readStoredLang()` (`LangContext.jsx`) noma'lum tilni standartga almashtiradi va `localStorage` ni ham darhol tuzatadi; `changeLang()` qo'llab-quvvatlanmaydigan tilni umuman qabul qilmaydi.
  - **Backend:** `translator.js` `SUPPORTED = ["uz","ru"]`; `translationWarmup.js` `TARGET_LANGS = ["ru"]`; `langOf()` validatorlari 5 ta faylda (`library`, `lessons`, `tests`, `lessonPractice`, `site`); `adminContent.js` oldindan tarjima ro'yxati; `Translation` modeli `enum`; `telegram-bot.js` STT tili.
  - **AI javob tili (`legalAI.js`):** `detectLanguage()` endi faqat `uz`/`ru` qaytaradi (kirill > 3 belgi → ru, aks holda uz). Inglizcha yozilgan savol TUSHUNILADI, lekin javob o'zbek tilida keladi. Barcha inglizcha prompt/format/xabar matnlari o'chirildi (**52 qator, 13 ta obyektdan**). Diff bilan tasdiqlangan: faqat o'chirish, 0 ta qo'shish — salomlashish/minnatdorchilik/mavzu filtri mantiqiga TEGILMAGAN. Sinov: uz→uz, ru→ru, ingliz savol→uz, "Sen kimsan?" ikkala tilda to'g'ri.
  - **Kodeks nomlari:** `nameEn` maydoni modeldan, `adminLaws.js` marshrutidan, `lawRegistry.js` dan, admin UI dan va `config/laws.js` dagi 21 ta inglizcha nomdan olib tashlandi; bazadagi 21 yozuvdan ham `$unset` qilindi.
  - **Baza tozalandi:** `Translation` kolleksiyasidan **811 ta inglizcha tarjima** o'chirildi (endi faqat `ru` qolgan).
  - **Yo'l-yo'lakay qo'shildi:** admin panelidagi kodeks oynasiga **"Ruscha manba (lex.uz)"** maydoni (`urlRu`) — backend uni allaqachon qo'llab-quvvatlardi, lekin UI da yo'q edi, ya'ni admin yangi kodeks qo'shsa rasmiy rus matnini ko'rsata olmasdi. Ruscha nom maydoni ham yolg'iz qolgan ikki ustunli griddan bitta ustunga o'tkazildi.
  - **Foyda:** bepul AI kvotasi endi butunlay boshqa ehtiyojlarga qoladi — ilgari uning katta qismi ingliz tarjimasiga ketardi va u baribir 11,4% da qolib ketgan edi.
  - **Tekshiruv 13/13:** til almashtirgich, tarjima kalitlari tengligi, backend validatorlari, `legalAI` tozaligi, `nameEn` qolmagani, o'lik shoxlar, ikkala build.

- **TIL MUAMMOSI HAL QILINDI — qonunlar endi RASMIY rus tarjimasidan keladi (2026-08-07):** foydalanuvchi "tilni almashtirsam modda, qonunlar tili almashmayapti" dedi. Sabab AI kvotasi edi va u KOD BILAN hal bo'lmasdi: o'lchandi — Cloudflare 10 000 neuron TUGAGAN, Groq 99 583/100 000 token TUGAGAN, Gemini kunlik kvota TUGAGAN. Shu tezlikda 28 412 ta matn **~14 kun** olardi; 20 ta kodeksdan faqat 3 tasi (MK, BK, UJK) tarjima qilingan edi, qolgani 0-41%.
  - **YECHIM — AI umuman kerak emas.** lex.uz har bir kodeksni **rasmiy rus tilida** ham chop etadi (ALOHIDA hujjat ID bilan, masalan Mehnat kodeksi: o'zbekcha `-6257288`, ruscha `6257291` — minussiz va boshqa raqam). Bu uch jihatdan ustun: **aniq** (rasmiy huquqiy tarjima, AI taxmini emas — huquqiy mahsulot uchun hal qiluvchi), **tekin** (kvota sarflanmaydi), **tez** (bir necha daqiqa).
  - **21 ta rus manzili topildi va TEKSHIRILDI.** Qo'shni ID ni taxmin qilish ishlamadi (404) — har biri sarlavhasi bo'yicha tasdiqlandi. ⚠️ Faqat "moddalar bor" degan tekshiruv YETARLI EMAS: JK uchun dastlab butunlay boshqa hujjat ("О собственности") topilgan edi, shuning uchun sarlavha kutilgan nom bilan solishtiriladi. Qiyinlari qidiruv orqali topildi (YK ru `149947` — o'zbekchasi `-152653` dan uzoq, FK 1-qism `111181` va FK2 `180550` sanalari bo'yicha ajratildi: 1995 va 1996).
  - **Yangi fayl `scripts/ingestLawsRu.js`** — rus sahifasini o'sha `.CLAUSE_DEFAULT`/`.ACT_TEXT` tuzilmasi bilan tahlil qiladi ("Статья N." naqshi), moddalarni `{lawCode, articleNumber}` bo'yicha MAVJUD yozuvlarga biriktiradi (`bulkWrite`, yangi hujjat yaratmaydi). `LegalChunk` ga `titleRu`/`textRu`/`sectionRu`/`chapterRu` qo'shildi.
  - **⚠️ NIMAGA TEGILMADI:** `ingestLaws.js` (o'zbekcha yuklash) o'zgarmagan; `embedding` QAYTA HISOBLANMAYDI — RAG o'zbekcha matn bo'yicha ishlashda davom etadi; ingliz tili avvalgidek AI tarjimasi bilan.
  - **⚠️ KIRILL HOMOGLIFLARI — nozik xato, ikki joyda tuzatildi.** lex.uz rus matnida rim raqamlari BITTA hujjat ichida ham aralash yoziladi: "Глава VIII" (lotin), "Глава IХ" va "Глава ХII" (**kirill Х, U+0425**), "Глава XVIII" (lotin X, U+0058). Ekranda bir xil ko'rinadi, lekin `[IVXLC]` faqat lotinni taniydi — natijada IX-bobdan keyingi BARCHA boblar aniqlanmay, Konstitutsiyaning 3-bo'limidan boshlab boblar o'zbekcha qolib ketgan edi. `ingestLawsRu.js` va `frontend/src/pages/constitutionLabels.js` da normallashtirish qo'shildi. **Muhim tafsilot:** normallashtirish FAQAT raqamga qo'llanadi, butun satrga emas — aks holda sarlavha ham buziladi ("Олий Мажлис" → "Олий M-ажлис"; bu sinovda aniqlandi).
  - **Frontend `constitutionLabels.js`** endi uch tilni biladi: `BOʻLIM` (raqam oldin), `РАЗДЕЛ`/`SECTION` (raqam keyin), `bob`/`ГЛАВА`/`CHAPTER`. Aks holda ruscha nom naqshga tushmay, "Раздел второй. основные принципы" kabi prefiksli va noto'g'ri kichik harfli sarlavha chiqardi. 22 ta test.
  - **Marshrutlar:** `library.js` rus tilida `titleRu`/`textRu` ni ishlatadi (AI umuman chaqirilmaydi), rasmiy tarjimasi yo'q noyob modda uchun AI keshiga qaytadi. Qidiruv ham rus matnini qamraydi (avval sahifa ruscha ko'rinib turib, ruscha so'z bilan qidiruv 0 natija berardi). **Tezlik saqlandi:** qoplovchi indeksga `titleRu` kiritildi — o'lchandi, 581 modda **3 ms**, `totalDocsExamined: 0`.
  - **`translationWarmup.js`:** rus tili uchun qonun moddalari endi fon navbatiga UMUMAN kirmaydi (rasmiy matn bor), butun kvota ingliz tiliga qoladi. To'lqin hajmi 60/3daq → 200/1daq.
  - **`server.js`:** rus matni ishga tushishda va har 24 soatda o'zbekchadan KEYIN yangilanadi. Admin panelidan yangi kodeks qo'shishda `urlRu` kiritish mumkin (`adminLaws.js`), bo'sh qoldirilsa AI tarjimasiga qaytadi.
  - **Natija (o'lchangan):** rus tili — **6947/6948 modda (99,99%)**, yagona istisno FPK 419-25 (u lex.uz rus manbasida umuman yo'q). Konstitutsiya tuzilmasi uz=ru (6 bo'lim, 28 bob). Yakuniy regressiya **9/9**. ⚠️ **Ingliz tili — 11,4%** va AI kvotasiga bog'liqligicha qolmoqda (rasmiy ingliz manbasi yo'q); kvota to'liq ingliz tiliga yo'naltirilgani uchun kuniga ~2 000 matn tarjima qilinadi.

- **Kirish sahifasi tepa/pastga yopishib qolardi — tuzatildi (2026-08-07):** foydalanuvchi "tepa va past yopishib qolyapti, joy qolishi kerak" dedi.
  - **Ikkita sabab bor edi.** (1) `≤620px` balandlikda `.formSide` ga `align-items: flex-start` qo'yilgan edi va u kartani TEPAGA yopishtirardi (atigi 12px bo'shliq). (2) Boshqa holatlarda `align-items: center` ishlatilardi — bu flex'ning ma'lum nuqsoni: kontent konteynerdan balandroq bo'lsa u YUQORIGA ham chiqib ketadi va scroll bilan unga yetib bo'lmaydi, ya'ni **karta tepasi qirqiladi**.
  - **⚠️ Nima uchun oddiy hisobda muammo ko'rinmagan:** dastlab o'lchaganda joy yetarli chiqdi. Sabab — hisob ekran balandligini olgan, holbuki telefonda brauzer paneli (manzil qatori) 110-130px egallaydi. Haqiqiy `100dvh` bilan iPhone SE da 667 emas, **557px** qoladi va karta (565px) sig'may qoladi.
  - **To'g'ri yechim — `margin: auto` naqshi:** `.formSide { align-items: flex-start }` + `.card { margin: auto }`. Joy bo'lsa karta markazda turadi, joy yetmasa konteynerning ichki bo'shlig'i SAQLANIB qoladi va scroll to'g'ri ishlaydi. `align-items: center` dagi qirqilish muammosi umuman yuzaga kelmaydi.
  - Bo'shliqlar oshirildi: asosiy holat 20 → **28px**, `≤720px` balandlikda **20px**, `≤620px` da 12 → **16px**.
  - **Tekshirildi (brauzer paneli hisobga olingan holda):** iPhone SE — ichki scroll, chetlarda 16px saqlanadi; iPhone 14 — 37px; iPhone 11 — 55px; 14 Pro Max — 73px; gorizontal holat — 16px saqlanadi. Hech qanday holatda yopishmaydi.

- **Kirish sahifasida matn chetga chiqib ketardi — tuzatildi (2026-08-06):** foydalanuvchi mobil versiyada matn chetdan chiqayotganini bildirdi. Sabab — klassik flex/grid xatosi, o'zim kiritgan ikki ustunli forma tufayli yuzaga kelgan.
  - **⚠️ ILDIZ SABAB:** flex va grid elementlarining standart `min-width: auto` qiymati ularni KONTENT enidan pastga tushishga qo'ymaydi. `<input>` ning tabiiy eni esa brauzer standarti bo'yicha ~20 belgi (**≈189px**) — ya'ni tor ustunda u siqilmay, ustunni kengaytirib yuborardi. O'lchandi: **414px ekranda 32px, 430px da 24px, 480px+ da 13px chetga chiqardi** (iPhone 11, iPhone 14 Pro Max — keng tarqalgan qurilmalar).
  - **Tuzatish ikki qatlamli:** (1) `min-width: 0` — `.label`, `.input`, `.card`, `.googleBtn` va `.formGrid > *` ga qo'shildi (asosiy, ishonchli yechim); (2) bitta ustunga o'tish chegarasi **400px → 560px** ga ko'tarildi. Sabab: karta eng ko'pi 420px, ichki bo'shliq 56px — ikki ustunda maydon eni eng yaxshi holatda ham 175px bo'ladi, bu "To'liq ism" kabi yorliqlar uchun juda tor. Endi barcha telefonlarda bitta ustun.
  - **Balandlik qayta hisoblandi:** 1 ustunda ro'yxatdan o'tish 696px. iPhone SE (667px) da `@media (max-height: 720px)` ixchamlashuvi bilan **11px zaxira**, boshqa telefonlarda 148-236px zaxira — scroll paydo bo'lmaydi.
  - **Tor ekran (≤400px) uchun qo'shimcha:** karta ichki bo'shlig'i kamaytirildi, Google tugmasi shrifti 14px, tasdiqlash kodi maydoni 28→23px va harf oralig'i 8→5px (katta shrift + katta oraliq maydonni kengaytirib yuborardi).
  - **Tekshirildi:** 320-768px oralig'ida 9 ta kenglikda hisoblandi — hammasida maydon eni 175-364px (qulay), chetga chiqish yo'q. Modal oyna ham xavfsiz (`global.css` da `box-sizing: border-box` bor).

- **Kirish/ro'yxatdan o'tish sahifasi qayta dizayn qilindi — SCROLLSIZ (2026-08-06):** foydalanuvchi "sahifada scroll bo'lmasin" dedi. O'lchandi: ro'yxatdan o'tish formasi **776px** joy olardi — iPhone SE (667px) da 109px, past noutbuk oynasida ham chiqib ketardi.
  - **Asosiy yechim — 4 ta maydon IKKI USTUNDA** (`.formGrid`): ism/username va email/parol yonma-yon. Bu yolg'iz ~120px tejaydi. ≤400px da avtomatik bitta ustunga tushadi.
  - **Ixchamlashtirish:** karta padding 2,5→1,85rem, sarlavha 28→22px, forma gap 1,25→0,85rem, maydon padding 11→10px, Google/ajratgich/pastki matnlar bo'shliqlari qisqartirildi. Natija: **kirish 576px, ro'yxatdan o'tish 552px**.
  - **Past ekranlar uchun ikki bosqich:** `@media (max-height: 720px)` — yana bir pog'ona kichrayadi; `@media (max-height: 620px)` — tavsif matni yashiriladi va o'lchamlar minimalga tushadi (**−98px**), shunda 568px balandlikdagi ekranda ham sig'adi.
  - **⚠️ Ikkita nozik xato tuzatildi:** (1) `.page` da `min-height: 100vh` bor edi — telefonda `100vh` manzil paneli YASHIRINGAN holatdagi kattaroq balandlikni bildiradi, ya'ni u `100dvh` dan katta bo'lib elementni ko'rinadigan maydondan uzunroq qilardi va aynan scroll hosil qilardi; (2) fayl oxirida `overflow-y: auto` turgan edi va u yangi `overflow: hidden` ni bekor qilardi. Endi: `height: 100vh` → `height: 100dvh` (eski brauzer birinchisini, zamonaviysi ikkinchisini oladi) + `overflow: hidden`.
  - **Ehtiyot chorasi:** `.formSide` da ichki `overflow-y: auto` qoldirildi — kutilmagan holatda (juda past ekran, katta shrift sozlamasi) kontent yetib bo'lmas holga tushmaydi. Oddiy holatda u ishga tushmaydi.
  - **Dizayn yangilandi:** karta burchagi 18px, ikki qatlamli yumshoq soya, maydon burchagi 10px, placeholder rangi, tugmada bosilish animatsiyasi (`scale(0.985)`).
  - Tekshirildi: 37 ta sinf mavjud, build toza, o'zgarishlar to'rtala ekranga ham (kirish · ro'yxatdan o'tish · OTP · Telegram) tegishli — hammasi bir xil `.page`/`.card` sinflaridan foydalanadi.

- **Loyiha tozalandi — 2-audit (2026-08-06):** butun loyiha import grafi, CSS sinflari, tarjima kalitlari va npm bog'liqliklari bo'yicha skanerlandi.
  - **O'chirildi:** `backend/_ingest_all.js` va `backend/_t_ing.js` (mening vaqtinchalik test skriptlarim, qolib ketgan) · `backend/SMTP_SETUP.md` (48 qator — faqat Brevo haqida edi, holbuki endi 6 ta provayder bor va `backend/.env` da to'liq jadval (bepul limit, domen kerakmi, havolalar) yozilgan; eskirgan hujjat chalg'ituvchi edi — "Brevo sozlanmasa hech kim ro'yxatdan o'ta olmaydi" degan da'vo endi noto'g'ri) · **48 ta o'lik CSS qoidasi** (−6 KB) · **72 ta ishlatilmaydigan tarjima kaliti × 3 til = 216 yozuv** (`translations.js`: 2216 → 2034 qator, −12,8 KB; 498 → 426 kalit).
  - **⚠️ TUZATILDI — `logo-96.png` 383 KB edi.** Nomiga qaramay u 600×600 to'liq o'lchamli rasm ekan va HAR SAHIFADA favicon hamda 30-60px logo sifatida yuklanardi (aynan avvalgi tezlik optimizatsiyasi bartaraf qilgan muammo qaytgan). 96×96 ga kichraytirildi: **383 KB → 15 KB (−96%)**.
  - **⚠️ TUZATILDI — `logo.png` fayli YO'Q edi, lekin 5 joyda ishlatilardi:** `og:image`, `twitter:image`, `apple-touch-icon`, schema.org JSON-LD va `Seo.jsx` dagi `DEFAULT_IMAGE`. Ya'ni ijtimoiy tarmoqda ulashilganda rasm ko'rinmasdi. To'liq o'lchamli nusxa `logo.png` sifatida tiklandi.
  - **Soxta signallar — TEKSHIRILDI va SAQLANDI:** `admin` dagi `color_*`/`badge_*`/`btn_*` (22 sinf) va `Lessons` dagi `badge_basic|mid|deep` — bular `s[\`badge_${x}\`]` shaklida DINAMIK ishlatiladi, oddiy qidiruv ularni "o'lik" deb ko'rsatadi. `ChatDrawer.jsx` ham "ishlatilmaydi" bo'lib ko'rindi — u `lazy(() => import(...))` bilan yuklanadi. `frontend/src/data/lessons.js` + `tests.js` — frontend o'qimaydi, lekin `backend/scripts/seedContent.js` ularni seed manba sifatida ishlatadi. `backend/uploads/` — bo'sh va gitignore'da, lekin `routes/chat.js` uni o'zi yaratadi.
  - **⚠️ O'ZIM YO'L QO'YGAN XATO — topildi va tuzatildi:** birinchi yozgan CSS tozalash skriptim juda qo'pol regex ishlatib, `admin/Dashboard.module.css` dan **ishlatiladigan `.twoCol` sinfini** o'chirib yubordi. Sinf tekshiruvi buni tutdi (`Dashboard.jsx → twoCol`), qoida moslashuv bilan tiklandi. Keyin skript to'g'ri parserga (qavs sanash + selektor guruhini to'liq tekshirish) almashtirildi.
  - **Tekshiruv:** import grafi — frontend 0, admin 0, backend 0 ta ishlatilmaydigan fayl · backend 51 fayl sintaktik toza · CSS sinf murojaatlari 580 + 254 — hammasi mavjud · tarjima kalitlari uz/ru/en 426 tadan (teng) · npm bog'liqliklari (35 ta) — hammasi ishlatiladi · ikkala build toza.

- **Konstitutsiya sahifasi — mobil ko'rinish qayta qurildi (2026-08-06):** foydalanuvchi "telefonda umuman yomon, textlar chetga chiqib ketgan" dedi va aniq talablar berdi.
  - **Yopishqoq boshqaruv:** qidiruv VA bob tanlagich navbar ostida QOTIB turadi, moddalar ularning ostida scroll bo'laveradi. Tepadan **10px**, ikkovining orasida ham **10px** (ikkalasi ham so'ralgan). Qiymatlar ANIQ hisoblangan:
    - ≤1024px (navbar 60): qidiruv `top: 70` → pastki cheti 118 → tanlagich `top: 128`
    - ≤420px (navbar 56): qidiruv `top: 66` → pastki cheti 114 → tanlagich `top: 124`
    - Qidiruv balandligi ATAYLAB qat'iy (`height: 48px`) — `top` qiymatlari shundan hisoblanadi; ichki bo'shliq shunga moslandi (46,7px kontent 48px ichida, 1,3px zaxira).
    - **⚠️ Oradagi 10px — TIRQISH EMAS.** Dastlab ikkovi orasida haqiqiy 10px bo'shliq qoldirilgan edi va o'sha joydan sahifa matni scroll bo'lib ko'rinib turardi. Uni qidiruvning `::after` psevdo-elementi (oq chiziq) bilan yopishga urinildi — lekin chiziq matnni ham qoplab qo'ydi (foydalanuvchi: "orasida nimadir bor, textlar ko'rinmayapti"). **Yakuniy yechim:** tanlagich qidiruvga TEGIB yopishadi (`top` = qidiruvning pastki cheti, ya'ni 118/114px), 10px esa uning O'Z `padding-top` i va foni ichida — ko'rinishda bo'shliq bor, lekin ochiq joy yo'q. Qidiruvning `margin-bottom` i 0 ga tushirildi (aks holda yopishmagan holatda bo'shliq ikki barobar ko'rinardi).
    - **Yo'l-yo'lakay tuzatildi:** yopishqoq qiymatlar avval ≤768px blokida edi, lekin ular navbar 56px ga hisoblangan — natijada 421-768px oralig'ida (u yerda navbar hali 60px) qidiruv navbar ostida 4px bo'shliq qoldirardi. Endi ular alohida ≤420px blokida.
  - **Mundarija endi kontentni surmaydi:** ochilganda `position: absolute` bilan kontent USTIGA tushadi (avval blok bo'lib turardi va 27 ta bobni har safar aylanib o'tishga to'g'ri kelardi). Yopiq holatda `pointer-events: none` — bosilib qolmaydi.
  - **Chetga chiqish sabablari:** `.layout` grid o'rniga `display: block` (yopishqoq element uchun soddaroq va ishonchli), `.content`/`.toc` ga `min-width: 0`, matn elementlariga `max-width: 100%` umumiy chegara. Hero va modda o'lchamlari telefon uchun kichraytirildi (sarlavha 1,25rem, modda matni 13,5px, ichki bo'shliqlar).
  - **Bo'lim bo'shlig'i:** umumiy `shared.pageSection` (80px yuqori/pastki) o'rniga sahifaga xos `.section` — telefonda **1,5rem**. Uzun hujjatda 80px bo'shliq matnni bekorga pastga surardi. `site.module.css` ga TEGILMADI (u boshqa sahifalarda ham ishlatiladi).
  - **`scroll-margin-top: 168px`** — bobga o'tilganda sarlavha yopishqoq boshqaruv ostida qolmaydi.
- **Pastki panelda Bosh sahifa → Konstitutsiya (2026-08-06):** login qilgan foydalanuvchi uchun pastki panel endi **Konstitutsiya · Darslar · AI · Testlar · Kutubxona**. Bosh sahifaga yuqoridagi LOGO orqali o'tiladi, ya'ni yo'l yo'qolmadi. `/constitution` `BOTTOM_NAV_PATHS` ga qo'shilgani uchun burger menyudan **avtomatik** chiqib ketdi (alohida o'zgartirish kerak bo'lmadi — bu aynan shu ro'yxat mexanizmi to'g'ri qurilganini ko'rsatadi). Natijada burger menyuda endi faqat Profil va "Biz haqimizda" guruhi qoladi.

- **Til almashtirgich mobil header'ga chiqarildi (2026-08-06):** avval login qilgan foydalanuvchida u FAQAT burger menyu ichida edi (`.langDesktop` ≤768px da `display: none`, `.mobileLang` esa menyu tubida) — tilni o'zgartirish uchun menyuni ochib, pastgacha scroll qilish kerak edi. Endi u **doim yuqorigi panelda**, mehmon ham, login qilgan foydalanuvchi ham. Burger menyudan butunlay olib tashlandi (`.mobileLang` qoidasi va `langStyles.fullWidth` ishlatilishi o'chirildi).
  - `.langDesktop` → **`.langWrap`** deb qayta nomlandi (eski nom endi chalg'ituvchi edi — u telefonda ham ko'rinadi). `.langMobileVisible` (faqat mehmonlar uchun vaqtinchalik yechim edi) butunlay olib tashlandi — endi shart yo'q, hamma uchun bir xil.
  - **Ixchamlik `LangSwitcher.module.css` da yangi `.navCompact` sinfi orqali:** ≤768px — ichki bo'shliq va shrift kichrayadi, bayroq 24→21px, chevron 8→7px; **≤360px** — "UZ" matni yashiriladi, faqat bayroq qoladi (bayroqning o'zi tilni aniq ko'rsatadi). Sinf ATAYLAB alohida: `LangSwitcher` kirish/ro'yxatdan o'tish sahifalarida ham ishlatiladi, u yerda joy yetarli va kichraytirish kerak emas (`ChatDrawer` esa allaqachon `compact` prop bilan ishlatadi).
  - **Kenglik hisob-kitobi bilan tekshirildi** (eng yomon holat — "Bepul · 30d" tarif nishoni bilan): 430/390/375/360/320px ekranlarda login qilgan foydalanuvchi uchun 233-259px ishlatiladi, mavjud joy 296-382px — barchasida bemalol sig'adi. Mehmon uchun ham shunday. Til almashtirgich 320-360px da 48px, kattaroqda 70px joy egallaydi.
  - Ochiluvchi ro'yxat qirqilmasligi tasdiqlandi: `.nav`/`.navInner`/`.navRight` da `overflow: hidden` yo'q, `.wrap` — `position: relative`, dropdown esa `right: 0` (o'ng chetdan chapga ochiladi, ekrandan chiqmaydi).

- **Email provayderlari kengaytirildi — 6 ta variant (2026-08-06):** foydalanuvchi "emailga tasdiqlash kod bormadi, Brevo'dan tashqari boshqalarini ham qilib ko'raylik" dedi. Sabab avval aniqlangan: Brevo akkaunti faollashtirilmagan (403) va bu KOD bilan hal qilinmaydi. Endi `services/emailService.js` da **provayderlar REYESTRI** bor — yangi provayder qo'shish uchun ro'yxatga bitta yozuv qo'shish kifoya.
  - **Zanjir tartibi:** Mailjet → SMTP2GO → Resend → SendGrid → Brevo → SMTP. Brevo ataylab OXIRIDA (hozir ishlamayapti — har safar unga urinish ro'yxatdan o'tishni behuda sekinlashtirardi), SMTP eng oxirida (Render bepul tarifi 25/465/587 portlarini bloklaydi, ya'ni u yerda deyarli har doim muvaffaqiyatsiz; lokal sinov uchun qoldirilgan).
  - **Yangi qo'shilganlari:** `sendViaMailjet` (MAILJET_API_KEY + MAILJET_SECRET_KEY, 200 xat/kun, **domen SHART EMAS** — tavsiya etilgani), `sendViaSmtp2go` (SMTP2GO_API_KEY, 1000/oy, domen shart emas), `sendViaResend` (RESEND_API_KEY, 3000/oy, lekin domensiz FAQAT akkaunt egasiga yuboradi), `sendViaSmtp` (nodemailer — allaqachon bog'liqlikda bor edi; Gmail uchun "App password" kerak). Barchasi HTTPS API orqali (SMTP dan tashqari).
  - **`EMAIL_PROVIDER_ORDER`** — tartibni qo'lda belgilash (masalan `EMAIL_PROVIDER_ORDER=Resend,Brevo`). Ro'yxatda ko'rsatilmagan sozlangan provayderlar zaxira sifatida oxiriga qo'shiladi.
  - **Ikkala xat turi ham** (`sendOTPEmail` va `sendPasswordResetEmail`) yagona `sendEmail()` zanjiridan o'tadi — to'g'ridan-to'g'ri provayder chaqiruvi qolmagan.
  - **Tashxis yangilandi:** `checkEmailHealth()` endi `providers` maydonini qaytaradi (sozlangan provayderlar ro'yxati), server ishga tushish logida `Provayderlar: Brevo → SMTP2GO` ko'rinishida chiqadi. Brevo 403 ("faollashtirilmagan") holatida maslahat endi 4 ta muqobilni havolalari bilan ko'rsatadi. **DIQQAT:** Mailjet/SMTP2GO/Resend uchun alohida "sog'liq" endpointi ishlatilmaydi — kalit BORLIGI yetarli deb hisoblanadi (aks holda har ishga tushishda sinov xati yuborishga to'g'ri kelardi); haqiqiy xato ro'yxatdan o'tishda logda provayder nomi bilan chiqadi.
  - **`.env` hujjatlashtirildi** — barcha provayderlar izohli jadval bilan (bepul limit, domen kerakmi), hammasi kommentda; foydalanuvchi faqat kerakli qatorni ochadi.
  - **Sinov: 12/12** (har bir provayder to'g'ri API manziliga so'rov yuboradi, zanjir yiqilganda keyingisiga o'tadi, `EMAIL_PROVIDER_ORDER` tartibni o'zgartiradi, sozlanmagan holatda `configIssue` bilan aniq xato) + SMTP yo'li alohida tekshirildi. Sinov `fetch` ni almashtirib o'tkazildi — haqiqiy xat yuborilmadi.
  - **⚠️ HALI HAM FOYDALANUVCHI TOMONIDA:** kalit olinishi kerak. Hozir `.env` da faqat `BREVO_API_KEY` bor va u ishlamayapti (log: "Authorised IPs cheklovi"). Eng tez yo'l — Mailjet (https://app.mailjet.com/account/apikeys), domen shart emas, bitta jo'natuvchi manzilni tasdiqlash kifoya.

- **Footer qayta qurildi — minimalist premium (2026-08-05):** avvalgi footer bitta qatordan iborat edi (logo · copyright · ijtimoiy ikonalar), ostida huquqiy havolalar. Endi `SiteLayout.jsx` da to'liq tuzilma: **brend bloki** (logo + `footer_desc` shiori + ijtimoiy ikonalar) + **4 ta ustun** (Xizmatlar / Foydali ma'lumot / Kompaniya / Aloqa) → ajratgich → **pastki qator** (copyright · 3 ta huquqiy havola · SSL va "O'zbekistonda ishlab chiqilgan" nishonlari) → ogohlantirish matni.
  - **⚠️ RASM BIRIKTIRILMAGAN:** foydalanuvchi "reference image" ga havola qildi, lekin rasm suhbatga kelmagan — footer FAQAT yozma tavsif asosida qurildi (piksel-aniq moslik kafolatlanmaydi). Tavsifdagi barcha talablar bajarildi: to'q ko'k fon (`--navy: #0b1322`, so'ralgan #0B1220-#101A2E oralig'ida), oltin urg'u, 4 ustun, ijtimoiy ikonalar, copyright, 3 ta havola, ikkala nishon.
  - **Obunaga yozilish ("Yangiliklardan xabardor bo'ling") bloki loyihada UMUMAN BO'LMAGAN** — kod bo'yicha tekshirildi (`newsletter`/`subscribe`/`Yangilik` bo'yicha qidiruv, 0 natija). Ya'ni "olib tashlash" talabi bo'yicha hech narsa o'chirilmadi; muhimi — o'rniga HECH QANDAY yangi blok (karta, email input, CTA, statistika) qo'shilmadi va footer sokin qoldi. Bu CSS izohida ham qayd etilgan, kelajakda "bo'sh joyni to'ldirish" niyati paydo bo'lmasligi uchun.
  - **Yangi kalitlar** (uch tilda, 486 taga yetdi): `footer_col_info`, `footer_col_company`, `footer_ssl`, `footer_made_in`. Qolganlari qayta ishlatildi (`nav_services`, `nav_contact`, `footer_desc`, `faq_title` va h.k.).
  - **Yo'l-yo'lakay tuzatildi:** `FAQ.jsx` ga `id="faq"` qo'shildi — footer'dagi `/#faq` havolasi hech qayerga olib bormasdi (anchor mavjud emas edi). `SiteLayout.jsx` ga `useChatPanel` ulandi ("AI Yordamchi" bandi havola emas, chat oynasini ochadigan tugma — vizual jihatdan havoladan farq qilmaydi) hamda `phone`/`address` o'zgaruvchilari (avval faqat `email` bor edi). Eskirgan `.footerInner` CSS qoidasi (mobil media ichida) o'chirildi — JSX'da bunday sinf qolmagan.
  - **Moslashuv:** desktop `1.6fr + 4×1fr` grid → ≤1080px brend butun kenglikda, ustunlar 2×2 → ≤768px pastki qator ustun bo'lib joylashadi → ≤520px barcha ustunlar bir-birining ostida.
  - **Tekshirildi:** footer sinflari 15/15 CSS da mavjud, tarjima kalitlari 21/21 uchala tilda, havolalar 13/13 haqiqiy marshrut yoki mavjud anchor'ga boradi, build toza.

- **TIL FUNKSIYASI SAYT BO'YLAB — bazadagi kontent ham tarjima qilinadi (2026-08-05):** avval `i18n/translations.js` faqat INTERFEYS matnlarini (tugma, sarlavha) uch tilda berardi; bazadan keladigan kontent — admin qo'shgan darslar/testlar, lex.uz qonun moddalari, sayt matni — bitta tilda saqlangani uchun til "Ruscha"ga o'zgartirilsa interfeys ruscha bo'lardi-yu, dars matni o'zbekcha qolardi. Endi kontent AI orqali tarjima qilinadi va natija keshlanadi.
  - **Yangi fayllar:** `backend/services/translator.js` (umumiy: til aniqlash, batch tarjima, kesh, sifat tekshiruvi) va `backend/services/contentI18n.js` (loyihaning aniq tuzilmalari: dars bo'limlari, test savol-variantlari, modda matni). Yangi model — `Translation` (`models/index.js`): `{hash, lang, text}`, `{hash,lang}` unique indeks. Kesh ALOHIDA kolleksiyada, chunki u Lesson/Quiz/LegalChunk uchun BIR XIL ishlaydi va bir xil matn (takrorlanuvchi sarlavha) bir marta tarjima qilinadi. Kontent tahrirlansa hash o'zgaradi → eski tarjima o'z-o'zidan ishlatilmay qoladi.
  - **Ulangan joylar:** `routes/lessons.js`, `tests.js`, `library.js`, `site.js` (`?lang=` qabul qiladi), `lessonPractice.js` (AI topshiriqlari), `adminContent.js` (admin saqlaganda FON rejimida qolgan 2 tilga oldindan tarjima — `pretranslate()`, ataylab `await` qilinmaydi, admin javobni darhol oladi).
  - **Frontend:** `utils/api.js` interceptoriga til qo'shildi — HAR BIR so'rovga `?lang=` va `X-Lang` avtomatik ketadi (har bir chaqiruvda qo'lda yozilsa, bitta joyi unutilishi bilan o'sha bo'lim boshqa tilda qolardi). `Lessons.jsx` / `Tests.jsx` / `Library.jsx` / `Home.jsx` / `SiteLayout.jsx` `useEffect` bog'lanishlariga `lang` qo'shildi — til o'zgarganda kontent qayta so'raladi. **DIQQAT:** `Home.jsx` va `SiteLayout.jsx` `/site/content` ni `api` orqali emas, to'g'ridan-to'g'ri `fetch` bilan oladi — interceptor ularga TEGMAYDI, shuning uchun `lang` qo'lda qo'shilgan.
  - **Tezlik uchun ataylab qilingan tanlovlar:** (a) o'zbek tilida AI UMUMAN chaqirilmaydi (manba tili bir xil) — javob **1 ms**; (b) darslar RO'YXATIDA faqat sarlavha/tavsif tarjima qilinadi, bo'lim matnlari dars OCHILGANDA (`GET /lessons/:id`); (c) kutubxona ro'yxatida faqat modda SARLAVHALARI, to'liq matn modda ochilganda (yangi `GET /library/laws/:code/:num`); (d) testlarda esa savol-variantlar RO'YXATDA tarjima qilinadi — frontend testni ro'yxatdagi ma'lumot bilan boshlaydi, alohida so'rov yubormaydi.
  - **O'lchangan tezlik** (Oila kodeksi, 40 ta modda sarlavhasi, sovuq kesh): bitta ketma-ket so'rov 17,3 s → 3 parallel × 12 bo'lak 9,5 s → **5 parallel × 8 bo'lak 5,1-7,5 s** (joriy). Keshdan keyin har doim ~0,3 s. `MAX_PARALLEL` cheksiz emas: bepul provayderlarda 429 (rate limit) kelib, tarjimaning bir qismi bajarilmay qolardi.
  - **⚠️ SIFAT TEKSHIRUVI shart bo'ldi:** bepul modellar (llama-3.3-70b) tarjimani buzishi mumkin — haqiqiy sinovda ruscha javobga XITOY iyeroglifi aralashgan (`...正式изируются в письменном договоре`). `looksValid()` uch mezon bilan tekshiradi: begona yozuv (CJK/arab/ibroniy/devanagari/tay), alifbo maqsad tilga mos kelishi (ru→kirill, uz/en→lotin), uzunlik nisbati (0,35-3×; kesilgan yoki izoh qo'shilgan javobni tutadi). O'tmagan bo'lak YOLG'IZ qayta urinib ko'riladi, u ham o'tmasa — **MANBA matn qoladi**. Asosiy qoida: tarjima hech qachon sahifani buzmasin, eng yomon holat "tarjima qilinmagan matn", "bo'sh sahifa" emas. 8 ta test bilan tekshirilgan.
  - **AI SAVOLGA JAVOB BERIB QO'YARDI:** dastlab dars topshiriqlari ham AI tarjimasidan o'tardi va model buyruq gapni ("«X» bo'yicha bilishi kerak 2 narsani yozing") topshiriq deb tushunib, TARJIMA o'rniga JAVOB yozib qaytardi (`The two most important things are: 1. Terms of Employment...`). Ikki chora: (1) promptga 9-qoida qo'shildi ("savol/buyruqqa JAVOB BERMANG, savolning O'ZINI o'giring"); (2) muhimrog'i — topshiriq shablonlari (`WRITTEN_TEMPLATES`, `ARTICLE_QUESTION`) loyihaning O'Z matnlari bo'lgani uchun `lessonPractice.js` da QO'LDA uch tilda yozildi, AI'ga faqat o'zgaruvchi qism (bo'lim sarlavhasi, qonun nomi) beriladi. Xuddi shu sababdan `config/laws.js` ga `nameI18n` qo'shildi — 6 ta kodeksning rasmiy rus/ingliz nomi qo'lda (AI har safar "Кодекс о труде"/"Трудовой кодекс" deb turlicha berardi).
  - **`lessonPractice` da AI O'ZBEKCHA ishlashda QOLDI** — promptlar, baholash mezoni va `BAHO/IZOH/TAVSIYA` formati uzoq sozlangan; modelni boshqa tilda javob berishga majburlash baholash aniqligini ham, `parseVerdict` ni ham buzardi (masalan "BAHO" yorlig'i ruscha qaytsa baho o'qilmay qolardi). Faqat KO'RINADIGAN chiqish (savol, izoh, tavsiya) tarjima qilinadi; `verdict` — ichki kod (`correct`/`partial`/`wrong`), tegilmaydi. Sinov: ruscha javob → `correct`, izoh va tavsiya ruscha.
  - **Yo'l-yo'lakay tuzatildi:** `PricingSection.jsx` tarif nomini backenddan kelgan xom `plan.label` ("Bepul") bilan ko'rsatardi — endi `t[\`tier_${plan.id}\`]` ishlatiladi (tarjimasi allaqachon bor edi). "so'm" 5 joyda QATTIQ yozilgan edi — yangi `currency_uzs` kaliti (uz "so'm" / ru "сум" / en "UZS") bilan almashtirildi.
  - **Sinovlar:** til aniqlash 8/8, sifat tekshiruvi 8/8, uchidan-uchiga HTTP 12/12 (kodeks nomlari 3 tilda, dars bo'limlari, test savollari + `correct` indeksi va variantlar soni saqlangani, modda matni, o'zbekchaga tegilmagani). Tarjima kalitlari: uz/ru/en — 482 tadan, farq yo'q.
  - **⚠️ Bilingan cheklov:** tarjima har bir yangi matn uchun AI so'rovi (kesh sovuq bo'lganda). Bepul provayderlar ishlamay qolsa foydalanuvchi manba tildagi matnni ko'radi — xato chiqmaydi, lekin tarjima ham bo'lmaydi.

- **Darslar sahifasi to'liq qayta dizayn qilindi (2026-08-05):** `pages/Lessons.jsx` va `Lessons.module.css` boshdan yozildi (premium SaaS uslubi — Stripe/Linear/Notion/Coursera yo'nalishida). Palitra SAHIFAGA LOKAL (`.page` ichida CSS o'zgaruvchilari: `#FAFAF9` fon, `#C58B1C` asosiy, `#0F172A` to'q, `#64748B` kulrang) — global mavzu, navbar va footer TEGILMAGAN. Tuzilma: dars hero (ikona + sarlavha + meta pill'lar) + progress kartasi → ikki ustun (70% kontent / 30% yopishqoq panel) → har bo'lim alohida karta (raqam, o'qish vaqti, kodeks chipi, ajratgich, amaliy maslahat oynasi, asosiy fikrlar, ogohlantirish, "Bilasizmi?") → mini test → xulosa → gradient CTA. Yopishqoq panelda: bo'limlar navigatsiyasi (o'qilganlar ✓ bilan), tezkor amallar (AI, kutubxona, konspekt yuklab olish, saqlash), aloqador darslar.
  - **Ma'lumot manbai — haqiqiy:** o'qish vaqti, murakkablik, qaysi kodeks, asosiy fikrlar va xulosa dars matnining O'ZIDAN hisoblanadi (`pages/lessonContent.js`); mini test savollari mavjud `/api/tests` bazasidan olinadi (mavzu bo'yicha moslanadi, 3 ta savol + izoh). Yangi huquqiy fakt to'qib chiqarilmagan — faqat "Bilasizmi?" va ogohlantirish matnlari qo'lda yozilgan va ular ATAYIN modda raqami/muddatsiz, umumiy tamoyil sifatida (qonun tahriri o'zgarsa eskirmasligi uchun).
  - **Progress haqiqiy ishlaydi:** bo'lim ekranga kirganda IntersectionObserver orqali "o'qildi" deb belgilanadi, holat `localStorage.lessonProgress` da dars bo'yicha saqlanadi; ro'yxatdagi kartalarda ham foiz ko'rinadi. "Saqlash" — `localStorage.savedLessons`. "Konspekt" — dars matnidan `.txt` fayl yaratib yuklaydi.
  - Barcha yangi UI matnlari uch tilda (`lesson_*` kalitlari). Ma'lumot olish (`GET /lessons`), video embed, kutubxona va AI havolalari — o'zgarmagan.

- **AI interaktiv darslik — 1-BOSQICH (2026-08-05):** foydalanuvchi to'liq "AI Interactive Learning Module" spetsifikatsiyasini berdi (12 bo'lim: kirish ekrani, AI tushuntirish, o'qilganini tekshirish, mini test + AI yozma baholash, AI savol-javob, moddalarni yodlash, speaking practice, case study, ochilish shartlari, progress/XP, AI mentor, modullar + boss test). Bu bir necha bosqichda quriladi. **Bajarilgani:**
  - *1-bo'lim (kirish ekrani):* dars boshida maqsad, davomiylik, bo'limlar soni, qiyinchilik, tegishli kodeks va "Ushbu darsdan keyin siz:" ro'yxati (bo'lim sarlavhalaridan hosil qilinadi).
  - *2-bo'lim (AI tushuntirish):* har bo'lim ostida **"Bu joyni tushuntirib ber"** tugmasi — chat oynasini shu bo'lim matni bilan tayyor savol qilib ochadi. Buning uchun `ChatPanelContext.openChat(message)` kengaytirildi (`pendingMessage` + `clearPending`), `ChatDrawer` sessiya tayyor bo'lishi bilan savolni avtomatik yuboradi. DIQQAT: `openChat` ba'zi joylarda `onClick={openChat}` sifatida ishlatiladi, ya'ni argument sifatida Event kelishi mumkin — shuning uchun faqat haqiqiy satr qabul qilinadi. Matn 900 belgigacha qisqartiriladi (token/xarajat nazorati), so'rov mavjud `/api/chat` orqali ketadi, ya'ni foydalanuvchining kunlik limitiga bo'ysunadi.
  - **Qaror:** 9-bo'limdagi qulflash tizimi QO'LLANMADI — foydalanuvchining oxirgi aniq ko'rsatmasi ("barcha darsliklar ochiq bo'lsin") ustun deb olindi.
  - **2-BOSQICH — AI baholash tizimi (4, 5, 6, 8-bo'limlar):** yangi `backend/routes/lessonPractice.js` (`/api/lesson-practice`, `userGuard` + `webLimitGuard`) ikki endpoint bilan: `POST /task` (topshiriq beradi) va `POST /check` (javobni baholaydi). To'rt tur: `written` (mavzuni o'z so'zi bilan tushuntirish), `qa` (AI savol beradi), `article` (moddani yodlash), `case` (amaliy vaziyat). Frontend: `pages/LessonPractice.jsx` — 4 ta tab, topshiriq → javob → baho (✅ to'g'ri / ⚠️ qisman / ❌ noto'g'ri) + izoh + eslab qolish tavsiyasi, to'g'ri javobga +5 XP, qismanga +2 XP.
    - **Baholash RAG'ga tayanadi:** har bir baholashda `retrieveLegalContext()` orqali haqiqiy qonun matni tortiladi va promptda "yagona ishonchli manba" sifatida beriladi — AI modda raqami/muddatni o'zidan to'qimasligi uchun (loyihaning asosiy qoidasi).
    - **`article` topshirig'i AI tomonidan O'YLAB TOPILMAYDI** — `LegalChunk` bazasidan HAQIQIY modda tanlanadi (mavjud bo'lmagan modda so'ralishining oldi olinadi), baholashda esa aynan o'sha moddaning matni manba bo'ladi.
    - **`written` topshirig'i ham deterministik** — dars mavzusidan tuziladi. Sinovda AI qo'shma va darsdan murakkabroq topshiriq berib qo'ydi ("qanday shartlar kiritilmasligi kerak va..."), natijada TO'G'RI javob "noto'g'ri" deb baholandi. Endi topshiriq aniq, AI faqat baholaydi (tezroq va arzonroq ham).
    - **Baholash darajaga moslashtirildi:** promptga aniq mezon qo'shildi (boshlang'ich talaba, so'zma-so'z ta'rif shart emas). Tuzatishdan keyin sinov: yaxshi javob → `correct`, "Bilmayman" → `wrong`.
    - **Yangi AI chaqiruvi `askStructured()`** (`legalAI.js` dan eksport) — provayder zanjirini qayta ishlatadi, lekin `getLegalAdvice()` ga TEGMAYDI (u huquqiy maslahat uchun moslangan: qat'iy format, xavfsizlik filtri, "huquqiy savolmi?" tekshiruvi — baholashda bular xalaqit berardi).
    - **TUZATISH (2026-08-05, foydalanuvchi xabaridan keyin):** ikkita jiddiy nuqson bor edi. (1) *Topshiriq har safar bir xil chiqardi* — endi backend dars BO'LIMLARI ro'yxatidan tasodifiy bo'lim va 4 ta savol shaklidan bittasini tanlaydi, oldingi savol `exclude` orqali chetlab o'tiladi (sinov: 4 urinishda 4 xil savol). (2) *To'g'ri javob "noto'g'ri" deb baholanardi va izohlar savolga aloqasiz chiqardi* — sababi baholash promptida RAG topgan tasodifiy modda "yagona ishonchli manba" deb berilgan edi, AI javobni savolga emas o'sha moddaga qarab tekshirardi. Endi **dars matni ASOSIY mezon** (`lessonContext`), qonun matni faqat qo'shimcha, va promptda aniq yozilgan: "faqat berilgan savolga javob berilganini tekshiring, dars matnida yo'q tafsilot uchun ball tushirmang". Shuningdek `parseVerdict` bahoni faqat BIRINCHI qatordan oladi (izoh ichidagi "noto'g'ri" so'zi bahoni buzardi). Sinov: mos javob → `correct`, "Nikoh — bu to'y" → `wrong`, boshqa mavzudagi javob → `wrong`.
    - **XP faqat BIR MARTA (2026-08-05):** har bir dars ichida har bir topshiriq TURI (yozma/savol-javob/modda/vaziyat) uchun XP bir marta beriladi — aks holda bitta topshiriqni qayta-qayta topshirib XP "yig'ib olish" mumkin edi. `lessonProgress.recordPracticeXp()` shu tur bo'yicha OLINGAN ENG YUQORI XP'ni saqlaydi va faqat FARQINI qaytaradi: qisman to'g'ri (+2) → keyin to'liq to'g'ri (+3), takrorlasa 0. Turlar va darslar bir-biridan mustaqil. UI: XP berilmasa "XP allaqachon olingan" yozuvi chiqadi, bajarilgan tab yonida ✓ belgisi turadi. 17 ta test bilan tekshirilgan.
    - **5-tur: GAPIRISH (speaking practice, spetsifikatsiyaning 7-bo'limi) — 2026-08-05:** AI savol beradi, foydalanuvchi mikrofon orqali OG'ZAKI javob beradi, javob matnga aylantirilib baholanadi. Yangi backend endpointi YOZILMADI — mavjud `POST /api/voice/transcribe` qayta ishlatildi (frontend `MediaRecorder` bilan yozadi → transkripsiya → odatdagi `/lesson-practice/check`). Baholashda og'zaki javobga yumshoqlik qo'shilgan: imlo/tinish belgilari va so'z tartibidagi kichik xatolar uchun ball tushirilmaydi, faqat mazmun tekshiriladi.
    - **MAVZUGA QAT'IY BOG'LASH (2026-08-05, foydalanuvchi shikoyati):** oila huquqi darsida boshqa sohadagi savollar chiqardi. Sabab — RAG qidiruvi va zaxira modda tanlash BUTUN baza bo'yicha ishlardi. Endi `lawCodeFor(lessonTitle)` dars mavzusiga mos kodeksni aniqlaydi (OK/MK/FK/YK/JK) va: (a) `retrieveLegalContext` shu kodeks bilan cheklanadi; (b) `article` turida zaxira modda ham faqat SHU kodeksdan olinadi (avval butun bazadan — oila darsida jinoyat kodeksi moddasi chiqishi mumkin edi); (c) promptga "MAVZUDAN CHIQMA" qoidasi qo'shildi. Sinov: Oila darsida 6 ta topshiriq — mavzudan chiqqani **0 ta**. Shuningdek modda savoli ketma-ket takrorlanmasligi uchun `exclude` dagi modda raqami chetlab o'tiladi (sinov: 5 urinishda ketma-ket takror 0).
    - **⚠️ Limit:** har bir topshiriq va har bir baholash — 1 ta so'rov (`webLimitGuard`), ya'ni bepul tarifdagi 5 ta kunlik limit tez tugaydi. Kelajakda darslar uchun alohida limit kerak bo'lishi mumkin.
  - **Keyingi bosqichlar (hali qilinmagan):** foydalanuvchi progressini bazaga ko'chirish (hozir faqat `localStorage` — boshqa qurilmada yo'qoladi, AI mentor va modullar uchun SHART), AI yozma topshiriq/case study/savol-javob baholash endpointlari, moddalarni yodlash, speaking practice (ovoz infratuzilmasi `routes/voice.js` da tayyor), modullar + boss test, AI mentor.

- **Mobil header tuzatildi (2026-08-05):** mehmonlar uchun til almashtirgich qo'shilgach, telefonda header ekrandan chiqib ketardi. **Asosiy sabab:** "Kirish" va "Ro'yxatdan o'tish" tugmalari JSX ichida INLINE uslub bilan yozilgan edi (qat'iy `padding`, `font-size`) — media-qoidalar ularga umuman ta'sir qila olmasdi. Ular `.authBtns` / `.authGhost` / `.authGold` klasslariga o'tkazildi va uch bosqichli mobil o'lchamlar qo'shildi: **≤768px** — header 68→60px, logo 36→30px, tugma matni 0.78rem; **≤420px** — header 56px, logo 26px, matn 0.72rem, yon bo'shliq 0.75rem; **≤340px** — "Kirish" tugmasi yashiriladi (ro'yxatdan o'tish sahifasida "Hisobingiz bormi? Kirish" havolasi bor, ya'ni kirish yo'li yo'qolmaydi). Qo'shimcha himoya: `.navInner > * { min-width: 0 }`. Hisob-kitob: 360px ekranda kerakli kenglik ~276px — bemalol sig'adi.

- **Mobil navigatsiya qayta taqsimlandi (2026-08-05):** foydalanuvchi so'roviga ko'ra mehmon va login qilgan foydalanuvchi uchun navigatsiya ajratildi.
  - **Mehmonlarda burger menyu YO'Q** (`{user && <button className={s.menuBtn}>}`) — telefonda ular uchun pastki panel yetarli. Pastki panel bandlari: **Bosh sahifa | Xizmatlar | AI yordamchi | Narxlar | Aloqa** (AI markazda, kattaroq). Desktopda mehmon menyusi o'zgarmadi (u yerda burger baribir ko'rinmaydi).
  - **⚠️ Yon ta'sir hal qilindi:** mobil rejimda til almashtirgich FAQAT burger menyu ichida edi (`.langDesktop` ≤768px da yashiriladi) — burgerni olib tashlash mehmonlarni tilni o'zgartirish imkonidan mahrum qilardi. Shuning uchun mehmonlar uchun u yuqorigi panelda ko'rsatiladi (`.langMobileVisible`). Kirish/Ro'yxatdan o'tish tugmalari ham yuqorigi panelda qoladi.
  - **Login qilganlarda pastki panel o'zgarmadi** (Bosh sahifa · Darslar · AI · Testlar · Kutubxona), lekin **burger menyudan takroriy bandlar olib tashlandi** — endi u yerda faqat pastki panelga sig'maganlari qoladi ("Biz haqimizda" guruhi, Profil, til, chiqish).
  - **Burger menyu endi SCROLL bo'ladi** (`overflow-y: auto` + `overscroll-behavior: contain` + nozik scrollbar; `.mobileMenu > * { flex-shrink: 0 }` bandlar siqilib ketmasligi uchun) — avval kichik ekranlarda pastdagi bandlar ko'rinmay qolardi.

- **⚠️ LOGOGA OQ FILTR QO'LLANMASIN (2026-08-05):** logo — och krem fonli, oltin rangdagi TAFSILOTLI medalyon (adolat ma'budasi + "HUQUQ TA'LIMI" yozuvi). Bir muddat unga `filter: brightness(0) invert(1)` qo'llangan edi (to'q ko'k navbarda ko'rinsin degan niyatda) — natijada u butunlay bir tekis OQ DOIRAGA aylanib, tanib bo'lmas holga keldi. Filtr olib tashlandi: medalyonning o'z foni allaqachon och, shuning uchun `--navy` fonda hech qanday filtrsiz ham aniq ko'rinadi. **Saboq:** rasmga filtr qo'llashdan oldin rasmning o'ziga qarash kerak (Read tool PNG'ni ko'rsata oladi) — men buni ko'rmasdan qilib, logoni buzgan edim.

- **Sayt logosi o'rnatildi (2026-08-05):** `frontend/public/logo.png` (600×495, 192 KB) loyihaning rasmiy logosi sifatida hamma joyga qo'yildi. Avval logo o'rnida inline SVG (yashil gradient kvadrat ichida tarozi ikonasi) ishlatilardi — u 6 joyda takrorlangan edi. **TEZLIK uchun ikki nusxa:** `logo-96.png` (**8,3 KB**, `sips -Z 96` bilan yaratilgan) — interfeys uchun (header, mobil menyu, footer, kirish/ro'yxatdan o'tish sahifalari, chat oynasidagi AI avatari, suzuvchi tugma, favicon); `logo.png` (to'liq o'lcham) — ijtimoiy tarmoqlarda ulashish uchun (`og:image`, `twitter:image`, `apple-touch-icon`). CSS'da `.logoIcon` (gradient fon + ikona) `.logoImg` (toza rasm, `object-fit: contain`) bilan almashtirildi, hover effekti saqlandi. `ChatDrawer.jsx` va `FloatingConsultButton.jsx` da `import favicon from "../../public/favicon.png"` olib tashlandi — `public/` fayllari allaqachon statik beriladi, ularni bundle'ga qo'shish ortiqcha edi. Eski `public/favicon.png` (40 KB) o'chirildi — havolalar qolmagan.

- **Mobil pastki navigatsiya (2026-08-05):** yangi `components/BottomNav.jsx` + `.module.css` — telefon ekranida (≤768px) ekran pastida doimiy panel (Instagram/Telegram ilovalaridagi kabi), desktopda umuman ko'rinmaydi. **Login qilganlar uchun:** Bosh sahifa · Darslar · **AI** · Testlar · Qonunlar kutubxonasi. **Mehmonlar uchun:** Bosh sahifa · Narxlar · **AI** · Kirish. Markazdagi AI tugmasi ataylab kattaroq va paneldan biroz chiqib turadi (oltin gradient doira) — u saytning asosiy funksiyasi; bosilganda chat oynasini ochadi. Nozik jihatlar: `env(safe-area-inset-bottom)` bilan iPhone'ning "home indicator" zonasi hisobga olinadi; `.main` va `.footer` ga pastki bo'shliq qo'shildi (kontent panel ostida qolmasin); chat ochilganda panel yashiriladi (`isOpen` tekshiruvi) va `z-index: 900` — ChatDrawer (1000) dan past; **telefonda suzuvchi chat tugmasi (`FloatingConsultButton`) yashiriladi** — aks holda ikkalasi ekranning pastki qismida ustma-ust tushardi. Faollik mantig'i 7 ta holatda tekshirilgan; `/#pricing` kabi hash havolalar hech qachon "faol" deb belgilanmaydi (ular alohida sahifa emas). Yangi tarjima kaliti qo'shilmadi — mavjudlari qayta ishlatildi.

- **Loyiha tozalandi — keraksiz fayllar o'chirildi (2026-08-05):** import grafi bo'yicha to'liq skan qilinib, 15 ta fayl o'chirildi (~2,9 MB): `frontend/assets/` butunlay (saytLogo.png va adaptive-icon.png — har biri 1,4 MB, favicon `/favicon.png` ga o'tkazilgach ishlatilmay qoldi); `pages/Login.jsx`, `pages/Register.jsx` (1 qatorlik takroriy re-export'lar), `pages/Profile.jsx` (ProfileModal bilan almashtirilgan); **eski takroriy admin panel** — `pages/AdminPanel.jsx` + `.module.css` + `AdminLogin.jsx` va `App.jsx` dagi `/admin/*` marshrutlari (haqiqiy admin — alohida `admin/` ilovasi); `admin/src/pages/LoginLogs.jsx` + `Visitors.jsx` (yozilgan, lekin marshrutga ulanmagan); `frontend/Dockerfile`, `frontend/nginx.conf`, `admin/Dockerfile`, `admin/nginx.conf`, `docker-compose.yml` (ikkala frontend Vercel'da, baza esa Atlas'da).
  - **⚠️ MUHIM:** endi asosiy saytda `/admin` YO'Q. Admin paneliga kirish uchun `admin/` ilovasi deploy qilinishi shart (hozir `huquq-ai-admin.vercel.app` 404 qaytaradi). Kerak bo'lsa qaytarish: `git checkout HEAD -- frontend/src/pages/AdminPanel.jsx frontend/src/pages/AdminLogin.jsx frontend/src/pages/AdminPanel.module.css` + `App.jsx` ga marshrutlarni qaytarish.
  - **Tekshirilgan va SAQLANGAN** (birinchi qarashda ishlatilmaydiganday ko'rinadi): `frontend/src/data/lessons.js` + `tests.js` (`backend/scripts/seedContent.js` o'qiydi), ikkala `styles/global.css` (`main.jsx` da `import "..."` shaklida), `backend/uploads/` (bo'sh, lekin `routes/chat.js` ishlatadi), `backend/Dockerfile` (Render aynan shu bilan build qiladi), `backend/scripts/*` (server.js chaqiradi). `backend/SMTP_SETUP.md` o'chirilmadi — undagi eskirgan xatboshi ("email tasdiqlash o'chirilgan") hozirgi holatga moslashtirildi.

- **TEZLIK optimizatsiyasi (2026-08-05):** foydalanuvchi "sayt juda sekin ishlayapti" deb xabar berdi. O'lchov asosida 5 ta sabab topildi va tuzatildi:
  1. **Favicon 1.4 MB edi.** `index.html` da `assets/saytLogo.png` (1024×1024, **1,4 MB**) favicon sifatida ulangan edi — brauzer uni har bir sahifa yuklanishida tortardi va u bundle'ga ham tushardi. `public/favicon.png` (640×640, 39 KB) allaqachon mavjud edi — shunga o'tkazildi (**−97%**).
  2. **Kod bo'linishi yo'q edi.** Barcha sahifalar (admin panel, ovozli chat, darslar, huquqiy hujjatlar) bitta 677 KB bundle'da kelardi. `React.lazy` + `Suspense` bilan 13 ta alohida bo'lakka ajratildi; `ChatDrawer` ham (react-markdown + remark-gfm ni tortadi, ~174 KB) faqat chat ochilganda yuklanadi. Birinchi yuklanish: **677 KB → 392 KB (−42%)**. Bosh sahifa va `SiteLayout` ataylab lazy QILINMADI (birinchi ko'rinish uchun kerak).
  3. **Har bir sahifa yuklanishida TASHQI so'rov.** `Home.jsx` visitor tracking uchun `api.ipify.org` ga murojaat qilardi — ortiqcha tarmoq kechikishi va begona xizmatga bog'liqlik. Backend IP'ni so'rovning o'zidan biladi (`getClientIp`), shuning uchun `routes/visitor.js` endi `req.body.ip` bo'lmasa o'zi aniqlaydi, frontenddagi ipify chaqiruvi olib tashlandi.
  4. **Scroll inersiyasi juda og'ir edi** (`WHEEL_LERP = 0.082`, 300px burash ~1.3 s) — bu aynan "sayt sekin" hissini berardi. `0.16` ga (~0.45 s) va dasturiy scroll `950 → 620 ms` ga tezlashtirildi. Sinovlar (15 + 9 test) qayta o'tkazildi.
  5. **`.reveal` animatsiyasida `filter: blur(6px)`** — blur har kadrda GPU'da qayta hisoblanadi va sahifada o'nlab element bir vaqtda animatsiya qilinganda sezilarli sekinlashuv beradi (ayniqsa kuchsiz telefonlarda). Blur butunlay olib tashlandi, davomiylik `1.1s → 0.6s` (endi faqat `transform` + `opacity` — kompozitor darajasida deyarli tekin).
  - Yo'l-yo'lakay: `@google/genai` (14 MB paket) frontend `package.json` dan olib tashlandi — u yerda umuman ishlatilmagan (build va `npm install` tezlashadi).

- **Inersiyali g'ildirak scrolli (2026-08-05):** foydalanuvchi aniq so'ragani uchun (avval xavf tufayli qilinmagan edi) `initSmoothWheel()` qo'shildi — sahifa g'ildirak burashida darhol emas, "og'irlik" bilan siljiydi (har kadrda maqsadga `lerp = 0.082` ulush yaqinlashadi, kadr chastotasidan mustaqil). Xavfsizlik choralari: sensorli qurilmalarda (`pointer: coarse`) va `prefers-reduced-motion` da UMUMAN yoqilmaydi; modal ochiq (body qulflangan) bo'lsa aralashmaydi; ichki scrollli bloklar avtomatik chetlab o'tiladi (`findScrollableAncestor`). **`data-scroll-lock`** atributi qo'yilgan blok (dars sahifasidagi yopishqoq panel) ustida g'ildirak burilganda SAHIFA umuman qimirlamaydi — panelda scroll qiladigan joy qolmagan holatda ham (`e.preventDefault()`), chunki foydalanuvchi "o'ng tomonni scroll qilsam chap tomon ketyapti" muammosini bildirgan edi. Dasturiy scroll boshlanganda `smoothscroll:start` hodisasi orqali damper to'xtaydi (ikkalasi tortishmasligi uchun). `start()` shartida `raf !== null` tekshiriladi — `running` bo'yicha tekshirilganda kadr tushib qolsa damper qotib qolardi. Jami 24 ta test (15 g'ildirak + 9 dasturiy scroll).

- **Sekin va yumshoq scroll — sayt bo'ylab (2026-08-05):** yangi `frontend/src/utils/smoothScroll.js` — harakat `requestAnimationFrame` + `easeInOutCubic` bilan chiziladi (~950ms, masofaga qarab moslashadi), shuning uchun brauzerning qat'iy ~300ms silliqlashidan ko'ra ancha yoqimli. `initSmoothAnchors()` (App.jsx da bir marta) sayt bo'ylab BARCHA `#` havolalarini delegatsiya orqali qamrab oladi; `scrollToTop()` / `scrollToElement()` esa dasturiy chaqiruvlar o'rniga ishlatiladi (Lessons, Home, Tests, Library). **MUHIM:** `global.css` dagi `html { scroll-behavior: smooth }` `auto` ga o'zgartirildi — aks holda brauzer silliqlashi bilan JS animatsiyasi bir-biriga xalaqit berib, harakat uzuq-yuluq bo'lardi. Foydalanuvchi o'zi scroll qilsa (wheel/touch) animatsiya darhol to'xtaydi; `prefers-reduced-motion` da umuman animatsiya qilinmaydi. `.reveal` animatsiyasi 0.9s → 1.1s ga sekinlashtirildi. Sichqoncha g'ildiragi ATAYIN ushlab olinmagan (Lenis kabi inersiya kutubxonalari ichki scrollli bloklarni — yopishqoq panel, chat oynasi — va mobil/klaviatura navigatsiyasini buzadi). Mantiq 9 ta test bilan tekshirilgan (aniq yetib borish, monoton harakat, easing egri chizig'i, navbar ofseti, chegaradan chiqmaslik, a11y).

- **Darsni tugatish tizimi — Duolingo uslubida (2026-08-05):** `pages/lessonProgress.js` (yangi, React'dan mustaqil toza mantiq) + `Lessons.jsx` dagi `FinishLesson` komponenti.
  - **Scroll bilan dars TUGALLANMAYDI.** O'qish faqat 1-bosqich ko'rsatkichi; tugallanish uchun dars oxiridagi "Darsni tugatish" tugmasi bosilib, qisqa test (`QUIZ_SIZE = 2` savol) BEXATO yechilishi shart. Progress: o'qish eng ko'pi 50%, 100% faqat testdan keyin.
  - **Savollar banki:** bazadagi haqiqiy testlardan (`/api/tests`) yig'iladi — avval dars mavzusiga mos test savollari, keyin qolganlari (`buildQuestionBank`). Har urinishda savollar tasodifiy va OLDINGILARIDAN BOSHQA (`pickQuestions` + `burned` ro'yxati). Bank tugasa qayta boshlanadi, lekin oxirgi urinish savollari darhol takrorlanmaydi.
  - **Halollik nazorati:** test davomida tab almashtirilsa / sahifa tark etilsa (`visibilitychange`, `pagehide`, `blur`) urinish BEKOR qilinadi va qaytganda yangi savollar beriladi. Savollar tanlangan ZAHOTI "ishlatilgan" deb `localStorage`ga yoziladi — shu sabab sahifa yangilansa ham o'sha savollar qayta chiqmaydi.
  - **Darslar QULFLANMAYDI** (foydalanuvchi so'roviga ko'ra, 2026-08-05): dastlab ketma-ket ochilish (`isUnlocked`) qo'shilgan edi, keyin butunlay olib tashlandi — har bir dars istalgan tartibda ochiladi. Test faqat "tugallandi" belgisi va XP beradi, kirishni cheklamaydi.
  - **Yopishqoq yon panel tuzatildi (2026-08-05):** `.layout` da `align-items: start` turgani sabab o'ng ustun faqat kontenti balandligicha qisqarardi va ichidagi `position: sticky` element "sayohat" qila olmay, chap ustun bilan birga scroll bo'lib ketardi. Olib tashlandi (standart `stretch`) + `.sidebar { align-self: stretch }`. `.sideInner` esa `max-height: calc(100vh - 108px)` + `overflow-y: auto` + `overscroll-behavior: contain` bilan ALOHIDA scroll bo'ladi (nozik scrollbar uslubi ham qo'shilgan). `top: 88px` — navbar 68px (`SiteLayout`) + 20px.
  - **Gamifikatsiya:** har tugallangan dars uchun +10 XP (`localStorage.lessonXp`, ro'yxat va dars hero'sida ko'rinadi), "Tugallandi" nishoni, bosqich ko'rsatkichi (📖 → 📝 → ✅), g'alaba animatsiyasi (`celebrateIn`/`pop`, `prefers-reduced-motion` hurmat qilinadi).
  - **Mantiq 27 ta test bilan tekshirilgan** (savol takrorlanmasligi, kichik bankda tsikl bo'lmasligi, bosqichlar, qulf ochilishi, shuffle tasodifiyligi) — `node` orqali, brauzersiz.

- **Lokal va server bir vaqtda ishlashi (2026-08-04):** avval `VITE_API_URL` 12 ta faylda to'g'ridan-to'g'ri o'qilardi, shu sabab lokal test uchun `.env` ni `localhost:3000` ga, deploy oldidan yana Render manziliga qo'lda o'zgartirish kerak edi (unutilsa sayt ishlamay qolardi — aynan shu bo'ldi). Endi yagona manba: `frontend/src/utils/apiBase.js` va `admin/src/utils/apiBase.js` — manzil SAHIFA QAYERDA OCHILGANIGA qarab avtomatik tanlanadi (localhost/127.0.0.1 → `http://localhost:3000`, boshqa domen → `VITE_API_URL` yoki standart Render manzili). Bitta build ikkala muhitda ham to'g'ri ishlaydi, `.env` ni o'zgartirish SHART EMAS. Kerak bo'lsa `VITE_LOCAL_API_URL` bilan lokal manzilni boshqarish mumkin. Yo'l-yo'lakay tuzatildi: `admin/.env` da `VITE_API_URL=http://localhost:3000` turgan edi — ya'ni serverga chiqarilgan admin panel localhost'ga ulanmoqchi bo'lardi. Dev proxy'lar (`vite.config.js`) ham lokal backendga qaratildi.

- **AI javob sifati — 2-audit (2026-08-05, foydalanuvchi shikoyatidan keyin):** savol *"ajrim bo'lsa, bola 12 yoshga yetmagan bo'lsa kimda qoladi?"* — javob esa "sudga ariza yozing / bola manfaati hisobga olinadi" degan bo'sh shablon bo'lgan, ASOSIY SAVOLGA javob umuman berilmagan. Ikki mustaqil sabab aniqlandi va ikkalasi ham tuzatildi:
  1. **RAG mutlaqo noto'g'ri moddalarni topardi.** Shu savolga farzandlikka olish (157), aliment miqdori (99), ZAGS (43) moddalari qaytgan; kerakli **Oila kodeksi 44-moddasi** ("...er va xotin voyaga yetmagan bolalari KIM BILAN YASHASHI...") bazada BOR, lekin vektor qidiruvga tushmagan. Sabab: foydalanuvchi kundalik tilda yozadi ("ajrim", "bola kimda qoladi"), qonun esa rasmiy atama bilan ("nikohdan ajratish", "bolalar kim bilan yashashi"). Yechim `legalRetrieval.js` da: (a) `expandQuery()` — so'rovga rasmiy atamalar QO'SHILADI (almashtirilmaydi, 11 ta mavzu naqshi); (b) `keywordSearch()` — kalit so'z bo'yicha zaxira qidiruv, natijalar vektor natijalariga dublikatsiz qo'shiladi (apostrof variantlari hisobga olinadi). Tuzatishdan keyin 44-modda topilyapti.
  1b. **MASLAHAT faqat so'ralganda (2026-08-05, ikkinchi shikoyat):** foydalanuvchi aniq aytdi — *"men shunchaki savol bersam, savolimga javob bersin, maslahat bermasin; agar menda shunday bo'ldi desam — keyin maslahat bersin"*. Shuning uchun mantiq TESKARI qurildi: standart holat — **faqat javob**, maslahat esa faqat ikki holatda: (a) foydalanuvchi o'z holatini aytgan (`PERSONAL_PATTERNS`: "meni", "erim", "ish beruvchim", "shunday bo'ldi"...), (b) aniq harakat so'ragan (`ACTION_PATTERNS`: "nima qilay", "qayerga murojaat"). Qo'shimcha qoida: egasiz aytilgan voqea ("ishdan bo'shatishdi", "ish haqimni bermayapti") ham maslahat oladi, LEKIN savol so'zi bo'lsa ("meros qanday taqsimlanadi?") — oddiy savol hisoblanadi. **Muhim tuzatish:** dastlabki naqsh `\w+di` edi va u `-di/-adi` bilan tugaydigan HAR QANDAY fe'lni voqea deb hisoblardi ("taqsimlanadi", "bo'linadi") — natijada oddiy savolga ham maslahat berilardi. `EXPLAIN_FORMATS` dan maslahat qismi butunlay olib tashlandi va uch tilda QAT'IY TAQIQ qo'shildi: "sudga murojaat qiling / ariza yozing / Hozir nima qiling / Murojaat" bo'limlarini yozish taqiqlanadi. Sinov: 7/7 to'g'ri (4 ta oddiy savol → faqat javob, 3 ta shaxsiy holat → maslahat).
  2. **Javob formati har doim "harakat" qolipida edi.** `buildSystemPrompt` hamma savolga majburiy "Hozir nima qiling / Huquqlaringiz / Murojaat" formatini qo'llardi — tushuntirish talab qiladigan savolda bu bo'sh byurokratik matn beradi. Endi `isExplanatory()` savol turini aniqlaydi va `EXPLAIN_FORMATS` (uch tilda) ishlatiladi: avval SAVOLGA to'g'ridan-to'g'ri javob, keyin mezonlar, oxirida qisqa amaliy maslahat; "sudga ariza yozing" bilan boshlash TAQIQLANGAN. Harakat so'rovlari (`ACTION_PATTERNS` — "nima qilay", "qayerga murojaat") eski formatni saqlaydi. Naqsh o'zbek fe'l qo'shimchalari bo'yicha ishlaydi (`qanday \\S*(adi|iladi|ladi|nadi|tadi)`) — avval 5 ta fe'l qo'lda sanalgan va "qanday bo'linadi" e'tibordan chetda qolgan edi. Sinov: 5/5 savol turi to'g'ri aniqlandi, javob endi 44-moddaga asoslanib to'g'ridan-to'g'ri javob beryapti.

- **AI javob sifati auditi va tuzatishlar (2026-08-04):** haqiqiy savollar bilan o'tkazilgan audit 6 ta jiddiy nuqsonni ochdi, hammasi `services/legalAI.js` da tuzatildi (arxitektura o'zgarmadi):
  1. **Oddiy huquqiy savollar rad etilardi.** "Ajrashganda bolam kim bilan qoladi?" → *off_topic* edi, chunki `LEGAL_SIGNALS` da faqat "ajralish" bor, "ajrash..." yo'q edi. Endi o'zak (stem) bo'yicha qidiriladi + apostrof variantlari (`'ʻʼ'`) `normalizeUz()` bilan normallashtiriladi (avval "boʻshatish" yozgan foydalanuvchi kategoriyasiz qolardi).
  2. **Suhbat davomidagi savollar rad etilardi.** "Buning uchun qanday hujjatlar kerak?" → *off_topic*. Endi `isLegalQuestion(text, inDialog)` — tarix bo'lsa savol huquqiy hisoblanadi (faqat aniq begona mavzu rad etiladi). Qo'shimcha: qisqa davom savolida kategoriya va RAG so'rovi oldingi xabarlar bilan boyitiladi, aks holda mehnat nizosi suhbatida Fuqarolik kodeksidan javob qidirilardi.
  3. **Javob formati buzilardi.** `validateAnswer()` matnni jumlalarga bo'lib, natijani BO'SH JOY bilan qayta birlashtirardi — nuqta bilan tugagan ro'yxat bandlaridan keyingi qator uzilishlari yo'qolib, javob bir uyum matnga aylanardi. Endi qator ajratgichlari saqlanadi (5 qator → 12-14 qator).
  4. **Modellar aniq raqamlarni TAXMIN QILARDI.** Bitta savolga ("sudga qancha muddat ichida murojaat qilaman?") uchta model uchta xil javob berdi: 1 oy / 2 oy / 3 oy. Promptga qat'iy qoida qo'shildi: muddat/foiz/summa faqat RAG bergan tekshirilgan matnda bo'lsa keltiriladi, bo'lmasa "tekshiring" deyiladi. Tuzatishdan keyin javob **MK 560-moddasi**ga asoslanib "uch oy" berdi — bazadan tekshirildi, modda matni aynan shunday (to'g'ri javob).
  5. **Rus tilidagi javobda o'zbekcha sarlavhalar aralashardi** ("Хозир нима қилинг:") — prompt formati va namunasi faqat o'zbek tilida edi. Endi `FORMAT_SECTIONS` uch tilda alohida.
  6. **Inglizcha savollar rad etilardi** ("I was fired without reason" → off_topic) — inglizcha signal ro'yxati juda tor edi. Kengaytirildi + rus/ingliz kategoriya o'zaklari qo'shildi.
  - **Suhbat holatlari:** "Sen kimsan?" endi o'zini va imkoniyatlarini tanishtiradi, "Rahmat" iliq javob oladi (avval ikkalasi ham quruq "faqat huquqiy savollarga javob beraman" olardi).
  - **Yakuniy regressiya: 14 test, 0 xato.** ⚠️ **Claude hali ham ISHLAMAYDI** — `400 "credit balance is too low"` (2026-08-04 da qayta tekshirildi). Ya'ni Pro/Premium obunachilar ham bepul modellar (Cloudflare llama-3.3-70b) bilan javob olyapti. Gemini (`gemini-2.5-flash`) va Groq ishlayapti.

- **Joriy jonli manzillar + CORS (2026-08-04):** frontend — `https://huquq-ai-xi.vercel.app`, backend — `https://huquq-ai-2gvs.onrender.com`. Eski manzillar (`huquq-ai-rose.vercel.app`, `huquq-ai-fpa2.onrender.com`) O'CHIRILGAN, 404 qaytaradi. `server.js`dagi CORS ro'yxatida faqat o'sha eski manzillar turgani uchun jonli sayt backenddan HECH NARSA ololmasdi (brauzer hamma so'rovni "No Access-Control-Allow-Origin" bilan bloklardi). Tuzatildi: ro'yxat yangilandi + `huquq-ai*.vercel.app` naqshi qo'shildi (har yangi Vercel deploy'da kodga qayta tegish shart emas) + rad etishda endi Error tashlanmaydi (log stack trace bilan to'lmasin). **Eslatma:** kodga tegmasdan ham tuzatish mumkin — Render'da `FRONTEND_URL` ga frontend manzilini yozish kifoya, chunki CORS ro'yxati shu o'zgaruvchidan ham to'ldiriladi. Barcha fayllardagi eski domen havolalari ham yangilandi (`index.html`, `sitemap.xml`, `robots.txt`, `Seo.jsx`, ikkala `vite.config.js` proxy, `telegram-bot.js`, admin `Settings.jsx`/`Login.jsx`).

- **Profil tarif ma'lumoti, parolni tiklash va email tasdiqlash (2026-08-04):**
  - **Profil > "Pro" tab endi tarifga qarab o'zgaradi.** Avval ikkita qotib qolgan matn (`pro_daily_limit_free` / `pro_daily_limit_pro`) ishlatilardi — shu sabab Basic sotib olgan ham, Premium sotib olgan ham "kuniga 200 ta savol" ni ko'rardi. Endi `GET /api/payment/plan` javobiga `label`, `dailyLimit`, `useClaude`, `daysLeft` va (bepul foydalanuvchi uchun) `upgrade` maydonlari qo'shildi — hammasi `config/plans.js` dan keladi, ProfileModal esa faqat shuni ko'rsatadi + tegishli `pricing_features_{tier}` ro'yxatini chiqaradi. Yo'l-yo'lakay tuzatildi: bepul foydalanuvchiga sotib olish narxi `0 so'm` ko'rinardi (joriy tarif narxi olinardi) — endi taklif qilinayotgan tarif (`pro`) narxi ko'rsatiladi.
  - **"Parolni unutdingizmi?" ishlamas edi.** Login sahifasidagi oyna `axios.post("/api/support")` — ya'ni NISBIY manzilga so'rov yuborardi; frontend Vercel'da, backend Render'da bo'lgani uchun so'rov backendga umuman yetib bormasdi ("Xatolik yuz berdi"). Endi yangi `POST /api/auth/forgot-password` (`api` instansiyasi orqali) ishlatiladi: admin paneliga `type: "password_reset"` turidagi murojaat tushadi (`SupportMessage`ga `type`, `userId`, `passwordResetAt` maydonlari qo'shildi), admin `admin/src/pages/SupportMessages.jsx` dan bir bosishda parolni yangilaydi va u foydalanuvchi emailiga ketadi (`PATCH /api/admin/support/:id/reset-password` — parol kiritilmasa xavfsiz tasodifiy parol yaratadi). MUHIM: email yuborilmasa parol O'ZGARTIRILMAYDI (aks holda foydalanuvchi yangi parolni bilmay hisobidan ayrilardi). Bir email uchun 3 daqiqalik cooldown bor. Xavfsizlik: `GET/PATCH/DELETE /api/support` avval umuman himoyalanmagan edi (istalgan odam barcha murojaat va emaillarni o'qiy olardi) — `adminGuard` qo'shildi, admin ilovasi allaqachon adminToken yuborgani uchun hech narsa buzilmadi.
  - **Ro'yxatdan o'tishda email tasdiqlash QAYTA YOQILDI.** Avval `register` da "OTP bosqichi olib tashlangan" edi va hisob darhol `emailVerified: true` bo'lardi. Endi qo'lda ro'yxatdan o'tganda hisob `emailVerified: false` holatida yaratiladi, emailga 6 xonali kod ketadi (10 daqiqa) va token faqat `POST /verify-otp` dan keyin beriladi. Email ketmasa — yarim yaratilgan hisob DARHOL o'chiriladi (aks holda username/email "band" bo'lib qolardi). `login` ham endi tasdiqlanmagan hisobga yangi kod yuborib `403 + needsVerification` qaytaradi (frontend allaqachon shu javobni OTP ekraniga ulagan edi). Google orqali kirish o'zgarmadi (u yerda email allaqachon tasdiqlangan).
  - **YOLG'ON XABAR tuzatildi (2026-08-05):** `DEV_OTP_TO_CONSOLE` rejimida foydalanuvchiga baribir "Tasdiqlash kodi emailingizga yuborildi" deb ko'rsatilardi — holbuki email umuman ketmagan, kod faqat server konsoliga yozilgan edi. Foydalanuvchi pochtasini bekorga kutardi (aynan shu haqda shikoyat keldi). Endi `trySendOtp()` `viaConsole: true` qaytaradi, `register`/`login`/`resend-otp` javoblariga `devCodeInConsole` bayrog'i qo'shildi, frontend esa OTP ekranida sariq ogohlantirish ko'rsatadi ("kod pochtaga YUBORILMADI — backend terminaliga yozildi", uch tilda `otp_dev_console`).
  - **Email xatolarini turkumlash + lokal test zaxira yo'li (2026-08-04):** `emailService.js` endi xatoga `configIssue` belgisini qo'yadi (401/403/5xx — API kalit, tasdiqlanmagan jo'natuvchi, IP cheklovi yoki Brevo nosozligi). `auth.js`dagi yangi `trySendOtp()` shu belgiga qarab to'g'ri xabar beradi — avval har qanday nosozlikda "email manzilingiz to'g'ri ekaniga ishonch hosil qiling" deyilardi, ya'ni ayb foydalanuvchiga yuklanardi. IP cheklovi aniqlansa server logiga aniq ko'rsatma (havola bilan) yoziladi. `DEV_OTP_TO_CONSOLE=true` (`.env`) — LOKAL test uchun: email xizmati ishlamasa ham ro'yxatdan o'tish davom etadi, kod faqat SERVER KONSOLIGA yoziladi (HTTP javobida hech qachon qaytarilmaydi). `register`, `login` va `resend-otp` — uchalasi ham shu yagona yo'ldan o'tadi. **HOZIR bu bayroq `backend/.env` da YOQILGAN** (Brevo ishlamagani uchun, 2026-08-04) — Brevo tuzatilgach O'CHIRILISHI kerak; unutib qo'yilmasligi uchun `server.js` har ishga tushishda katta ogohlantirish chiqaradi. **Hostingda bayroq ATAYIN ishlamaydi:** Render deploy logida u yoqilgani aniqlangach (2026-08-04) `trySendOtp` ga `!process.env.RENDER` sharti qo'shildi — u yerda konsol Render loglari bo'lgani uchun foydalanuvchi kodni ko'ra olmasdi va "kod yuborildi" degan xabar bilan boshi berk ko'chaga kirardi, ustiga bir martalik kodlar loglarga yozilib qolardi. Production'da har doim aniq xato qaytadi.
  - **Moderatsiya banlari tozalandi (2026-08-04):** `BannedEmail` da 2 ta yozuv bor edi (`toxirovi82@gmail.com`, `asatoxr@gmail.com` — ikkalasi ham 2026-08-04 da "moderation" sababi bilan) va ular egalari qayta ro'yxatdan o'ta olmasdi. Foydalanuvchi so'roviga ko'ra barcha banlar o'chirildi (`BannedEmail` bo'shatildi, `isBlocked=false`, `violationCount=0`). DIQQAT: moderatsiya tizimi o'chirilmagan — 3 ta yangi qoidabuzarlikda hisob yana bloklanadi (`services/moderation.js`, `MAX_VIOLATIONS_BEFORE_BLOCK`).
  - **Email tashxisi qo'shildi (2026-08-04):** `emailService.checkEmailHealth()` — hech qanday email yubormasdan Brevo akkaunti bilan aloqani tekshiradi va aniq sababni ajratadi (API kalit yo'q / kalit noto'g'ri / jo'natuvchi tasdiqlanmagan / IP cheklovi), qo'shimcha ravishda SERVERNING TASHQI IP manzilini qaytaradi (Brevo ruxsat ro'yxatiga qo'shish uchun kerak). Ikki joyda ishlatiladi: (1) `server.js` har ishga tushganda natijani logga yozadi — Render logidan sababni darrov ko'rish mumkin; (2) `GET /api/admin/email-health` (adminGuard) — so'rov bo'yicha tekshirish. **DIQQAT:** Render tashqi IP manzili o'zgarishi mumkin, shuning uchun ishonchli yechim — Brevo'da IP cheklovini BUTUNLAY O'CHIRISH (bitta IP qo'shish vaqtincha yordam beradi, keyin yana buziladi). **2026-08-04 da aynan shu holat kuzatildi:** Render IP'si ro'yxatga qo'shilgach ishga tushish tekshiruvi "ISHLAYAPTI" deb yozdi, lekin haqiqiy xat yuborish baribir 502 bilan tugadi — Render bir nechta chiquvchi IP ishlatgani uchun bir so'rov ruxsat etilgan IP'dan, boshqasi ruxsatsizidan ketadi. Shundan keyin `checkEmailHealth()` kengaytirildi: endi `GET /v3/senders` orqali `BREVO_SENDER_EMAIL` TASDIQLANGANLIGI ham tekshiriladi (akkaunt ochiq bo'lsa-yu jo'natuvchi tasdiqlanmagan bo'lsa, xat baribir ketmaydi — bu ikkinchi eng ko'p uchraydigan sabab) va ishga tushish logida server IP ko'rsatiladi.
  - **Jo'natuvchi manzil endi AVTOMATIK aniqlanadi (2026-08-04):** avval kodda ikkita manzil qattiq yozilgan edi — `BREVO_SENDER_EMAIL` va zaxira sifatida `acd052001@smtp-brevo.com` (loyiha birinchi sozlangandagi Brevo akkauntiniki). Agar API kaliti BOSHQA akkauntga tegishli bo'lsa yoki sozlangan manzil tasdiqlanmagan bo'lsa — ikkala urinish ham rad etilardi va xat hech qachon ketmasdi. Endi `resolveSender()` akkauntning O'ZIDAN (`GET /v3/senders`) tasdiqlangan manzillar ro'yxatini oladi va ishlaydigan manzilni tanlaydi (har bir Brevo akkauntida kamida bitta avtomatik tasdiqlangan `@smtp-brevo.com` manzil bo'ladi), natija 10 daqiqaga keshlanadi; yuborish 400/401/403 bilan tugasa kesh yangilanib bir marta qayta urinib ko'riladi. Yuborish xatosi endi loglarda ajratilgan blok bilan yoziladi ("BREVO XATOSI" deb qidirish mumkin).
  - **🔴 ASOSIY SABAB TOPILDI (2026-08-04): Brevo akkaunti faollashtirilmagan.** Render logi aniq javob berdi: `403 — "Unable to send email. Your SMTP account is not yet activated. Please contact us at contact@brevo.com to request activation"`. Brevo yangi akkauntlarda transactional email yuborishni QO'LDA faollashtiradi — bu holatda IP, jo'natuvchi yoki kalit sozlamalari umuman yordam bermaydi va KOD orqali hal qilib bo'lmaydi. Shu sababli **provayderlar zanjiri** qo'shildi (`sendEmail()`): `SENDGRID_API_KEY` qo'yilgan bo'lsa avval SendGrid, keyin Brevo urinadi; biri ishlamasa ikkinchisiga o'tadi, ikkalasi ham ishlamasa oxirgi xato `configIssue` belgisi bilan qaytadi. SendGrid tanlangani: HTTPS API orqali ishlaydi (Render bepul tarifi SMTP portlarini bloklaydi), domen shart emas — bitta jo'natuvchini tasdiqlash kifoya, bepul tarifda kuniga 100 xat. Kalit qo'yilmasa hech narsa o'zgarmaydi — tizim avvalgidek faqat Brevo bilan ishlaydi. `checkEmailHealth()` endi ikkala provayderni ham biladi va "faollashtirilmagan" holatini alohida xabar bilan ajratadi.
  - **`ALLOW_UNVERIFIED_SIGNUP=true` (vaqtinchalik chora, 2026-08-04):** email provayderi TAMOMAN ishlamay qolganda (hozirgi holat — Brevo akkaunti faollashtirilmagan) hech kim ro'yxatdan o'ta olmay qoladi; bu email tasdiqlanmasligidan ko'ra jiddiyroq muammo. Kalit yoqilganda `trySendOtp()` provayder muammosini (`configIssue`) aniqlasa, hisob saqlanadi va foydalanuvchi email tasdiqlanmasdan kiritiladi (`register` va `login`ning tasdiqlanmagan tarmog'i). Foydalanuvchi noto'g'ri email yozgan holatda (configIssue EMAS) baribir xato qaytadi. Standart holat — O'CHIQ, ya'ni kalit qo'yilmasa hech narsa o'zgarmaydi. Server ishga tushganda katta ogohlantirish chiqadi; email tiklangach o'zgaruvchini o'chirish kerak.
  - **⚠️ SHART: Brevo'da IP cheklovi o'chirilishi/ruxsat berilishi kerak.** Test paytida Brevo har bir email so'roviga `401 "unrecognised IP address"` qaytardi — ya'ni akkauntda "Authorised IPs" cheklovi yoqilgan. Endi ro'yxatdan o'tish emailga bog'liq bo'lgani uchun, bu tuzatilmasa HECH KIM ro'yxatdan o'ta olmaydi. Yechim: https://app.brevo.com/security/authorised_ips — cheklovni o'chirish yoki Render server IP'siga ruxsat berish.
  - **SEO asosi qo'yildi:** `index.html` to'liq qayta yozildi (`lang="uz"`, tavsif, kalit so'zlar, canonical, Open Graph/Twitter, `theme-color`, schema.org Organization + WebSite + WebApplication JSON-LD), `frontend/public/robots.txt` va `sitemap.xml` qo'shildi, yangi `frontend/src/components/Seo.jsx` (tashqi kutubxonasiz — `react-helmet` qo'shilmadi) sahifaga xos `<title>`/`<meta>`/canonical/`<html lang>` ni boshqaradi. Ulangan sahifalar: Home (+ FAQ uchun `FAQPage` JSON-LD), Login/Register (`noindex`), Lessons/Tests/Library (`noindex` — login talab qiladi), Legal sahifalari. Tarjimalarga `seo_*` kalitlari uch tilda qo'shildi. Domen `VITE_SITE_URL` orqali o'zgartiriladi (standart: `https://huquq-ai-rose.vercel.app`).

- **⚠️ Provayder hisoblarida balans/kvota muammolari (2026-08-03 aniqlangan):**
  - **ANTHROPIC_API_KEY — kredit balansi yo'q.** Claude (Pro/Premium tarif) hozircha ISHLAMAYDI, so'rov 400 xato bilan qaytadi va tizim sezdirmasdan Groq'ga (bepul daraja) tushib ketadi — ya'ni **Pro sotib olganlar hozircha to'lagan narsasini olmayapti**. https://console.anthropic.com/settings/billing orqali balans to'ldirish shart.
  - **OPENAI_API_KEY — kvota tugagan** (429 xato). Shu sabab RAG embeddinglari uchun OpenAI o'rniga **Mistral** (`MISTRAL_API_KEY`, ishlayapti) ishlatildi.
  - **Gemini `gemini-2.0-flash` — bepul kvotasi 0 (butunlay tugagan).** `.env`dagi `GEMINI_MODEL=gemini-2.5-flash` sozlamasi kodda ISHLATILMAGAN edi (hardcode qilingan eski model) — bu aynan "rasm tahlili ishlamayapti" muammosining bir sababi edi, tuzatildi (pastga qarang).

- **Pricing bo'limi 4 ta tarifga qaytarildi (2026-08-03, beshinchi bosqich):** avval "Bepul" karta atayin olib tashlangan edi (faqat Basic/Pro/Premium ko'rsatilardi), lekin foydalanuvchi keyinroq aniq 4 ta tarif (Bepul + 3 pullik) uchun professional dizayn so'radi — bu yangi, aniqroq ko'rsatma sifatida qabul qilindi va eskisi bekor qilindi. `PricingSection.jsx`: `TIER_ORDER` ga "free" qaytarildi, har bir tarif uchun ikona (User/Zap/Star/Gem, navy-gradient badge) va aniq feature ro'yxati qo'shildi (`translations.js`dagi `pricing_features_{free,basic,pro,premium}` massivlari, uch tilda). Grid endi 4 ustunli (desktop) → 2×2 (planshet, ≤1180px) → 1 ustun (mobil). Bepul karta boshqalardan vizual jihatdan pastroq (neytral kulrang fon, oltin urg'usiz) — qiymat pullik tariflarda ekanini ta'kidlash uchun. Bepul kartaning CTA holati 3 xil: mehmon → "Ro'yxatdan o'tish" tugmasi (/register), ro'yxatdan o'tgan+bepul foydalanuvchi → "Joriy tarifingiz" (yashil), ro'yxatdan o'tgan+pullik tarifdagi foydalanuvchi → info matn ("pullik tarifni bekor qilsangiz shu tarifga tushasiz"). **Diqqat:** ko'rsatilgan ba'zi imkoniyatlar (reklama tizimi, AI suhbat tarixi muddati — 7/30/cheksiz kun, AI xatolar tahlili, shaxsiylashtirilgan tavsiyalar, ustuvor/tezkor javob) hozircha faqat marketing matni — backend'da bunday cheklov/funksiya sifatida REAL amalga oshirilmagan (backend faqat `dailyLimit` va `useClaude`ni real qo'llaydi). Agar bular haqiqatda ishlashi kerak bo'lsa, alohida backend ishi talab qilinadi.

- **RAG'dagi modda raqamlash xatosi tuzatildi (2026-08-03, uchinchi bosqich):** lex.uz "qo'shimcha modda"larni superscript bilan yozadi — masalan `276<sup>13</sup>-modda` (= "276-13-modda"). `scripts/ingestLaws.js`dagi eski `parseArticles()` bu `<sup>` tegini chiziqchasiz matnga qo'shib yuborardi, natijada haqiqiy "276-13" o'rniga **"27613"** kabi butunlay noto'g'ri modda raqami saqlanardi (6 ta kodeksda jami ~140 ta moddaga tegishli, eng ko'pi JK'da — 102 ta). Foydalanuvchi buni "Sanitariyaga oid qonunchilikni... buzish — 257-1-modda emas, 27613 deb chiqyapti" deb xabar qildi (haqiqiy joriy raqam 257-1 EMAS, balki 276-13 ekani lex.uz jonli manbasidan tasdiqlandi — 257-1 eski/bekor qilingan tahrirga tegishli bo'lishi mumkin). Tuzatildi: yangi `extractText()` funksiyasi `<sup>` matnidan oldin "-" qo'yadi. Shuningdek `ingestLaw()`ga tozalash logikasi qo'shildi — manba sahifasida endi mavjud bo'lmagan (yoki eski xato raqam bilan saqlangan) yozuvlarni avtomatik o'chiradi. Barcha 6 ta qonun qayta ingestion qilindi, xato **0**ga tushdi (tekshirilgan: `/^\d{4,}$/` naqshli modda raqami qolmagan).

- **PDF import: "bo'lim matni bo'sh bo'lmasin" xatosi tuzatildi (2026-08-03, to'rtinchi bosqich):** agar PDF'da ikkita sarlavha ketma-ket kelsa (masalan "I BOB. ..." darhol "1-modda"dan oldin, orasida matn bo'lmasa), birinchi sarlavha uchun MATNSIZ ("bo'sh") bo'lim yaratilib qolardi — bu bo'lim saqlashda backend validatsiyasidan o'tolmay, admin PDF yuklab hamma joyni to'ldirsa ham dars saqlanmay qolishiga sabab bo'lardi. Tuzatildi: `services/documentImport/parsers/pdf/index.js`da endi matnsiz bo'lim topilsa, uning sarlavhasi TASHLAB YUBORILMAYDI — keyingi (haqiqiy matnli) bo'limning sarlavhasi oldiga qo'shib qo'yiladi (masalan "I BOB. Kirish qoidalari — 1. Asosiy tushunchalar"), shunda bob nomi yo'qolmaydi va bo'sh bo'lim umuman saqlanmaydi. Ikkita sun'iy test PDF bilan (oddiy va "ketma-ket sarlavha" holatlari) tekshirildi — regressiya yo'q.

- **Xatolik auditi + Header/Admin CRUD tuzatishlari (2026-08-03, ikkinchi bosqich):**
  - **Rate limit bug (asosiy sabab, "hech nima ishlamayapti" muammosi):** `server.js`dagi umumiy `/api/` rate limit 100 edi — bitta sahifa yuklanishida ~6 so'rov ketadi, ya'ni ~16 marta yangilashdan keyin butun sayt 429 bilan ishlamay qolardi. `/api/auth/` limiti (20) ham xuddi shu muammoga ega edi (`/auth/me` har 60s chaqiriladi). Tuzatildi: localhost so'rovlari (`isLocalRequest()`) limitdan ozod, umumiy limit 600ga, auth limiti 150ga oshirildi.
  - **`formatDate` haqiqiy xato (ChatDrawer.jsx):** `toLocaleDateString`ga locale sifatida bayroq-emoji (`🇷🇺`/`🇺🇿`/`🇬🇧`) berilgan edi — bu **RangeError** tashlaydi (haqiqiy BCP-47 kod kerak, masalan "ru-RU"). Suhbatlar tarixini ochganda saytni butunlay buzardi. Tuzatildi.
  - **Header qayta qurildi:** "AI Yordamchi" tugmasi header'dan OLIB TASHLANDI (foydalanuvchi buni qaytarib berishni so'radi — chat baribir suzuvchi tugma orqali ochiladi). Endi: **mehmon** uchun menyu o'zgarmadi (Bosh sahifa/Biz haqimizda/Xizmatlar/Narxlar/Maqolalar/Aloqa bir qatorda). **Login qilgan** uchun: Bosh sahifa | "Biz haqimizda" (hover-dropdown: Biz haqimizda/Xizmatlar/Narxlar/Maqolalar/Aloqa) | Darslar | Testlar | Qonunlar kutubxonasi | Profil (matn tugma, ProfileModal'ni ochadi). O'ngdagi alohida avatar tugmasi olib tashlandi — profil endi faqat menyuda.
  - **YANGI: Admin panelidan Darslar/Testlar qo'shish.** `admin/` ilovasiga (haqiqiy ishlatiladigani, `frontend/src/pages/AdminPanel.jsx` EMAS) "Darslar" va "Testlar" bo'limlari qo'shildi (`admin/src/pages/Lessons.jsx`, `Tests.jsx` — to'liq CRUD: yaratish/tahrirlash/o'chirish/nashr-yashirish, video havolasi, dinamik bo'lim/savol-variant qo'shish). Backend: `models/index.js`ga `Lesson`/`Quiz` sxemalari, `routes/lessons.js` + `routes/tests.js` (foydalanuvchilar uchun GET, `userGuard`), `routes/adminContent.js` (to'liq CRUD, `adminGuard`), barchasi `server.js`da ro'yxatdan o'tkazilgan (`/api/lessons`, `/api/tests`, `/api/admin/content`). Admin qo'shgan/tahrirlagan kontent **darhol** barcha ro'yxatdan o'tgan foydalanuvchilarga ko'rinadi (`published:true` bo'lsa). Frontend `pages/Lessons.jsx`/`Tests.jsx` endi statik `data/lessons.js`/`data/tests.js` fayllarini EMAS, API'ni o'qiydi. **MUHIM:** `frontend/src/data/lessons.js` va `tests.js` fayllari o'chirilmagan — ular endi faqat `backend/scripts/seedContent.js` uchun bir martalik seed manba (server birinchi marta ishga tushganda, kolleksiya bo'sh bo'lsa, shu fayllardagi uz-tildagi 6 dars/4 testni bazaga ko'chiradi — shunda eski statik kontent yo'qolmaydi). Video: YouTube havolasi avtomatik iframe embedga aylantiriladi (`toYoutubeEmbed()`), boshqa manzillar `<video>` tegi bilan ko'rsatiladi.

- **Mehmon (guest) AI + yangi bo'limlar (2026-08-03):**
  - **Mehmon AI:** ro'yxatdan o'tmagan foydalanuvchi ham AI'dan foydalana oladi — `webLimitGuard` (`middleware/usageLimit.js`) endi login qilmaganlarni 401 bilan bloklamaydi, balki IP bo'yicha `GUEST_DAILY_LIMIT` (`.env`, hozir **2**) ta bepul savol beradi. Limit tugagach javob `429 + registerRequired:true` qaytaradi va `ChatDrawer.jsx`dagi `RegisterPrompt` oynasi chiqadi. `POST /api/chat` va `GET /api/chat/usage` endi `optionalUserGuard` bilan ishlaydi; mehmon uchun `getGuestUsageStats(ip)` qaytariladi. `Home.jsx`dagi `handleAiClick` endi login sahifasiga yo'naltirmaydi, to'g'ridan-to'g'ri chatni ochadi.
  - **Header:** navbar'da "AI Yordamchi" tugmasi (`navAiBtn`) — havola emas, `openChat()` chaqiradi; mobil menyuda ham bor (`mobileAiLink`).
  - **Yangi bo'limlar (faqat login qilganlar uchun, `RequireAuth` — aks holda `/register`ga yo'naltiriladi):**
    - `/library` — **Qonunlar kutubxonasi** (`pages/Library.jsx` + `routes/library.js`). Yangi kontent yozilmadi: mavjud **LegalChunk** RAG bazasidan foydalanadi (6 kodeks, ~1730 modda, lex.uz'dan avtomatik yangilanadi). Qidiruvda o'zbek apostrof variantlari (`'`, `ʼ`, `ʻ`, `'`) bir-biriga moslashtiriladi — aks holda "ta'til" 0 natija berardi.
    - `/lessons` — **Darslar** (`pages/Lessons.jsx`, kontent: `src/data/lessons.js`, 6 mavzu × 3 bo'lim, uz/ru/en).
    - `/tests` — **Testlar** (`pages/Tests.jsx`, kontent: `src/data/tests.js`, 4 test × 5 savol, izohli, uz/ru/en).
    - Darslar/Testlar kontentida **aniq modda raqamlari ataylab yozilmagan** — qonun tahriri o'zgarsa eskirib qolmasligi uchun; o'rniga har dars oxirida Qonunlar kutubxonasiga havola bor.
  - **Landing:** "Biz haqimizda" bo'limi ichida `PlatformFeatures.jsx` — 4 ta imkoniyat kartasi (AI Yordamchi / Darslar / Testlar / Qonunlar kutubxonasi).

- **Rasm tahlili tuzatildi (2026-08-03) — 2 ta sabab bor edi:**
  1. Rasm yuborilganda ham majburiy 4-bosqichli "huquqiy maslahat" formati (`buildSystemPrompt`) va aloqasiz namunali prompt ishlatilardi — endi alohida `imageAnalysisSystemPrompt()` qo'shildi (`legalAI.js`), rasm bo'lganda shu ishlatiladi.
  2. `gemini-2.0-flash` hardcode qilingan va kvotasi tugagan edi (yuqoriga qarang) — endi `.env`dagi `GEMINI_MODEL` o'zgaruvchisi ishlatiladi.

- **RAG (Retrieval-Augmented Generation) tizimi qurildi (2026-08-03):** AI endi har bir oddiy huquqiy savolda javob berishdan OLDIN qonun matnini MongoDB Atlas Vector Search orqali qidirib, faqat shu tekshirilgan matn asosida modda/qonun nomini keltiradi — o'z "xotirasidan" taxmin qilmaydi. Foydalanuvchiga qidiruv jarayoni hech qachon ko'rsatilmaydi (faqat tayyor javob).
  - **Arxitektura:** yangi xizmat qo'shilmadi — mavjud MongoDB Atlas'da Vector Search yoqilgan ekan (tekshirildi), shu ishlatildi. Embedding uchun **Mistral AI** (`mistral-embed`, 1024 o'lcham) — OpenAI kvotasi tugagani sabab.
  - **Yangi fayllar:** `backend/models/index.js` (`LegalChunk` — modda + embedding), `backend/config/laws.js` (kuzatiladigan 6 ta qonun: MK, OK, JK, FK, YK, IHQ — lex.uz ID'lari 2026-08-03 da web orqali TEKSHIRILGAN, chunki koddagi eski ID'lar 404 qaytarardi — masalan Mehnat kodeksi 2022'da TO'LIQ yangilangan), `backend/services/embeddings.js`, `backend/services/legalRetrieval.js` (`$vectorSearch` chaqiruvi), `backend/scripts/ingestLaws.js` (cheerio bilan lex.uz HTML'ni moddalarga bo'ladi, embedding qilib `LegalChunk`ga yozadi, `contentHash` orqali faqat o'zgargan moddalarni qayta ishlaydi).
  - **Auto-update:** `server.js` ishga tushganda fon rejimida (`await` qilinmaydi) indeksni tekshiradi/yaratadi va qonunlarni yuklaydi, keyin **har 24 soatda** avtomatik qayta tekshiradi (`ingestAllLaws()`) — qonun tahriri o'zgarsa, faqat o'zgargan moddalar qayta embedding qilinadi.
  - **Hozirgi holat:** 6 ta qonun, jami 1734 modda yuklangan va ishlab turibdi (haqiqiy so'rovlar bilan tekshirilgan — Mehnat, Oila, Yer huquqi bo'yicha). Boshqa qonunlar (masalan Fuqarolik kodeksining 2-qismi, Soliq kodeksi, Ma'muriy javobgarlik kodeksi) keyinroq `config/laws.js`ga qo'shilishi mumkin — kod tayyor, faqat ro'yxatga yangi qonun qo'shish kifoya.
  - **Bilingan cheklov:** vektor qidiruv 100% aniq emas — ba'zan eng mos emas, lekin yaqin mavzudagi moddani qaytarishi mumkin (masalan "ishdan haydalganda ish haqi" so'rovida "xodim vafot etganda ish haqi" moddasini qaytargan holat kuzatildi). Bu normal RAG xarakteristikasi, vaqt o'tishi bilan (ko'proq modda, yaxshiroq so'rov) yaxshilanadi.
  - `legalAI.js`dagi `isDoc` (hujjat generatsiyasi rejimi) filtri ham tuzatildi: avval faqat "shartnoma" kabi so'z borligi kifoya edi (masalan "shartnomani qanday bekor qilaman" ham noto'g'ri "yangi shartnoma yoz" rejimiga tushib, RAG batamom ishlamay qolardi) — endi so'z BILAN BIRGA "yoz/tayyorla/tuz/yarat" fe'li ham talab qilinadi.
- **MUHIM — tuzatilgan production-buzuvchi xato (2026-08-02):** `payment.js`dagi `GET /plan` route'i tarif tizimiga o'tkazilganda eski `PRO_PLAN_PRICE_UZS` o'zgaruvchisiga murojaat qilib qolgan edi — bu endpoint chaqirilganda (masalan Profil > Pro tab ochilganda) **butun backend serverni qulatib qo'yardi** (uncaught ReferenceError, try/catch yo'q edi). Tuzatildi + `/plans`, `/plan`, `/history` endpointlariga try/catch qo'shildi (bunday xato endi faqat shu so'rovni 500 bilan qaytaradi, serverni qulatmaydi). **Saboq:** refactor qilinganda faylning HAMMA route'lari qayta test qilinishi kerak, nafaqat bevosita o'zgartirilgani — `node -c` faqat sintaksisni tekshiradi, runtime xatolarni emas.
- **Kontekst saqlash tuzatildi (2026-08-02):** `legalAI.js`dagi `buildHistory()` avval joriy xabar bilan so'zma-so'z mos kelmagan eski xabarlarni filtrlab tashlardi — tabiiy davom savollarida ("qanday hujjat kerak?" kabi) AI oldingi mavzuni yo'qotardi. Endi oxirgi 6 xabar so'zsiz saqlanadi. Haqiqiy AI so'rovi bilan tekshirilib, ketma-ket savollarda kontekst to'g'ri saqlanishi tasdiqlandi.
- **Dizayn muvofiqlashtirildi (2026-08-02):** Home.module.css/Services.module.css'dagi eski bo'limlar (Stats, Categories, Steps, Services, Hero feature kartalari) yangi bo'limlar bilan bir xil "icon-badge" uslubiga o'tkazildi (oldin yalang'och emoji edi), hardcoded ranglar (#666, #f8f9fa, #e5e7eb) CSS o'zgaruvchilariga almashtirildi, "Qanday ishlaydi" bosqichlari orasiga bog'lovchi chiziq qo'shildi.

- **DARSLAR — filtr scrolli + dars ichida 4 tadan mavzu + Oila huquqi 50 mavzuga (2026-08-09):** foydalanuvchi uchta narsani so'radi: filtr scroll bo'lmayapti; darslik ketma-ket o'rgatsin; dars ichida 4 tadan mavzu ko'rsatilib, 50 tagacha davom etsin.
  - **⚠️ FILTR SCROLLI — sabab CSS emas, JS edi.** `.modTabs` da `overflow-x: auto` allaqachon bor edi va u to'g'ri. Haqiqiy sabab `utils/smoothScroll.js` dagi `initSmoothWheel()`: u vertikal scrollli ota-onani topa olmasa HAR QANDAY g'ildirak hodisasiga `e.preventDefault()` qilardi. `findScrollableAncestor` faqat `overflowY` ni bilardi, shuning uchun gorizontal tasmada har doim `null` qaytarardi. Natijada trackpad'dagi YON SURISH butunlay bloklanardi. Ikki tuzatish: (a) gorizontal ustunlikdagi hodisaga (`|deltaX| > |deltaY|`) umuman aralashilmaydi; (b) yangi `findHorizontalScrollable()` — oddiy sichqonchada `deltaX` umuman bo'lmagani uchun vertikal burash tasmani gorizontal suradi (fayl menejerlari va brauzer tab tasmalaridagi kabi). Tasma chetiga yetganda hodisa sahifaga qaytadi — aks holda sahifa qotib qolardi. **8 ta test** (brauzersiz, DOM taqlidi bilan).
  - **DARS ICHIDA QADAMLASH (`TOPICS_PER_STEP = 4`).** Ro'yxat sahifasi TEGILMADI — u avvalgidek 9 ta darslik (3×3 grid). Bo'linish faqat DARS ICHIDA: 50 mavzu → 13 qadam, oxirgisida 2 ta. Nozik jihatlar: (a) mavzu raqami `sec.index` dan olinadi, qadam ichidagi o'rnidan EMAS — aks holda 2-qadamda mavzular yana "1,2,3,4" bo'lib raqamlanardi va "o'qildi" belgisi noto'g'ri bo'limga tushardi; (b) `IntersectionObserver` `topicStep` ga bog'landi — qadam almashganda DOM yangilanadi, kuzatuvchi qayta ulanmasa eski elementlarni kuzatib qolardi; (c) **amaliyot, tugatish testi va xulosa faqat OXIRGI qadamda** — aks holda foydalanuvchi 50 mavzuning 4 tasini o'qib turib darsni "tugallandi" deb belgilay olardi; (d) yon paneldagi mundarija `<a href="#">` dan `<button>` ga o'tkazildi va `jumpToSection()` avval kerakli qadamga o'tadi — mavzu boshqa qadamda bo'lsa DOM'da umuman yo'q va oddiy anchor hech qayerga olib bormasdi; (e) `topicStep` nomi ataylab — `lessonProgress.lessonStep()` bilan to'qnashuvni oldini oladi.
  - **OILA HUQUQI: 4 → 50 MAVZU (2 746 → 57 925 belgi, 21,1×).** Ketma-ketlik foydalanuvchi bergan namunaga aynan mos: nikohgacha (1-8: yosh, monelik, qayd etish) → nikohda (9-20: huquqlar, mulk, shartnoma) → bolalar (21-30) → ajrashish (31-40) → ajrimdan keyin (41-50: bola kim bilan qoladi, ko'rishish, aliment, mulk bo'lish). Har qism o'zidan oldingisiga tayanadi — masalan mulkni bo'lish 50-mavzuda, chunki uni tushunish uchun umumiy/shaxsiy mulk qoidalari (15-18) allaqachon o'qilgan bo'lishi kerak.
  - **Yangi katalog `scripts/lessonContent/oila/`** — 5 ta qism fayli + `index.js`. Bitta faylda ~1700 qator bo'lardi va qolgan 5 ta dars orasida yo'qolib ketardi. `index.js` ikki himoya bilan: mavzular soni 50 emasligi va sarlavha takrorlanishi — ikkalasi ham modul yuklanishida darhol xato beradi (aks holda dars jimgina qisqarib qolardi).
  - **⚠️ HUQUQIY ANIQLIK QOIDASI SAQLANDI.** Foydalanuvchi aniq yoshni so'radi, lekin u MATNGA yozilmadi: `lawRefs` orqali `LegalChunk` dan JONLI keladi (OK 15 "Nikoh yoshi"). Ya'ni yosh sahifada KO'RINADI, lekin lex.uz'dan qonun yangilanganda o'z-o'zidan yangilanadi va dars hech qachon eskirmaydi.
  - **Barcha 120 ta havola BAZADAN TEKSHIRILGAN** — raqam ham, moddaning SARLAVHASI ham mavzuga mos kelishi ko'z bilan tasdiqlandi (faqat "modda mavjudmi" tekshiruvi yetarli emas: mavjud, lekin noto'g'ri moddaga havola dars sahifasida jimgina noto'g'ri iqtibos ko'rsatardi). Yakuniy: `verifyLessonRefs.js` → **1250 havola, 0 xato**. Har 50 mavzuda misol, asosiy fikrlar, ogohlantirish va havola bor (0 ta to'liqsiz).
  - **Qolgan 5 ta dars hali eski holatda** (Mehnat 5, Iste'molchi 4, Meros 4, Yer 3, Jinoiy 3 bo'lim) — ular ham 50 mavzuga xuddi shu andaza bo'yicha kengaytirilishi kerak.

- **BARCHA 6 DARSLIK 50 MAVZUGA YETKAZILDI (2026-08-09):** foydalanuvchi Oila huquqini ko'rib tasdiqlagach qolgan 5 tasini ham xuddi shu ketma-ketlikda so'radi. Endi oltala darslikning har birida **50 ta mavzu**, jami **300 mavzu** va **325 052 belgi** (avval ~14 900 edi).
  - **O'sish:** Mehnat 5 → 50 (16,3×) · Oila 4 → 50 (21,1×) · Iste'molchi 4 → 50 (23,2×) · Meros 4 → 50 (21,5×) · Yer 3 → 50 (28,7×) · Jinoiy 3 → 50 (25,2×).
  - **Har bir darslik AMALIY YO'L bo'yicha qurilgan** — nazariy tartibda emas. Mehnat: ishga kirishdan oldin → qabul → ish vaqti → ish haqi → intizom → bo'shash. Iste'molchi: kim ekaningni bilish → xarid → sifat muammosi → talab → himoya. Meros: asoslar → vasiyatnoma → qonun bo'yicha → qabul qilish → qarzlar. Yer: yer asoslari → olish → servitut/tugash → uy-joy → bitim. Jinoiy: asoslar → jazo → jinoyat turlari → HUQUQLAR.
  - **⚠️ Ataylab qilingan tartib qarorlari:** (a) Meros darsligida VASIYATNOMA qonun bo'yicha vorislikdan OLDIN — qonun bo'yicha vorislik faqat vasiyatnoma bo'lmaganda qo'llaniladi, ya'ni vasiyatnoma birlamchi asos; (b) Yer darsligida UY-JOY yerdan KEYIN — uy har doim yer ustida va uy sotilganda yerga bo'lgan huquq ham o'tadi; (c) Jinoiy darslikda 4-qism eng katta (16 mavzu) va u butunlay AMALIY HUQUQLARGA bag'ishlangan — foydalanuvchi uchun eng qimmatli qism nazariya emas, "endi nima qilaman" degan savolga javob; (d) Mehnatda ishdan bo'shash oxirida — uni tushunish uchun shartnoma, ish haqi va intizom allaqachon o'zlashtirilgan bo'lishi kerak.
  - **⚠️ JINOIY DARSLIK YO'NALISHI:** u jinoyat sodir etishni emas, HUQUQNI TUSHUNISHNI o'rgatadi. Jinoyat tarkiblari JABRLANUVCHI nuqtai nazaridan yozilgan (qanday himoyalanish, qanday dalil to'plash), 4-qism esa aybsizlik prezumpsiyasi, himoyachi huquqi, so'roqda o'zini tutish, qiynoq taqiqi, shikoyat muddatlari kabi amaliy mavzulardan iborat.
  - **⚠️ IKKI-UCH MANBALI DARSLIKLAR:** Iste'molchi — IHQ (atigi 33 modda) + FK2 (oldi-sotdi, pudrat, xizmat); Yer — YK + UJK + FK2 (ko'chmas mulkni sotish). Faqat bitta manbaga tayanish darslarni yarim qoldirardi: masalan IHQ da kafolat muddatini hisoblash yo'q, YK da uy sotish qoidalari yo'q.
  - **Yangi katalog `scripts/lessonContent/`** — har darslik alohida papkada, ichida 4-6 qism fayli + `index.js`. Jami **36 fayl**. Yagona `assemble.js` yordamchisi uch tekshiruvni bajaradi: mavzular soni 50 emasligi, sarlavha takrorlanishi, to'liqsiz mavzu (matn/asosiy fikrlar/havola yo'q). Uchalasi ham modul yuklanishida DARHOL xato beradi — bu xatolar "jim" turadi (dars shunchaki qisqarib qoladi va sezilmaydi).
  - **⚠️ HUQUQIY ANIQLIK QOIDASI HAMMASIDA SAQLANDI:** matnda aniq muddat, foiz, summa yoki jazo yillari YOZILMAGAN. Ular `lawRefs` orqali `LegalChunk` dan JONLI keladi. Bu ayniqsa jinoyat huquqida muhim — jazo miqdorlari tez-tez o'zgaradi va eskirgan raqam og'ir chalg'itishga olib kelardi.
  - **Havolalar bazadan tekshirilgan:** Mehnat 178 · Oila 120 · Iste'molchi 142 · Meros 104 · Yer 151 · Jinoiy 170 = **865 yangi havola, 0 xato**. Faqat "modda mavjudmi" emas, moddaning SARLAVHASI mavzuga mos kelishi ham namunalar bo'yicha ko'z bilan tasdiqlandi. Yakuniy: `verifyLessonRefs.js` → **1971 havola, 0 xato**.
  - **Har 50 mavzuda misol, asosiy fikrlar, ogohlantirish va qonun havolasi bor** (0 ta to'liqsiz). Har biri `TOPICS_PER_STEP = 4` bo'yicha **13 qadam**ga bo'linadi.
  - **Tegilmagan:** `scripts/lessons/` (124 ta qo'shimcha modul darsi), frontend mantig'i, AI, tarjima tizimi, admin paneli. Frontend build va backend sintaksis toza.

- **124 TOR DARSNI KENGAYTIRISH — boshlandi (2026-08-09):** foydalanuvchi so'rovi: "barcha darslikga maximum mavzular qo'sh, mavzu aynan o'sha darslikka oid bo'lsin, boshqa darslardan yig'ib olib kelma, qancha mavzu bo'lsa shuncha qo'sh".
  - **⚠️ NIMA UCHUN "HAMMASIGA 50 TA" QILINMADI:** o'lchandi — 124 ta tor darsga 50 tadan mavzu = 5 918 yangi mavzu ≈ 6,4 mln belgi. Lekin asosiy to'siq hajm emas: bu darslar allaqachon 50-mavzuli KENG darsliklarning ICHKI mavzulari. Misol — "Nikoh shartnomasi" bo'yicha butun Oila kodeksida atigi **6 ta modda** bor; oila modulidagi 11 ta darsning har birida 50 mavzu bo'lsa **har bir modda 7,5 marta** takrorlanardi. Bu darsni yaxshilamaydi, suvlantiradi. Foydalanuvchi buni tushunib, "qancha mavzu chiqsa shuncha" degan aniq ko'rsatma berdi.
  - **QOIDA:** har bir dars uchun kodeksdan AYNAN O'SHA mavzuga oid moddalar ajratiladi va har modda (yoki mazmunan bog'liq guruh) bitta mavzu bo'ladi. Sun'iy raqam qo'yilmaydi — shuning uchun mavzular soni darsdan darsga farq qiladi (10, 14, 15…).
  - **⚠️ MODDA CHEGARALARI ATAYLAB AJRATILGAN** — bir modda ikki darsda takrorlanmasligi uchun. Oila moduli: 01-dars OK 13–18/51–53/57–59/212–217 · 02-dars OK 37–48/218–222 · 03-dars OK 23–28/34–36/12. Nikoh shartnomasi (OK 29–33) 3-darsga QO'SHILMADI — u o'z darsida qoladi.
  - **Yangi tuzilma `scripts/lessons/04-oila/`** — eski `04-oila.js` → `00-qolganlar.js` ga ko'chirildi, kengaytirilgan darslar alohida fayllarda, `index.js` ikkalasini birlashtiradi. **Bu yondashuv XAVFSIZ:** hali kengaytirilmagan darslar asl holida ishlashda davom etadi, ya'ni ish yarim qolganda ham sayt buzilmaydi.
  - **⚠️ `REWRITTEN` ro'yxati muhim:** dars kengaytirilganda uning kaliti shu ro'yxatga qo'shilishi shart, aks holda u ikki marta (eski va yangi holatida) ro'yxatga tushardi va seed skripti ularni bir-birining ustiga yozardi. `index.js` da takroriy kalit tekshiruvi bor — unutilsa darhol xato beradi.
  - **BAJARILGANI (6/124, oila moduli):** Nikoh tuzish 3 → **14** · Ajratish 2 → **15** · Er-xotin mulki 2 → **10** · Nikoh shartnomasi 2 → **7** · Bolalar alimenti 2 → **13** · Oila ta'minoti 2 → **14**. Oila modulida jami 13 → **81 mavzu**. Havolalar bazadan tekshirilgan (`verifyLessonRefs.js --library` → **1149 havola, 0 xato**), seed 124/124 muvaffaqiyatli.
  - **⚠️ "Nikoh shartnomasi" 7 mavzu — bu QOIDANING ISBOTI:** kodeksda bu mavzuga atigi 6 ta modda (OK 29–33, 35) tegishli, shuning uchun 7 mavzu chiqdi. Boshqa darsdan modda olib kelib 15 taga yetkazish mumkin edi, lekin bu aynan foydalanuvchi taqiqlagan narsa. Mavzular soni darsdan darsga farq qilishi — xato emas, qoidaning to'g'ri ishlayotgani.
  - **MEHNAT MODULI ham boshlandi (2026-08-09):** `scripts/lessons/06-mehnat/` — xuddi oiladagi tuzilma. "Mehnat shartnomasi" 3 → **11 mavzu** (MK 103–117). ⚠️ Mehnat modulida 16 ta darsning MODDA CHEGARALARI `index.js` da OLDINDAN belgilab qo'yildi (01: 103–117 · 02: 118–132 · 03: 133–154 · 04: 155–169 · 05: 170–174 · 06: 181–200 · 07: 201–212 · 08: 213–235 · 09: 236–242 · 10: 243–257 · 11: 258–294 · 12: 295–315 · 13: 316–350 · 14: 351–366 · 15: 534–560 · 16: 388–431). Sabab: kengaytirish bir necha sessiyaga cho'zilgani uchun keyingi safar bir modda ikki darsga tushib qolishi oson — chegara oldindan yozilsa bu xato umuman yuzaga kelmaydi. Xuddi shunday chegaralar qolgan 11 ta modul uchun ham kengaytirish boshlanishida belgilanishi kerak.
  - **✅ MEHNAT MODULI TO'LIQ TUGALLANDI (16/16 dars, 171 mavzu, 286 havola).** Bu birinchi to'liq tugallangan modul. Barcha 16 ta darsning modda chegaralari `06-mehnat/index.js` da yozilgan va ular bir-biri bilan kesishmaydi.
  - **✅ OILA MODULI HAM TO'LIQ TUGALLANDI (10/10 dars, 120 mavzu).** Ikkinchi yakunlangan modul. Endi `04-oila/00-qolganlar.js` faylida kengaytirilmagan dars qolmadi.
  - **✅ MEROS MODULI HAM TUGALLANDI (6/6 dars, 49 mavzu).** Uchinchi yakunlangan modul.
  - **UMUMIY HOLAT: 32/124 dars kengaytirilgan**, 124 darsda jami **553 mavzu** (avval 282). Havolalar **1321 ta, 0 xato**.
    - Oila 10/10 — 120 mavzu ✅
    - Mehnat 16/16 — 171 mavzu ✅
    - Meros 6/6 — 49 mavzu ✅
  - **⚠️ TEKSHIRISHDA E'TIBOR:** "kengaytirilgan" darsni `sections.length >= 5` bilan sanash NOTO'G'RI natija beradi — ayrim asl darslarda (masalan `konstitutsiya-huquqlar`) allaqachon 5 ta bo'lim bor edi. Aniq hisob uchun modul `index.js` laridagi `REWRITTEN` ro'yxatiga qarang.
  - **MEROS MODULI boshlandi** (`05-meros/`): qonun bo'yicha meros 3 → 14 (FK2 1112–1119, 1134–1140) · vasiyatnoma 3 → 13 (FK2 1120–1133). ⚠️ Vorislik ASOSLARI (1112–1119) 1-darsga kiritildi — ular alohida dars bo'lishga yetmaydi, lekin navbatlar mavzusini tushunish uchun zarur.
  - **⚠️ MAVZULAR SONI TABIIY RAVISHDA FARQ QILADI:** "Ijtimoiy ta'tillar" 7 mavzu (MK 236–242 — atigi 7 modda), "Bo'shatish tartibi" 6 mavzu (MK 170–174 — 5 modda), "Ishga qabul" esa 14 mavzu (MK 118–132 — 15 modda). Bu qoidaning to'g'ri ishlayotgani: mavzu soni qonun ko'targancha, sun'iy raqamga tenglashtirilmaydi.
  - **QOLGANI: 92 ta dars.** Har biri uchun ish tartibi: (1) darsning mavzusiga oid modda oralig'ini aniqlash, (2) boshqa darslar bilan kesishmasligini tekshirish, (3) har moddaga mavzu yozish, (4) `REWRITTEN` ga qo'shish, (5) havolalarni tekshirish. Modul bo'yicha qolgan (mehnat tugallangan): contracts 14 · process 11 · civil 10 · criminal 10 · housing 9 · tax 9 · administrative 9 · constitution 8 · special 6 · consumer 6.

- **✅ BARCHA 124 DARS TUGALLANDI — 13/13 MODUL (2026-08-11):** yuqoridagi ish oxiriga yetkazildi. Endi kutubxonadagi HAR BIR dars kengaytirilgan.
  - **Yakuniy holat: 124 dars, 1069 mavzu, 794 643 belgi, 3278 modda havolasi (0 xato).** Boshlanish nuqtasi 282 mavzu edi — **3,8 barobar o'sish**.
  - **Modullar bo'yicha:** konstitutsiya 8/8 (99) · fuqarolik 10/10 (113) · shartnomalar 14/14 (130) · oila 10/10 (120) · meros 6/6 (49) · mehnat 16/16 (171) · uy-joy 9/9 (61) · iste'molchi 6/6 (52) · soliq 9/9 (49) · ma'muriy 9/9 (49) · jinoyat 10/10 (49) · sud 11/11 (54) · maxsus 6/6 (73).
  - **Bu sessiyada tugallangan 7 modul (92 dars):** konstitutsiya (1 qolgani), fuqarolik (10), shartnomalar (14), uy-joy (9), soliq (9), ma'muriy (9), jinoyat (10), sud (11) — hamda ular uchun 7 ta yangi katalog (`02-fuqarolik/`, `03-shartnomalar/`, `07-uy-joy/`, `09-soliq/`, `10-mamuriy/`, `11-jinoyat/`, `12-sud/`), jami **151 ta fayl**.
  - **⚠️ TOPILGAN VA TUZATILGAN BUZILISH:** `01-konstitutsiya/` katalogida `index.js` YO'Q edi (oldingi sessiya yarim qolgan) — natijada `require("./01-konstitutsiya")` yiqilib, BUTUN dars kutubxonasi yuklanmasdi. Ya'ni `seedLessonLibrary.js` va darslarga oid har qanday skript ishlamas holatda edi. Index yozildi va yetishmayotgan 8-dars (`konstitutsiya-fuqarolik`, KONS 22–24) qo'shildi.
  - **MODDA CHEGARALARI har bir modul `index.js` da yozildi** — kelajakda dars qo'shilganda takrorlanish yuzaga kelmasligi uchun. Modullararo kesishishlar ham izohda ochiq qayd etilgan (masalan FK2 479–496 va 600–616 SHARTNOMALAR emas, UY-JOY modulida; FK2 985–1030 FUQAROLIK modulida; FK2 709–731 MAXSUS modulda).
  - **⚠️ ATAYLAB OLINMAGAN QISMLAR (izohda qayd etilgan):** SK 176–367 (transfert narx, QQS, aksiz, foyda solig'i) · JK 146–163, 181–204, 216–404 (davlatga qarshi, harbiy, iqtisodiy jinoyatlar) · MJK ning soha tarkiblari (soliq, bojxona, mehnat — ular o'z modullarida) · JPK ning tergov va sud muhokamasi qismlari. Sabab: modullar ODDIY FOYDALANUVCHIGA mo'ljallangan; bu bo'limlar professional tayyorgarlik talab qiladi va kerak bo'lsa ALOHIDA darslar sifatida qo'shilishi kerak — mavjudlarini suvlantirmasdan.
  - **⚠️ JINOYAT MODULI YO'NALISHI:** darslar jinoyat sodir etishni emas, HUQUQNI TUSHUNISHNI o'rgatadi — tarkiblar jabrlanuvchi va himoya nuqtai nazaridan bayon etilgan (qanday dalil qayd etish, qachon advokat talab qilish, qanday shikoyat berish).
  - **Har bir mavzuda:** matn + hayotdan misol + 4 ta asosiy fikr + ogohlantirish + kamida bitta JONLI qonun havolasi. Tekshirildi: to'liqsiz mavzu **0**, havolasiz mavzu **0**.
  - **HUQUQIY ANIQLIK QOIDASI SAQLANDI:** matnda aniq muddat, foiz, summa yoki jazo yillari yozilmagan — ular `lawRefs` orqali `LegalChunk` dan jonli keladi va lex.uz yangilanganda o'z-o'zidan yangilanadi.
  - **Tekshiruv:** 151 fayl sintaktik toza · `verifyLessonRefs.js --library` → 3278/3278 · `seedLessonLibrary.js` → 124/124 yangilandi (0 dublikat) · frontend va admin build toza.
  - **Tegilmagan:** `scripts/lessonContent/` (6 ta keng darslik, 300 mavzu), frontend mantig'i, AI, tarjima tizimi, admin paneli.

- **PROGRESS HISOBGA, LOGIN HIMOYASI QURILMAGA (2026-08-11):** foydalanuvchi ikkita xatoni bildirdi — (1) "bitta Mac'da har xil akkauntga kirsam, barcha akkauntda darslar tugallangan deb chiqyapti"; (2) "bitta Wi-Fi'da parolni xato kiritsam, faqat mening kompyuterim bloklanishi kerak, telefondan kira olishim kerak". Ikkalasi bir xil ildizga ega: **holat NOTO'G'RI narsaga bog'langan edi** — progress qurilmaga, blok esa IP'ga.
  - **⚠️ 1-XATO — progress qurilmaga bog'langan edi.** `localStorage` kalitlari UMUMIY edi: `lessonState`, `lessonXp`, `savedLessons`. Ya'ni kalit brauzerga tegishli, hisobga EMAS. Oqibati: A hisobi bilan darsni tugatgan odam chiqib, B hisobi bilan kirsa — B ham hamma darsni "tugallandi" holatida ko'rardi. **Server tomonida xato YO'Q edi** (`LessonProgress.findOne({ userId })` — har doim to'g'ri ishlagan); butun muammo frontend keshida edi va u serverdan kelgan to'g'ri ma'lumot ustiga begona hisob ma'lumotini BIRLASHTIRIB qo'yardi (`pullProgress` local + remote merge qiladi).
  - **Yechim — yangi `frontend/src/utils/accountScope.js`:** har bir kalitga hisob identifikatori qo'shiladi (`lessonState:68f2…`), mehmon esa alohida `:guest` bo'shlig'ida qoladi. `scopedKey()` har CHAQIRUVDA qayta hisoblanadi (bir marta emas) — foydalanuvchi sahifani yangilamasdan boshqa hisobga kirishi mumkin. Manba — `localStorage.user`, uni `AuthContext` login/logout paytida yozadi va o'chiradi.
  - **⚠️ Eski umumiy kalitlar KO'CHIRILMADI, O'CHIRILDI** (`purgeSharedKeys`). Ko'chirish jozibali ko'rinadi, lekin o'sha ma'lumot QAYSI hisobga tegishli ekani noma'lum — uni joriy hisobga ko'chirish aynan tuzatilayotgan xatoni takrorlardi. Yo'qotish xavfi yo'q: progress allaqachon serverga yuborilgan (`pushLesson`) va `pullProgress()` uni qaytaradi.
  - **Tegilgan fayllar:** `utils/accountScope.js` (yangi), `pages/lessonProgress.js` (`stateKey()`/`xpKey()`), `pages/Lessons.jsx` (`savedKey()`). Server, `progressService.js` va Telegram bot yo'li — TEGILMADI (ular allaqachon to'g'ri edi).
  - **Qamrov tekshirildi:** boshqa hisobga oid kalit qolmadi — `huquq_chat_*` allaqachon `userId` bilan ajratilgan, `lang`/`deviceId` esa ATAYLAB qurilma darajasida (til tanlovi va qurilma identifikatori hisobga bog'lanmasligi kerak).
  - **2-XATO — allaqachon TUZATILGAN edi** (2026-08-07 dagi qurilma bo'yicha brute-force ishi, yuqoriga qarang), lekin foydalanuvchi shikoyati bo'yicha uchidan-uchiga qayta tekshirildi va ishlayotgani tasdiqlandi: Mac'da 5 ta xato → Mac 15 daqiqaga bloklandi, **o'sha Wi-Fi'dagi telefon bemalol kirdi**. Uch qatlam ham ishlaydi (qurilma 5 · hisob 10 · IP 50).
  - **⚠️ CORS tekshirildi:** `X-Device-Id` — safelist'ga kirmaydigan sarlavha, ya'ni u bloklansa BUTUN sayt ishlamay qolardi. `server.js` da `allowedHeaders` ataylab ko'rsatilmagan, shu sabab `cors` paketi brauzer so'ragan sarlavhalarni qaytaradi va sarlavha o'tadi. Preflight allaqachon bor edi (`X-Lang` ham safelist'da emas) — regressiya yo'q.
  - **Sinovlar 25/25** (brauzersiz): hisob ajratish 14/14 (asosiy ssenariy — A tugatadi, B ko'rmaydi, A qaytsa progressi joyida; buzuq JSON'da yiqilmaslik; eski kalitlar tozalanishi) va login himoyasi 11/11 (telefon bloklanmasligi, to'g'ri parol bilan ham blok davom etishi, muvaffaqiyatli login hisoblagichni tozalashi, sarlavhasiz so'rov zaxira barmoq izi bilan bloklanishi, qurilma almashtirgan hujumchini HISOB qatlami to'xtatishi). Ikkala build toza.

- **HERO'LAR — Konstitutsiya rasmi, "bob" ko'rsatkichi va Kutubxona qayta dizayni (2026-08-11):** foydalanuvchi uch narsani so'radi: Konstitutsiya hero rasmi chetga chiqib, ba'zi qismlari ko'rinmayapti · modda/bo'lim yoniga BOB soni ham qo'shilsin · Kutubxona hero'si berilgan namunadagidek bo'lsin. Ish davomida to'rtinchi talab keldi: **to'rtala hero bir xil kenglik va balandlikda bo'lsin.**
  - **(1) Konstitutsiya rasmi kesilardi.** `background-size: cover` + 460px balandlik edi. Rasm 16:9 (1600×900), hero esa 1152×460 (≈2,5:1) — ya'ni balandlikning **~29% i qirqilardi** va aynan CHETLARDAGI bezaklar yo'qolardi: tepadagi bayroq lentasi, pastdagi kitob postamenti, o'ng pastdagi lenta. `contain` ga o'tkazildi — endi rasm to'liq ko'rinadi.
    - **⚠️ `aspect-ratio` sinab ko'rildi va RAD ETILDI.** U rasmni to'liq KENGLIKDA ko'rsatardi (eng chiroyli variant), lekin hero balandligini 460 → ~648px ga ko'tarib, to'rtala sahifadagi bir xil balandlik qoidasini buzardi. Foydalanuvchi buni darhol sezdi va bir xil balandlikni so'radi — shuning uchun `contain` + `--hero-h` qoldi.
    - **Yon tomonlarda qoladigan joy — muammo emas:** o'lchandi, rasm chetidagi fon (#fdfcf8) hero `background-color` i bilan **AYNAN bir xil** (farq 0-2/255), ya'ni chegara ko'zga tashlanmaydi. Kattaroq farqlar (99, 200) — bu kitob va lenta TASVIRINING o'zi, chegara nuqsoni emas.
  - **(2) "Bob" ko'rsatkichi qo'shildi** — endi hero'da: modda · bo'lim · **bob** · lex.uz. Tarjima kaliti (`const_chapters_count`) allaqachon ikkala tilda bor edi.
    - **⚠️ `flat.length` ISHLATILMADI.** Ba'zi bo'limlarning o'z bobi yo'q (OLTINCHI BO'LIM) va ular uchun kodda SUN'IY bob yaratiladi — moddalar bobsiz qolmasligi uchun. Uni sanash haqiqiy bo'lmagan raqam berardi, shuning uchun faqat nomi BOR boblar hisoblanadi (`unnamed` bayrog'i aynan shu uchun mavjud edi).
  - **(3) Kutubxona hero'si namuna bo'yicha qayta qurildi:** IKKI ustun — chapda oltin nishon → sarlavha → oltin ajratgich → tavsif, o'ngda illyustratsiya; ustidan ichki oltin ramka (markazlarida romb), ikkala tepa burchakda nuqta to'ri, pastki chapda boshoq.
    - **⚠️ Matn ustun ICHIDA markazlashtirilgan, chapga tekislanmagan** — namunada "kutubxonasi" so'zi "Qonunlar" dan chaproqda boshlanadi, ya'ni matn markazda.
    - **Illyustratsiya — namunadan qirqib olindi** (`public/library-hero.jpg`, 40 KB). Butun namunani fon sifatida qo'yib bo'lmasdi: unda matn CHIZILGAN va ruscha rejimda o'zbekcha sarlavha qolib ketardi.
    - **⚠️ CHEGARA MUAMMOSI — uch marta o'lchab yechildi.** Qirqilgan rasm foni bir tekis emas (#fef6ed…#f2e1c9), shuning uchun hero foniga qo'yilganda TO'RTBURCHAK dog' bo'lib bilinib turardi. Uch yondashuv sinaldi: (a) alfa kanalli PNG — ishlaydi, lekin **203 KB** (JPEG 40 KB, 5 barobar og'ir); (b) CSS `mask-image` — chetlari to'liq so'nmadi, `mask-composite` bilan aniqroq bo'lardi-yu u brauzerlarda turlicha ishlaydi; (c) **tanlangan yechim** — chetlar RASMNING O'ZIDA krem rangga o'tkazildi + hero foniga rasm ortidan iliq radial nur qo'shildi. Natija ekrandan o'lchandi: chegaradagi sakrash **7 → 4/255** (~1,5%, ko'z ilg'amaydi).
    - **Nur o'zgaruvchi bilan beriladi** (`--glow-x/--glow-y`): desktopda rasm o'ng ustunda (76%), telefonda matn ostida markazda (50%/78%). Qiymat almashmasa telefonda nur bo'sh joyni yoritib, dog' qaytib kelardi.
    - **`LibraryBookArt` va `LibraryBuildingArt` o'chirildi** (`HeroArt.jsx`) — haqiqiy rasm qo'yilgach ular ikki xil uslubdagi kitob tasvirini bir hero'da ko'rsatardi. O'rniga `LibrarySprigArt` (boshoq) qo'shildi: uni namunadan toza qirqib bo'lmasdi — ustidan matn va ramka chizig'i o'tadi.
  - **(4) To'rtala hero endi bir xil.** Tekshirilganda HAQIQIY nomuvofiqlik topildi: `Lessons.module.css .wrap` **1180px** edi, qolgan uchtasi 1200px — ya'ni Darslar hero'si 20px tor chiqardi. 1200px ga tenglashtirildi. Endi to'rttasi ham: kenglik `--hero-max-w` (100% × 1200px konteyner), balandlik `--hero-h` (460px), desktopda bekor qiluvchi qoida yo'q.
  - **⚠️ SKRINSHOT USULIDAGI TUZOQ (yo'l-yo'lakay aniqlandi):** macOS'da Chrome oynasining eng kichik eni **500px** — `--window-size=390` bersangiz ham sahifa 500px da chiziladi, faqat rasm 390px qilib qirqiladi. Shu sabab "mobil versiyada matn chetga chiqib ketyapti" degan SOXTA xulosa chiqdi va yo'q muammoni tuzatishga kirishildi. Haqiqiy o'lcham `iframe` ichida olindi: 390/360/320px — uchalasida ham `body.scrollWidth == clientWidth`, ya'ni gorizontal chiqish YO'Q.
  - **Tekshirildi:** CSS sinflari 268/268 (to'rtala sahifa) · to'rtala hero `--hero-max-w`/`--hero-h` ishlatadi · frontend va admin build toza · Konstitutsiya va Kutubxona hero'lari desktop (1200px) va mobil (390/360/320px) da brauzerda ko'z bilan tekshirildi.

- **HERO'LAR TO'LIQ KENGLIKKA O'TKAZILDI (2026-08-11):** foydalanuvchi: "hero qismlari width 100% kenglikda bo'lsin, ekranni boshidan oxirigacha, rasm ham shu hero moslashib 100% kenglikda bo'lsin, height bir xil bo'lsin barchasida". Avvalgi urinishimda hero'lar 1200px konteyner ICHIDA kartochka bo'lib qolgan edi — shuning uchun "hech nima o'zgarmadi" degan haqli e'tiroz keldi.
  - **To'rtala hero endi ekran chetiga tegadi.** Har birida konteyner olib tashlandi: `Library.jsx` va `Tests.jsx` dan `shared.container` (ular kenglikni 1200px ga cheklardi), `Lessons.jsx` da hero o'rami `.wrap` dan `.heroOuter` ga o'tkazildi, `Constitution.module.css` da `.heroOuter` ning `max-width: 1200px` i olib tashlandi.
  - **⚠️ MATN BARIBIR 1200px CHIZIG'IDA QOLADI:** `padding: … max(1.25rem, calc((100% - 1200px) / 2))`. Bu yagona qoida ikkala talabni bajaradi — FON ekran bo'ylab cho'ziladi, KONTENT esa markazda banded holda turadi. Aks holda 1920px ekranda sarlavha bir chetda, rasm boshqa chetda qolib, hero bo'sh ko'rinardi. Qo'shimcha `div` ham, `100vw` hiylasi ham kerak bo'lmadi (`100vw` scrollbar enini hisobga olmaydi va gorizontal scroll berardi).
  - **⚠️ KONSTITUTSIYA RASMI UCHGA BO'LINDI — asosiy qaror.** Manba rasm 16:9 va uning MAZMUNI to'rtala chetida (chapda bayroq+kitob, o'ngda tarozi+bolg'a+lenta), butun ekran kengligidagi hero esa 3,1:1. Bitta rasm bilan ikkala talabni bajarib bo'lmasdi: `cover` → kenglikni to'ldiradi, lekin balandlikning **~43% i** qirqiladi (aynan o'sha bezaklar bilan); `contain` → hech nima kesilmaydi, lekin keng ekranda rasm o'rtada kichkina bo'lib qoladi. Yechim: `const-hero-left.jpg` · `const-hero-mid.jpg` (tarozi medalyoni) · `const-hero-right.jpg` — mustaqil qatlamlar, mos chetga biriktirilgan, balandlik bo'yicha to'ladi (`auto 100%`). **Istalgan ekran kengligida hech nima kesilmaydi**, ekran kengaysa faqat markazdagi krem maydon kengayadi (u yerda matn turadi).
    - Bo'laklarni qirqishda `sips --cropOffset` NOTO'G'RI natija berdi — u markazdan hisoblaydi, chetdan emas. Aniq qirqish `pngjs` bilan bajarildi.
  - **Kutubxona illyustratsiyasi endi rasmning O'ZIGA biriktirilgan nur bilan qo'shiladi.** Avval nur hero fonida foiz bilan berilgan edi (76%), lekin to'liq kenglikda rasm markazining foizi ekranga qarab suriladi (1440px da ~72%, 1920px da ~66%) — qat'iy foiz rasmni quvib yeta olmasdi. Endi nur `.heroArt` ning ichida, ya'ni rasm bilan birga siljiydi.
    - **Nur rangi = rasm chetlari o'tkazilgan rang (#f9f3e8)**, va gradient element chetida AYNAN shaffofga yetadi. Ikkovidan biri buzilsa to'rtburchak iz qaytadi. O'lchandi: **5 → 2/255**.
  - **⚠️ O'ZIM KIRITGAN VA TUZATILGAN XATO:** izoh matnini `/* … */` blokidan TASHQARIDA qoldirdim — CSS qoidasi yaroqsiz bo'lib, **illyustratsiya butunlay yo'qoldi**. Skrinshot buni darhol ko'rsatdi. Endi to'rtala hero CSS faylida izoh (`/*`/`*/`) va qavs muvozanati avtomatik tekshiriladi — 4/4 toza.
    - ⚠️ Shu sabab o'sha paytdagi "1/255" o'lchovi ham SOXTA edi: rasm umuman chizilmagani uchun o'lchanadigan chegara yo'q edi. Haqiqiy qiymat CSS tuzatilgandan keyin qayta olindi (2/255).
  - **Yo'l-yo'lakay:** `--hero-max-w` o'zgaruvchisi o'chirildi — u endi hech qayerda ishlatilmasdi va "kenglik shu yerdan boshqariladi" degan yanglish taassurot berardi. Balandlik esa avvalgidek yagona joyda: `--hero-h: 460px`.
  - **Tekshirildi:** CSS sinflari 269/269 (to'rtala sahifa) · izoh/qavs muvozanati 4/4 · to'rtalasi ham `min-height: var(--hero-h)` + bir xil `padding-inline` formulasi ishlatadi · Konstitutsiya va Kutubxona hero'lari 1440px da brauzerda ko'z bilan tasdiqlandi · frontend va admin build toza.

- **KUTUBXONA HERO'SI TAYYOR BANNER BILAN ALMASHTIRILDI (2026-08-11):** foydalanuvchi tayyor banner berdi va "shunchaki almashtirib ber" dedi. `public/library-hero.jpg` (1280×426, 110 KB) — unda sarlavha, tavsif va pastdagi to'rtta belgi ("Ishonchli manba · Dolzarb hujjatlar · Qulay izlash · Huquqiy axborot") ALLAQACHON chizilgan.
  - **HTML matn OLIB TASHLANDI** — sarlavha, tavsif, nishon, oltin ajratgich, boshoq, nuqta to'rlari, ichki ramka va o'ngdagi illyustratsiya. Ular rasm ustiga tushib, ekranda IKKI MARTA ko'rinardi. Shu bilan birga oldingi bosqichda qurilgan hero CSS'i (≈150 qator) ham o'chirildi.
  - **⚠️ `<h1>` QOLDIRILDI, faqat ko'rinmaydi** (`.srOnly`). Sahifada sarlavha bo'lishi shart: uni ekran o'quvchisi o'qiydi va qidiruv tizimi indekslaydi — rasmdagi matn ularga yetib bormaydi. Loyihada bunday yordamchi sinf yo'q edi, shu sabab qo'shildi.
  - **⚠️ BALANDLIK `--hero-h` DAN EMAS, RASM NISBATIDAN** (`aspect-ratio: 1280 / 426` ≈ 3:1). Sabab o'lchandi: 460px ga qotirilsa `cover` YON tomonlardan qirqadi va bu aynan keng tarqalgan noutbuk kengliklarida sodir bo'ladi — 1200px da ~182px yo'qoladi, ya'ni chapdagi bezak va o'ngdagi bayroq kesiladi. **Narxi:** bu hero qolgan uchtasiga (460px) faqat ~1380px kenglikda tenglashadi. Ikkalasini bir vaqtda saqlab bo'lmaydi — tayyor banner o'z nisbatiga ega. Qaytarish oson: `aspect-ratio` o'rniga `min-height: var(--hero-h)`.
  - **⚠️ TELEFON UCHUN ALOHIDA QOIDA ATAYLAB YO'Q.** `min-height` qo'yilsa 3:1 banner tor ekranda yon tomonlaridan qirqilib, sarlavhaning yarmi yo'qolardi. Nisbat saqlangani uchun banner 390px da ham butun holda ko'rinadi (130px balandlikda) — tekshirildi.
  - **⚠️ BILINGAN CHEKLOV — TIL:** bannerdagi matn O'ZBEK tilida "pishirilgan", ya'ni rus tiliga o'tilganda ham o'zbekcha qoladi. Buni kod hal qila olmaydi — ruscha variant uchun ALOHIDA rasm kerak (keyin `lang` bo'yicha almashtirish mumkin).
  - **Yo'l-yo'lakay:** `Library.jsx` dan `Scale` ikonasi va `LibrarySprigArt` importlari olib tashlandi (endi ishlatilmaydi). **Natijada `components/HeroArt.jsx` BUTUNLAY ishlatilmay qoldi** — uning uchala eksporti ham (`LibrarySprigArt`, `ConstBookArt`, `ConstScalesArt`) hech qayerdan chaqirilmaydi. Fayl ATAYLAB o'chirilmadi (vazifa "rasmni almashtirish" edi) — o'chirish kerak bo'lsa alohida aytilsin.
  - **Tekshirildi:** Library sinflari 23/23 mavjud, o'lik hero qoidasi 0, izoh/qavs muvozanati toza, build toza, 1440px va 390px da brauzerda ko'rildi.

- **KUTUBXONA HERO'SI — MATNSIZ BANNER + HTML MATN (2026-08-11):** foydalanuvchi avval matni "pishirilgan" banner berdi, keyin esa AYNAN o'shaning MATNSIZ variantini berib, "ustiga kerakli textlarni yozib ber" dedi. Bu oldingi qadamdagi til muammosini butunlay hal qildi.
  - **Rasm** — `public/library-hero.jpg` (1280×640, 119 KB): faqat bezaklar (oltin ramka, tepadagi medalyon, to'rtta ikona doirasi, o'ngdagi navy kompozitsiya). Matn rasmda YO'Q.
  - **Matn HTML da:** sarlavha (`feat_library_title`), tavsif (`library_sub`) va to'rtta yorliq — yangi kalitlar `library_perk1..4` (uz/ru). **Foyda:** rus tiliga o'tilganda matn ham o'giriladi, ekran o'quvchisi o'qiydi, qidiruv tizimi indekslaydi — avvalgi bannerda bularning hech biri yo'q edi.
  - **⚠️ JOYLASHUV RASMDAN O'LCHANGAN, taxmin qilinmagan.** `pngjs` bilan piksel tahlili: to'rtta ikona markazi x=181·331·475·617, y=456; doira diametri 66px; ostidagi oltin chiziq y=514; kitob ikonasi (117,165); medalyon (458,86). Barcha qiymatlar FOIZDA yozildi — rasm `cover` bilan element ustiga 1:1 yotgani va nisbat qat'iy bo'lgani uchun matn har qanday kenglikda o'z joyida qoladi.
  - **⚠️ SHRIFT `vw` DA, `px` DA EMAS** — rasm ekran bilan kattalashadi, matn ham u bilan birga o'sishi kerak. `px` bo'lsa katta ekranda matn rasmga nisbatan kichrayib, kompozitsiya buzilardi.
  - **⚠️ IKKI JOYLASHUV XATOSI TOPILDI VA TUZATILDI (render qilib ko'rilgach):** (1) sarlavha tepadagi medalyon va oltin bezak chizig'i USTIGA tushdi — sabab: `top` butun blokning (sarlavha+tavsif) markazini belgilaydi, sarlavha satrini emas; 25,8% → 36% ga ko'chirildi. (2) yorliqlar oltin chiziqqa tegib turardi — doira osti (76,4%) bilan chiziq (80,3%) orasida atigi 25px bor; yorliqlar chiziq OSTIGA (83%) o'tkazildi, u yerda ~40px bo'sh joy bor.
  - **⚠️ TOR EKRANDA TUZILMA O'ZGARADI (≤900px) — bu kichraytirish emas, boshqa joylashuv.** O'lchandi: rasm ustidagi `vw` matn 1024px da chegaraga keladi, 768px da tavsif 11px / yorliq 8px, 390px da 7px / 5px — o'qib bo'lmaydi. Matnni kattalashtirish yordam bermaydi: banner 2:1 va 390px da atigi ~195px balandlikda, ya'ni JOYNING O'ZI yo'q. Shuning uchun 900px dan pastda banner oddiy RASM bo'lib qoladi, matn esa uning OSTIDA, to'liq o'qiladigan o'lchamda (yorliqlar 2×2). Rasm shu sabab alohida `.heroImg` elementiga chiqarildi.
  - **BALANDLIK — ATAYLAB boshqa uchtadan FARQ QILADI (foydalanuvchi qarori).** Avval "to'rtala hero bir xil nisbat olsin" deb tanlangan edi (hammasi 1440px da 720px), lekin ko'rgandan keyin foydalanuvchi "3 ta sahifa hero o'z holiga qaytsin, library shu holicha tursin" dedi. Yakuniy holat: **Konstitutsiya · Darslar · Testlar — qat'iy 460px** (`--hero-h`), **Kutubxona — banner nisbati** (1280/640 → 1440px da 720px). Sabab: 460px ga qotirilsa `cover` bannerni tepa/pastdan ~36% qirqib, medalyon va to'rtta ikona yo'qolardi.
  - **⚠️ O'ZIM IKKINCHI MARTA YO'L QO'YGAN XATO:** izoh matnini `/* … */` blokidan TASHQARIDA qoldirdim — bu safar `global.css` da. Natijada CSS parseri xatodan keyingi birinchi `;` gacha bo'lgan qismni tashlab yubordi va `--hero-ratio` E'LON QILINMAY qoldi; hero'lar 720px o'rniga 460px bo'lib chiqdi. Endi **barcha CSS fayllar avtomatik tekshiriladi**: izoh muvozanati, qavs muvozanati va izohdan tashqarida qolgan matn (28 fayl, 0 xato).
  - **Yo'l-yo'lakay:** `--hero-max-w` (ishlatilmasdi) va vaqtincha qo'shilgan `--hero-ratio` (endi faqat bitta fayl uchun kerak, o'z joyiga ko'chirildi) `global.css` dan olib tashlandi. `HeroArt.jsx` hamon butunlay ishlatilmaydi — o'chirilmadi, alohida ruxsat kerak.
  - **Tekshirildi:** CSS 28 fayl toza · sinflar 264/264 · tarjima kalitlari 4/4 ikkala tilda · 1920/1440/1280/1024px da balandliklar o'lchandi · desktop, mobil (390px) va ruscha variant brauzerda ko'rildi · frontend va admin build toza.

- **ILOVA QOBIG'I — ro'yxatdan o'tganlar uchun sidebar'li interfeys (2026-08-19):** foydalanuvchi rasm (mockup) berdi: "ro'yxatdan o'tgan user'larga shunday ko'rinsin, ro'yxatdan o'tmaganlarga esa hozirgi holatda turaversin reklamniy sayt bo'lib, sahifalar sidebar'ga ko'chirilsin". Ish uch bosqichda, foydalanuvchi izohlari bilan aniqlashtirib bajarildi.
  - **ARXITEKTURA QARORI — `SiteLayout.jsx` GA UMUMAN TEGILMADI.** Tanlov `components/LayoutSwitch.jsx` da: mehmon → `SiteLayout` (reklama sayti, bir piksel ham o'zgarmagan), ro'yxatdan o'tgan + keng ekran → `AppShell`. Muqobil yo'l (683 qatorlik `SiteLayout` ichiga shart qo'yish) rad etildi — u ishlab turgan mehmon oqimini buzish xavfini tug'dirardi. `AppShell` `lazy` yuklanadi, ya'ni mehmon uni umuman tortmaydi.
  - **⚠️ TELEFONDA SIDEBAR YO'Q (foydalanuvchi qarori).** Chegara **1025px** (`LayoutSwitch` dagi `matchMedia`, `AppShell.module.css` dagi mobil chegara bilan bir xil). Undan pastda ro'yxatdan o'tgan foydalanuvchi ham AVVALGI qobiqni oladi: yuqorigi panel + burger menyu + pastki mobil panel. Dastlab sidebar telefonda "drawer" bo'lib ochilardi, lekin foydalanuvchi aniq "telefonda butunlay eski ko'rinish" variantini tanladi.
  - **Yangi fayllar:** `components/LayoutSwitch.jsx` · `components/app/` (`AppShell.jsx`, `AppSidebar.jsx`, `AppTopbar.jsx`, `AppUserMenu.jsx`, `appNav.js` + 4 ta CSS) · `components/ScrollTopButton.jsx` + CSS · `pages/Chat.jsx` · `pages/Settings.jsx` + CSS · `pages/Pricing.jsx`. Barcha JS/JSX fayllar 200 qatordan kam (shu sabab `App.jsx` dan `LayoutSwitch` ajratildi).
  - **⚠️ CHAT VA PROFIL MANTIG'I NUSXALANMADI — REJIM QO'SHILDI.** `ChatDrawer.jsx` (874 qator) ga `embedded` propi: sahifa rejimida qoplama, yopish tugmasi va `body` qulfi bo'lmaydi, `isOpen` o'rniga `active = embedded || isOpen`. Diff'da AI mantig'iga (sessiya, tarix, limit, rasm, ovoz, provayder zanjiri) bitta ham o'zgarish yo'q. Xuddi shunday `ProfileModal.jsx` ga `inline` propi — Sozlamalar SAHIFASI uni shu rejimda chizadi, mobil qobiq esa hamon MODAL sifatida ochadi. Ikkalasi ham bitta manba bo'lib qoldi.
  - **`/chat`** — AI Yordamchi endi suzuvchi panel emas, sahifa. **Suzuvchi AI tugmasi (`FloatingConsultButton`) ilova qobig'idan olib tashlandi, reklama saytida QOLDI.** Lekin panelning O'ZI qobiqda saqlandi: darsdagi "Bu joyni tushuntirib ber" tugmasi `openChat(savol)` ni chaqiradi va u ishlashda davom etishi shart. `/chat` sahifasida panel chizilmaydi — aks holda ikkita chat oynasi ochilardi.
  - **`/settings`** — Sozlamalar modal emas, sahifa: chapda hisob (profil, tarif, parol, Telegram, chiqish), o'ngda saytga oid (interfeys tili, tariflar, huquqiy hujjatlar). `?payment=success` endi Profil oynasini emas, `/settings?tab=pro` ni ochadi.
  - **`/pricing`** — "Tarif va to'lovlar" avval `/#pricing` ga, ya'ni REKLAMA sahifasiga tushirardi va foydalanuvchi hero/FAQ/marketing bo'limlarini aylanib o'tishga majbur edi. Endi alohida sahifa; ichida mavjud `PricingSection` ishlatiladi, shuning uchun narx/limit/to'lov oqimi Landing Page bo'limi bilan hech qachon ajralib ketmaydi. `ProfileModal` dagi `/#pricing` havolasi ham `/pricing` ga o'tkazildi.
  - **Sidebar tarkibi (foydalanuvchi tuzatishlaridan keyin):** AI Yordamchi · Darslar · Testlar · Konstitutsiya · Qonunlar kutubxonasi · Tarif va to'lovlar → [limit kartasi] → **Sozlamalar** → ajratuvchi chiziq → Sidebarni yashirish. **"Bosh sahifa" va "Profil" bandlari ATAYLAB yo'q** — Bosh sahifaga logo orqali, profilga esa Sozlamalar orqali boriladi (ikkita alohida yo'l chalg'itardi).
  - **Yuqorigi panel:** qidiruv QO'SHILGAN edi, keyin foydalanuvchi so'roviga ko'ra OLIB TASHLANDI (saytda global qidiruv endpointi yo'q — kutubxona qidiruvi faqat tanlangan kodeks ichida ishlaydi). Foydalanuvchi tugmasi bosilganda **ochiluvchi ro'yxat** chiqadi (Sozlamalar · Chiqish) — darhol o'tib ketmaydi. Panel rangi **`#101a2e`** — sidebar gradientining TEPA nuqtasi, shu sabab ikkovi tutashgan joyda chegara ko'rinmaydi.
  - **"Yuqoriga" tugmasi** (`ScrollTopButton`) — 400px scroll'dan keyin o'ng pastda. Ikkala qobiqda ham, lekin `SiteLayout` da FAQAT `{user && ...}` — mehmon sayti o'zgarmasligi uchun. `raised` propi: reklama qobig'ida ostida suzuvchi AI tugmasi turadi (54px chetdan, 70px), shuning uchun 140px ga ko'tariladi; ≤768px da FAB allaqachon yashiringan, tugma esa pastki panel ustida (`--bottom-nav-space`).
  - **⚠️ SAHIFA ALMASHGANDA BUTUN SAYT YO'QOLARDI — topildi va tuzatildi.** Yagona `<Suspense fallback={null}>` `App.jsx` da, `<Routes>` dan YUQORIDA turardi. Lazy sahifaga o'tilganda React eng yaqin chegarani topib, BUTUN daraxtni — yon menyu va yuqorigi panel bilan birga — `fallback` ga almashtirardi. Endi chegara ikkala qobiqda `<Outlet />` ni o'raydi; `App.jsx` dagi tashqi `Suspense` SAQLANDI (qobiqning o'zi lazy, hamda `/login`, `/register`, `/voice` qobiqdan tashqarida). O'lchandi: 6 sahifa bo'ylab o'tishda **306 namuna, 0 ta bo'sh kadr**.
  - **⚠️ SKRINSHOT TUZOG'I QAYTA UCHRADI:** macOS Chrome oynasining eng kichik eni 500px, shuning uchun `--window-size=390` bilan olingan rasm "matn chetga chiqib ketgan" degan SOXTA signal berdi. Haqiqiy o'lchov `iframe` ichida olindi — chiqish yo'q edi. Bu tuzoq loyiha tarixida allaqachon qayd etilgan, lekin baribir bir marta chalg'itdi.
  - **Yo'l-yo'lakay tuzatilgan 3 nuqson:** (1) tarif nomi `planInfo` javobi kechikkanda "Bepul" bo'lib qolardi, holbuki avatar oltin halqali edi — endi nom ham `activePlan` dan olinadi; (2) yig'ilgan sidebarda (78px) limit matni tashqariga chiqib, kontent ustiga tushardi — matn alohida `<span>` ga olinib yashiriladi; (3) mobil bekor qilishda `display: initial` ishlatilgan edi — u `inline` degani va flex ustunni buzardi, har biriga o'z qiymati berildi.
  - **Yangi tarjima kalitlari (uz/ru):** `app_settings`, `app_billing`, `app_menu`, `app_hide_sidebar`, `app_show_sidebar`, `app_limit_title`, `app_limit_unit`, `app_limit_unlimited`, `app_upgrade`, `app_user_paid`, `app_user_free`, `settings_sub`, `settings_site`, `settings_lang`, `settings_lang_hint`, `settings_billing_hint`. "Yuqoriga" uchun yangi kalit KERAK BO'LMADI — mavjud `const_top` ishlatildi. Vaqtincha qo'shilgan `app_search_ph` qidiruv olib tashlangach o'chirildi.
  - **Tekshiruv:** frontend va admin build toza · CSS sinf murojaatlari 0 xato · izoh/qavs muvozanati toza · tarjima kalitlari **565/565** ikkala tilda teng · **gorizontal chiqish 0**: ro'yxatdan o'tgan 9 sahifa × 8 kenglik (320–1920) × 2 til = 144/144 OK, mehmon 3 sahifa × 8 × 2 = 48/48 OK · mehmon saytida "Yuqoriga" tugmasi YO'Q va suzuvchi AI tugmasi BOR (tasdiqlandi) · desktop, mobil va ruscha variant brauzerda ko'z bilan ko'rildi.
  - **Tegilmagan:** `SiteLayout.jsx` ning tuzilishi (faqat 2 qator qo'shildi: `ScrollTopButton` va `Suspense`) · `BottomNav.jsx` · AI/RAG backend · `PricingSection` · admin paneli · barcha sahifa komponentlari (Home, Lessons, Tests, Library, Constitution).
  - **⚠️ BILINGAN CHEKLOV:** `SiteLayout.jsx` da ro'yxatdan o'tgan foydalanuvchiga oid eski kod (dropdown, `NAV_USER`, avatar, `pendingCheckout`, `payment=success`, `ProfileModal`) hamon bor va TELEFONDA ishlatiladi — ya'ni o'lik kod EMAS. Desktopda esa u ishga tushmaydi. Mobil menyuda `/chat`, `/settings` va `/pricing` ga havola YO'Q (foydalanuvchi "telefonda butunlay eski ko'rinish" dedi) — telefonda AI pastki paneldan, profil esa burgerdagi "Profil" modalidan ochiladi.

- **SEO / AEO / GEO / LLMO QATLAMI KUCHAYTIRILDI — Savol-javob markazi, entity grafi, E-E-A-T va Markdown nusxalari (2026-08-19):** foydalanuvchi "SEO, AEO, GEO, LLMO kabi tuzilmani juda kuchli qilib ber" dedi. Mavjud qatlam allaqachon kuchli edi (`seo/` moduli, ~7 100 modda sahifasi, `llms.txt`, sitemap index) — shu sabab noldan qurilmadi, faqat HAQIQIY bo'shliqlar to'ldirildi.
  - **⚠️ 1-BO'SHLIQ — ENTITY GRAFI UZUQ EDI.** Ochiq qonun sahifalarida (`/qonun/...`) faqat `Legislation` va `BreadcrumbList` bor edi. AI botlari (GPTBot, PerplexityBot, ClaudeBot) eng ko'p AYNAN shu sahifalarni o'qiydi — ular login talab qilmaydi va tayyor HTML beradi. Lekin `Organization` sxemasi faqat `index.html` da, ya'ni SPA ichida turardi: bot 7 100 ta modda sahifasini o'qib, ularni hech qanday manba bilan bog'lay olmasdi. Yangi `services/seoPages/entity.js` — `Organization` + `WebSite` tugunlari HAR BIR ochiq sahifaga qo'shiladi, `layout.js` esa har sahifaga `WebPage` tugunini yasab, uni `publisher`/`isPartOf` orqali grafga ulaydi. **⚠️ `@id` qiymatlari `index.html` dagilar bilan AYNAN bir xil** (`#organization`, `#website`) — farq qilsa qidiruv tizimi ikkita ALOHIDA tashkilot bor deb hisoblab, ishonchni ikkiga bo'lardi.
  - **⚠️ 2-BO'SHLIQ — AEO uchun SAVOL SHAKLIDAGI SAHIFA YO'Q EDI.** `/qonun/MK/106` "Mehnat kodeksi 106-modda" so'roviga javob beradi, lekin odam ham, AI tizimi ham qonunni MODDA RAQAMI bilan emas, SAVOL bilan qidiradi ("mehnat shartnomasi yozma bo'lishi shartmi?"). Yangi **Savol-javob markazi**: `/savollar` (ro'yxat) va `/savol/{slug}` (**65 ta savol**, 10 ta huquq sohasi). Har biri `QAPage` sxemasi bilan, `acceptedAnswer.citation` da qaysi qonun moddasiga tayanishi ko'rsatiladi, `speakable` bilan ovozli qidiruvga tayyor.
    - **⚠️ JAVOB MATNIGA ANIQ RAQAM YOZILMAGAN** — muddat, foiz, summa va modda raqami yo'q. Har bir savol `refs` orqali HAQIQIY moddaga bog'langan va matn `LegalChunk` dan JONLI olinadi (`seoPages/qaData.js` → `attachQuotes`). Ya'ni sahifada aniq raqam KO'RINADI (masalan aliment miqdori), lekin u lex.uz yangilanganda o'z-o'zidan yangilanadi. Bu loyihaning eng qat'iy qoidasi va darslar tizimidagi bilan bir xil naqsh.
    - **⚠️ 174 TA HAVOLA BAZADAN TEKSHIRILGAN** — yangi `scripts/verifyQaRefs.js` (`174/174, 0 xato`). Skript faqat "modda bormi" emas, topilgan moddaning SARLAVHASINI ham chiqaradi: mavjud, lekin BOSHQA mavzudagi moddaga havola sahifada ishonarli ko'rinadigan, lekin noto'g'ri iqtibos berardi.
    - **Sohalar va savollar soni:** mehnat 8 · oila 8 · meros 7 · uy-joy va yer 7 · iste'molchi 7 · shartnoma va fuqarolik 7 · jinoyat jarayonidagi huquqlar 7 · ma'muriy 4 · soliq 4 · konstitutsiyaviy huquqlar 6.
    - **Kontent `services/qaContent/` da**, har soha ikki faylga bo'lingan (200 qator qoidasi) — 18 ta kontent fayli + `areas.js` + `index.js`. `index.js` MODUL YUKLANISHIDA tekshiradi: slug takrorlanishi, noma'lum soha, ikkala tilda matn borligi, havola borligi. Bu xatolar "jim" turadi — savol shunchaki ro'yxatdan tushib qolardi.
  - **⚠️ 3-BO'SHLIQ — E-E-A-T (YMYL) SIGNALLARI YO'Q EDI.** Huquq — "Your Money or Your Life" sohasi: Google va AI tizimlari manbani baholashda ma'lumot QAYERDAN olinishi, QANDAY tekshirilishi va KIM javobgarligini talab qiladi. Yangi **`/metodologiya`** sahifasi (`seoPages/methodPage.js` + `methodText.js`, ikkala tilda qo'lda yozilgan): manba (faqat lex.uz), yangilanish tartibi, RAG zanjiri, nima tekshirilishi va cheklovlar (advokat emas, faqat O'zbekiston). `Organization.publishingPrinciples` shu sahifaga ishora qiladi — bot uchun aniq signal. Sahifadagi raqamlar (hujjatlar va moddalar soni) BAZADAN olinadi, qattiq yozilmagan.
  - **⚠️ 4-BO'SHLIQ — LLM UCHUN MARKDOWN NUSXALARI YO'Q EDI.** HTML da navigatsiya, uslub va CTA bloklari bor — til modeli uchun bular shovqin. Endi har bir ochiq sahifaning `.md` varianti bor: `/qonunlar.md` · `/qonun/{KOD}.md` · `/qonun/{KOD}/{MODDA}.md` · `/savollar.md` · `/savol/{slug}.md` · `/metodologiya.md` (`seoPages/markdown.js`). HTML sahifa `<link rel="alternate" type="text/markdown">` orqali o'z nusxasiga ishora qiladi, ya'ni bot uni O'ZI topadi. `<head>` ga `llms.txt` havolasi ham qo'shildi va `public/.well-known/llms.txt` nusxasi yaratildi (ba'zi tizimlar aynan o'sha katalogda qidiradi).
    - **⚠️ MARSHRUT TARTIBI HAL QILUVCHI:** `.md` marshrutlari HTML marshrutlaridan OLDIN ro'yxatdan o'tkazilishi SHART. Express qoidalarni tartib bo'yicha tekshiradi va `/qonun/:code/:article` naqshi `/qonun/MK/106.md` ni ham tutib olardi (article = "106.md") — natijada bot Markdown so'raganda HTML olardi. Shu sabab `routes/publicSeoMd.js` ALOHIDA fayl va `server.js` da `publicSeo` dan OLDIN ulanadi; `publicQa.js` ichida ham `.md` marshruti yuqorida turadi.
  - **⚠️ 5-BO'SHLIQ — `robots.txt` YANGI MARSHRUTLARDAN XABARSIZ EDI.** 2026-08-19 dagi ilova qobig'i ishida qo'shilgan `/chat`, `/settings`, `/pricing` marshrutlari ro'yxatda yo'q edi. `/pricing` ayniqsa muhim: u `/#pricing` bilan BIR XIL tariflarni ko'rsatadi, ya'ni indekslansa takroriy kontent hosil bo'lardi. Uchalasi `Disallow` ga qo'shildi; `/savollar`, `/savol/`, `/metodologiya` va `/*.md$` esa `Allow` ga.
  - **Yangi fayllar:** `services/seoPages/` — `entity.js`, `methodPage.js`, `methodText.js`, `qaPages.js`, `qaAnswerPage.js`, `qaData.js`, `markdown.js` · `services/qaContent/` — 20 fayl · `routes/publicQa.js`, `routes/publicSeoMd.js` · `scripts/verifyQaRefs.js` · `frontend/public/.well-known/llms.txt`.
  - **O'zgargan fayllar:** `seoPages/layout.js` (entity grafi + `mdPath` + `ogType` + nav/footer havolalari), `seoPages/meta.js` (yangi matnlar va yo'llar), `seoPages/styles.js` (savol-javob uslublari), `seoPages/articlePage.js` va `listPages.js` (`mdPath`), `server.js` (3 ta router), `frontend/index.html` (`llms.txt` havolasi, `publishingPrinciples`, `WebSite.hasPart`, `<noscript>` da ochiq bo'limlar ro'yxati), `vercel.json` (7 ta yangi rewrite), `robots.txt`, `sitemap.xml`, `llms.txt`, `llms-full.txt`, `SiteLayout.jsx` (footerga 2 havola), `translations.js` (`footer_qa`, `footer_method`), `seo/siteMeta.js`.
  - **⚠️ `siteMeta.js` dagi `SECTIONS` O'CHIRILDI** — u hech qayerda ishlatilmasdi va CHALG'ITUVCHI edi: ichida `/#pricing`, `/#faq` kabi anchor manzillar bor edi, holbuki anchor manzillar sitemap'dan ATAYLAB olib tashlangan (fragment — sahifaning qismi, alohida sahifa emas). O'rniga `OPEN_PAGES` qo'shildi.
  - **⚠️ CHROME 500px TUZOG'I YANA UCHRADI:** macOS Chrome oynasining eng kichik eni 500px, shuning uchun `--window-size=320` bilan olingan skrinshot "matn chetga chiqib ketgan" degan SOXTA signal berdi (o'lchandi: `window.innerWidth = 500`, so'ralgan 320 emas). Haqiqiy o'lchov sahifalarni lokal faylga saqlab, `--allow-file-access-from-files` bilan iframe ichida olindi: **4 sahifa × 3 kenglik (390/360/320) = 12/12 OK, gorizontal chiqish 0**.
  - **Tekshiruv:** uchidan-uchiga audit **24/24** (entity grafi 6 sahifada, `QAPage.citation`, `speakable`, `AboutPage`→`Organization`, hreflang+canonical, 6 ta Markdown nusxasi, ruscha variant RASMIY rus matni bilan, sitemap 67 manzil, 404 va 301) · **65 savol × 3 variant (uz/ru/md) = 195/195 200 OK** · havolalar **174/174** · tarjima kalitlari **567/567** ikkala tilda teng · CSS izoh/qavs muvozanati toza · yangi fayllarning hech biri 200 qatordan oshmaydi · frontend va admin build toza · desktop va mobil brauzerda ko'z bilan ko'rildi.
  - **Tegilmagan:** AI/RAG tizimi (`legalAI.js`, `legalRetrieval.js`), `parseArticles()` va `ingestLaws.js`, darslar va testlar, tarjima tizimi, admin paneli, to'lov/tarif tizimi, `frontend/src/components/Seo.jsx` va `seo/schema.js` mantiqi, mavjud qonun sahifalarining tuzilishi.
  - **⚠️ BILINGAN CHEKLOV — DOMEN:** sayt `huquq-ai-xi.vercel.app` subdomenida. Bu SEO uchun jiddiy cheklov (subdomen = past ishonch, brend qidiruv yo'q) va uni kod bilan hal qilib bo'lmaydi. Foydalanuvchi hozircha shu domende qolishni tanladi. Domen olinganda o'zgartirilishi kerak: `seo/siteMeta.js`, `index.html`, `robots.txt`, `sitemap.xml`, `sitemap-sahifalar.xml`, `llms.txt`, `llms-full.txt`, `.well-known/llms.txt`, `vercel.json`, backend `PUBLIC_SITE_URL`.

- **AI G'ALATI JAVOB BERARDI — 3 ta sabab topildi va tuzatildi (2026-08-19):** foydalanuvchi mijozga saytni ko'rsatayotganda AI noto'g'ri javob berdi. Sinov savoli: *"Umumta'lim maktablari uchun Davlat va huquq asoslari fanidan dasturlarni yuklab ber menga"*. Sabab bitta emas, uchta mustaqil nuqson birlashgan edi.
  1. **⚠️ `needsAdvice()` — "menga" olmoshi shaxsiy holat deb hisoblanardi.** `PERSONAL_PATTERNS` ichida `\b(meni|menga|mendan|menda|mening)\b` bor edi. Lekin bu so'zlar AI'ga qaratilgan eng oddiy iltimosda ham uchraydi: "menga tushuntirib ber", "menga reja tuzib ber", "menga yuklab ber". Natijada shunday HAR QANDAY xabar `explanatory: false` bo'lib, shoshilinch maslahat qolipiga tushardi — "Hozir nima qiling / Huquqlaringiz / Murojaat" + promptdagi pichoqlab ketish NAMUNASI. Ya'ni maktab dasturini so'ragan odam "siz jinoyat qurboni bo'ldingiz, prokuraturaga murojaat qiling" uslubidagi javob olardi. Endi olmosh alohida `SELF_PRONOUN` ga chiqarildi va faqat `EVENT_MARKERS` (voqea belgisi: bo'shat/urib/bermay/aldab/uvolil…) bilan BIRGA kelganda shaxsiy holat hisoblanadi. Voqea ro'yxati ATAYLAB tor — keng naqsh (`\w+di`) allaqachon bir marta shu xatoni bergan.
  2. **⚠️ Tizimda "men fayl yubora olmayman" degan tushuncha UMUMAN yo'q edi.** Model mavjud bo'lmagan fayl nomi va havolalarni o'ylab topardi, `validateAnswer` esa havolalarni o'chirib tashlardi — natijada javob yanada mantiqsiz ko'rinardi ("quyidagi havoladan yuklab oling:" — havolasiz). Yangi `services/aiCapability.js` (106 qator): `isFileRequest()` fayl/material so'rovini aniqlaydi, `fileRequestReply()` esa halol va foydali javob qaytaradi (nima qila olishini, materialni qayerdan olish mumkinligini, saytdagi qaysi bo'limda borligini aytadi). **Javob ATAYLAB deterministik** — mijozga ko'rsatiladigan javob har safar bir xil va to'g'ri bo'lishi kerak, LLM'ga umuman borilmaydi (1 ms). O'quv materiali so'ralsa (`EDU_MATERIAL`) javobga rasmiy manba haqida qo'shimcha izoh kiradi.
     - **Ushlanmaydigan holatlar ATAYLAB:** "ariza/shikoyat/shartnoma yozib ber" — AI buni haqiqatan MATN sifatida tayyorlaydi; "106-moddani ko'rsat" — modda bazada bor va RAG beradi. Ikkala shart ham talab qilinadi: predmet (fayl/pdf/kitob/dastur/havola) VA fe'l (yuklab/yubor/ber/kerak).
     - **⚠️ KIRILL VA `\b` TUZOG'I:** JS'da `\b` faqat ASCII harflarni biladi, shuning uchun `\bскачай` HECH QACHON mos kelmaydi. Sinovda aniqlandi — ruscha so'rovlar umuman ushlanmasdi. Kirill naqshlari alohida va `\b` siz yozildi.
     - Qo'shimcha zaxira: system promptga 5- va 6-qoida qo'shildi (avval savolni aniq tushunish; fayl/havola bera olmasligini ochiq aytish, o'ylab topilgan havola/fayl nomi/telefon YOZMASLIK).
  3. **⚠️ RAG kalit so'z qidiruvi TASODIFIY moddalarni "tekshirilgan manba" sifatida qo'shardi.** `keywordSearch` 5+ harfli so'zlar bo'yicha `title`/`text` ichidan qidiradi. "Davlat va huquq asoslari" so'rovida qidiruv so'zlari "davlat", "huquq", "asoslari" bo'lib chiqardi — bular deyarli HAR BIR modda matnida bor, ya'ni ajratuvchi kuchi nol. Natijada 4 ta tasodifiy modda system promptga *"YUQORIDAGI MATN — yagona ishonchli manbang"* sarlavhasi bilan qo'shilib, javobni mavzudan chetga burardi. Endi umumiy huquqiy so'zlar (`huquq`, `qonun`, `davlat`, `modda`, `kodeks`, `respublika`, `o'zbekiston`, `asoslari`, `tartibi` + ruscha ekvivalentlari) `STOP_WORDS` ga qo'shildi. Mavzuni vektor qidiruv va kategoriya filtri allaqachon topadi, shuning uchun signal yo'qolmaydi.
  - **Tekshiruv:** naqsh sinovlari **21/21** (8 ta fayl so'rovi ushlandi, 11 ta oddiy huquqiy savol ushlanmadi, 2 ta ta'lim tarmog'i) · qaror mantig'i regressiyasi **27/27** (`needsAdvice` 17 holat, `isLegalQuestion` 9 holat) · uchidan-uchiga HAQIQIY AI bilan 6 savol: fayl so'rovi → 1 ms deterministik javob · "menga mavzular rejasini tuzib ber" → endi haqiqiy 10 mavzuli reja (avval maslahat qolipi) · "ajrashganda bola kimda qoladi" → OK 44-moddasiga asoslangan to'g'ridan-to'g'ri javob · "meni ishdan bo'shatishdi" → maslahat qolipi SAQLANDI · ruscha fayl so'rovi → ruscha javob · suhbat davomi ("buning uchun qanday hujjatlar kerak?") → kontekst saqlandi.
  - **Tegilmagan:** provayder zanjiri, `validateAnswer`, `checkSafety`, `isLegalQuestion`, hujjat generatsiyasi, rasm tahlili, `parseArticles()`/`ingestLaws.js`, darslar, tarjima tizimi, frontend, admin paneli.
  - **⚠️ BILINGAN CHEKLOV:** bepul modellar (Cloudflare llama-3.3-70b) baribir ba'zan aniq raqamni o'zidan yozadi (masalan "bir oylik ish haqi"), promptdagi `numbersRule` ga qaramay. Buni butunlay bartaraf etish faqat kuchliroq model (Claude) yoki javobni RAG matni bilan avtomatik solishtirish orqali hal bo'ladi — alohida ish talab qiladi.

- **HOSTING RENDER'DAN RAILWAY'GA KO'CHIRILDI (2026-08-22):** backend endi `https://huquq-ai-production.up.railway.app` da (Render `huquq-ai-2gvs.onrender.com` o'chiriladi). Frontend va admin avvalgidek Vercel'da — ular uchun faqat backend manzili almashdi.
  - **⚠️ ENG XAVFLI TUZOQ — `process.env.RENDER`.** Ikki joyda ("hosting'dami yoki lokaldami?" tekshiruvi) aynan shu o'zgaruvchi qattiq yozilgan edi: `routes/auth.js` → `OTP_CONSOLE_FALLBACK` va `server.js` → ishga tushish ogohlantirishi. Railway'da bunday o'zgaruvchi YO'Q, ya'ni `backend/.env` dagi `DEV_OTP_TO_CONSOLE=true` Railway o'zgaruvchilariga ko'chirilsa u JIM YOQILIB qolardi — foydalanuvchi "kod emailingizga yuborildi" xabarini olib, kodni hech qachon ololmasdi (kod Railway loglariga tushardi). Yechim: yangi `backend/utils/hosting.js` — `isHosted()` platformalarning O'ZI qo'yadigan o'zgaruvchilariga qaraydi (`RAILWAY_ENVIRONMENT`, `RAILWAY_PROJECT_ID`, `RENDER`, `FLY_APP_NAME`, `DYNO`). Kelajakda yana ko'chilsa faqat shu ro'yxatga bitta qator qo'shiladi. **`NODE_ENV=production` ATAYLAB ishlatilmadi** — u lokalda ham qo'yilishi mumkin va u holda lokal test yo'llari kutilmaganda o'chib qolardi.
  - **⚠️ IKKINCHI TUZOQ — `.env` da protokolsiz manzil.** Railway dashboard domenni `huquq-ai-production.up.railway.app` ko'rinishida (https:// siz) ko'rsatadi va `.env` ga aynan shunday ko'chirilgan edi. `apiBase.js` avval faqat oxirgi `/` ni kesardi, natijada `API_BASE` = `huquq-ai-production.up.railway.app/api` — bu NISBIY yo'l, axios uni Vercel domeniga nisbatan hisoblab har bir so'rovni backend o'rniga frontendga yuborardi va butun sayt jim ishlamay qolardi. Ikkala `apiBase.js` da `stripSlash` o'rniga `normalize()` — protokol yo'q bo'lsa qo'shadi (localhost/127.0.0.1 → `http://`, qolgani → `https://`).
  - **CORS uchinchi qatlam qo'shildi:** `selfPublicUrl()` — Railway har deploy'da `RAILWAY_PUBLIC_DOMAIN` ni o'zi qo'yadi, shuning uchun backendning o'z domeni ro'yxatga AVTOMATIK tushadi. Avval bu manzil `defaultOrigins` da qo'lda yozilgani uchun hosting almashishi bilanoq eskirgan bo'lib qoldi. `FRONTEND_URL` (.env) va `huquq-ai-*.vercel.app` naqshi — o'zgarishsiz.
  - **Railway konfiguratsiyasi:** yangi `backend/railway.json` (builder `DOCKERFILE`, `healthcheckPath: /health`, `restartPolicyType: ON_FAILURE`) va `backend/.dockerignore`. `Dockerfile` da: `npm ci --only=production` → `--omit=dev` (eski bayroq deprecated), `ENV NODE_ENV=production`, va `RUN mkdir -p uploads` — `uploads/` `.gitignore` da bo'lgani uchun repozitoriyda yo'q, `routes/chat.js` esa unga yozadi. **Servisning "Root Directory" `backend` bo'lishi SHART** — monorepo ildizida Dockerfile yo'q.
  - **Manzil almashtirilgan joylar:** `frontend/vercel.json` (10 ta SEO rewrite — `/qonunlar`, `/savollar`, `/metodologiya` va sitemap'lar backenddan keladi, manzil eskirsa bu sahifalar 404 bo'lardi), `frontend/index.html` (`preconnect`/`dns-prefetch`), ikkala `apiBase.js` `DEFAULT_REMOTE`, ikkala `.env`, `server.js` CORS. `admin/src/pages/Settings.jsx` dagi "API holati" havolasi endi QATTIQ YOZILMAYDI — `API_ORIGIN` dan olinadi (aynan shu havola hosting almashganda eskirib qolgan edi).
  - **⚠️ TELEGRAM BOT — IKKI JOYDA BIR VAQTDA ISHLAMASIN.** `telegram-bot.js` long polling ishlatadi (`{ polling: true }`). Render va Railway servislari bir vaqtda tirik bo'lsa Telegram `409 Conflict` qaytaradi va bot ikkalasida ham buziladi. Render servisi O'CHIRILISHI shart.
  - **Tegilmagan:** AI/RAG zanjiri, email provayderlar zanjiri va tartibi (SMTP baribir oxirgi — Railway portlarni bloklamasa ham HTTPS API ishonchliroq), to'lov tizimi, `parseArticles()`/`ingestLaws.js`, darslar, tarjima, admin panel mantiqi, `vite.config.js` dev proxy'lari (ular lokal backendga qaragan — to'g'ri).
  - **Tekshiruv:** Railway backend jonli (`/` va `/health` → 200) · ikkala build toza (frontend 1,28 s, admin 1,33 s) · build natijasida `onrender` satri QOLMADI, `railway.app` manzili ikkala bundle'da bor · `node --check` 4 ta o'zgargan backend faylida toza · `isHosted()` Railway o'zgaruvchilari bilan `true` va `hostingName()` "Railway" qaytardi.

- **MOBIL ILOVA — PWA "Ilovani yuklash" tugmasi (2026-09-11):** foydalanuvchi "saytda ilovani yuklash tugmasi bo'lsin, bosilganda telefonga o'tsin, React Native/Flutter shart emas" dedi. Sayt O'ZI ilova sifatida o'rnatiladi (PWA). `site.webmanifest` va ikonalar (192/512/180) allaqachon bor edi — faqat service worker va tugma yetishmasdi.
  - **Yangi fayllar:** `utils/pwaInstall.js` (`beforeinstallprompt` ni ushlaydi, SW'ni ro'yxatdan o'tkazadi, platformani aniqlaydi) · `components/InstallAppButton.jsx` (+CSS) · `components/InstallGuide.jsx` (+CSS, lazy) · `public/sw.js` · `public/offline.html`.
  - **⚠️ `pwaInstall.js` `main.jsx` da import qilinadi, komponentda EMAS** — `beforeinstallprompt` React chizilishidan oldin kelishi mumkin; kech ulangan tinglovchi uni o'tkazib yuborib, tugma native oynani hech qachon ocha olmasdi.
  - **⚠️ SW ATAYLAB MINIMAL:** faqat sahifa (navigate) so'rovlari tarmoqdan olinadi, internet yo'q bo'lsagina `offline.html`. JS/CSS/API KESHLANMAYDI — keshlangan eski `index.html` yangi deploy'dan keyin yo'q bo'laklarga murojaat qilib, oq ekran berardi. SW faqat production'da ro'yxatdan o'tadi (dev'da Vite HMR bilan aralashmasin). `vercel.json` da `/sw.js` uchun `max-age=0` sarlavhasi.
  - **Platforma bo'yicha xatti-harakat:** Android (Chrome/Edge/Samsung) → brauzerning O'Z o'rnatish oynasi, bir bosishda bosh ekranga · iPhone → Safari native oyna bermaydi, "Ulashish → Bosh ekranga qo'shish" qo'llanmasi · Telegram/Instagram ichki brauzeri → "Brauzerda ochish" qo'llanmasi · kompyuter → telefonda ochish qo'llanmasi + havola nusxalash + ikkinchi darajali "shu kompyuterga o'rnatish" · ilova ichida ochilgan bo'lsa (`display-mode: standalone`) tugma ko'rinmaydi.
  - **Joylashuv:** Bosh sahifa hero'sida (Telegram tugmasi yonida) va footer brend blokida. Tarjima kalitlari `install_*` (uz/ru).
  - **Tekshiruv:** headless Chrome — manifest xatolari 0, `installabilityErrors` 0, SW `activated`, gorizontal chiqish 0 (390px va 1440px), desktop'da `beforeinstallprompt` keldi. Native Android oynasini headless'da sinab bo'lmaydi — haqiqiy telefonda tekshirilishi kerak.
